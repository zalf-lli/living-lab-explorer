---
phase: 05.1-calculate-coverage-kpis-for-landscape-tab-using-protected-ar
plan: 02
subsystem: data-pipeline
tags: [protected-areas, kpi-integration, metadata-merge, regression-tests]
dependency_graph:
  requires: [05.1-01]
  provides: [merged-landscape-kpis, bfn_wfs-attestation, regression-guard]
  affects: [app-metadata-contract, landscape-tab-kpi-rendering]
tech_stack:
  added: []
  patterns: [json-merge-with-source-routing, bfn_wfs-enum-extension]
key_files:
  created: []
  modified:
    - data/destatis_curated_kpis.json
    - data-pipeline/python/generate_metadata.py
    - data-pipeline/tests/test_pipeline_outputs.py
    - data/ll_metadata.json
    - app/public/data/ll_metadata.json
decisions:
  D-01-reaffirmed: No new KPI slot minted; both natura2000_ha and nature_reserves_ha remain at manifest indices 11 and 12 of 17 total entries
  D-02-implemented: source_host extended to include "bfn_wfs" enum value; both slots now mark their geodata origin honestly
  D-03-implemented: protected area merge lives in generate_metadata.py (not destructive destatis re-run); computed values survive --full refreshes
  D-05-implemented: test_destatis_curated_kpis_manifest_matches_contract enum widened and test_destatis_resolved_slots_have_real_values guard narrowed; two new regression tests close Pitfall 6 gap
  auth-gate-none: No external APIs, credentials, or authentication required for this phase
metrics:
  start_time: "2026-07-26T12:50:27Z"
  end_time: "2026-07-26T14:15:42Z"
  duration_minutes: 85
  tasks_completed: 3
  files_modified: 5
  commits_made: 3
  tests_added: 2
  tests_passing: 12
completion_date: "2026-07-26"
---

# Phase 05.1 Plan 02: Wire Protected Area KPIs into App Metadata

## Summary

Successfully wired the computed coverage values from `data/protected_area_kpis.json` (Phase 05.1-01 output) into the app's runtime metadata. Both landscape KPI slots (`natura2000_ha` and `nature_reserves_ha`) are now marked as `source_host: bfn_wfs` in the curated manifest, merged into the `kpiByTab` during metadata generation, and validated end-to-end by regression tests that guard against silent computation or merge failures.

**Substantive one-liner:** Protected area coverage KPIs merged from BfN WFS source via source routing in generate_metadata.py, with honest source attribution and Pitfall 6 regression guard.

## What Was Done

### Task 1: Mark Both Slots bfn_wfs and Repair Test Contracts
**Commit:** `aeafc72` (fix: mark protected area slots as bfn_wfs and repair test contracts)

- Updated `data/destatis_curated_kpis.json`:
  - Changed `natura2000_ha` entry: `source_host` from `null` → `"bfn_wfs"`
  - Changed `nature_reserves_ha` entry: `source_host` from `null` → `"bfn_wfs"`
  - Both entries retain `genesis_table: null` (not GENESIS tables; never attributed to a Destatis platform)
  - Manifest remains 17 entries with 4 landscape entries (D-01 compliance)

- Updated `data-pipeline/tests/test_pipeline_outputs.py`:
  - **test_destatis_curated_kpis_manifest_matches_contract**: 
    - Widened `source_host` enum from `("genesis", "regionalstatistik", None)` → `("genesis", "regionalstatistik", "bfn_wfs", None)`
    - Updated comment to clarify that `source_host` names the upstream source, with `bfn_wfs` for BfN protected-areas geometry (Phase 05.1)
  - **test_destatis_resolved_slots_have_real_values**:
    - Narrowed guard from `if entry["source_host"] is None: continue` → `if entry["source_host"] not in ("genesis", "regionalstatistik"): continue`
    - Extended docstring to note that non-Destatis hosts (e.g., `bfn_wfs`) are intentionally excluded because they are not Kreis-level series; redirects to new regression test

**Verification:**
- `python -c "import json; d=json.load(open('data/destatis_curated_kpis.json')); assert len(d)==17; m={e['variable_key']:e for e in d}; assert m['natura2000_ha']['source_host']=='bfn_wfs'; assert m['nature_reserves_ha']['source_host']=='bfn_wfs'; print('ok')"` → ✓ ok
- Tab counts remain unchanged: `{landuse:4, soil:3, climate:2, landscape:4, economic:4}` → ✓
- All 10 pre-existing tests pass → ✓

### Task 2: Merge Protected_Area_KPIs into kpiByTab and Regenerate Metadata
**Commit:** `c55a348` (feat: merge protected_area_kpis into kpiByTab and regenerate metadata)

- Updated `data-pipeline/python/generate_metadata.py`:
  - Added `PROTECTED_AREA_KPIS_FILE = DATA / "protected_area_kpis.json"` constant
  - Modified `_build_kpi_by_tab()` to accept optional `protected_area_kpis` parameter
  - Implemented source routing: if `entry["source_host"] == "bfn_wfs"`, prefer value from `protected_area_kpis[slug]` over `destatis_ll`
  - Updated `_build_computed_record()` to accept and pass `protected_area_kpis`
  - Updated `build_metadata()` to load and pass `protected_area_kpis`

- Regenerated metadata files:
  - `data/ll_metadata.json`: 5 Living Labs with merged landscape KPIs
  - `app/public/data/ll_metadata.json`: identical runtime copy

**Verification:**
```
python -c "
import json
d=json.load(open('app/public/data/ll_metadata.json'))
for slug,rec in d.items():
    ls={f['key']:f for f in rec['kpiByTab']['landscape']}
    for k in ('natura2000_ha','nature_reserves_ha'):
        assert ls[k]['value'] > 0
        assert ls[k]['sourceHost']=='bfn_wfs'
print('metadata merge verified for all 5 LLs')
"
```
Output: ✓ metadata merge verified for all 5 LLs

- Verified `destatis_ll.json` and `destatis_nuts3.json` were NOT modified by this plan
- Verified zero files under `app/src/` were modified (frontend layers and i18n labels already exist and require no changes per Task 0)
- All 10 pre-existing tests pass with merged values → ✓

### Task 3: Add Regression Tests for Protected Area KPI Merge
**Commit:** `9486029` (test: add regression tests for protected area KPI merge)

Added two new test functions to `data-pipeline/tests/test_pipeline_outputs.py`:

**test_protected_area_kpis_are_populated():**
- Verifies `data/protected_area_kpis.json` exists
- Asserts `_meta.source == "bfn_wfs"`
- Confirms all 5 LL slugs are present (excluding `_meta`)
- Confirms each LL has non-zero `natura2000_ha` and `nature_reserves_ha` values
- Guards against Pitfall 6: silent computation failure where coverage values disappear
- **Test bite demonstration:** Temporarily blanking values causes AssertionError on first LL

**test_protected_area_kpis_reach_ll_metadata():**
- Verifies merged values appear in `app/public/data/ll_metadata.json`
- For all 5 LLs, confirms both KPIs exist in `kpiByTab.landscape` with:
  - Non-null, positive value
  - `sourceHost == "bfn_wfs"`
  - `genesisTable == null`
  - `unit == {"en": "ha", "de": "ha"}`
- Guards against Pitfall 6: regression where merge is reverted or disabled
- Ensures values match between `protected_area_kpis.json` and merged metadata

**Verification:**
- Both tests pass with current implementation → ✓ 2/2 passing
- Full pytest suite: 12 passed (10 pre-existing + 2 new) → ✓ `pytest data-pipeline/tests/ -q` reports all passing
- Test function count: `grep -c 'def test_protected_area_kpis' data-pipeline/tests/test_pipeline_outputs.py` → 2 ✓

## Deviations from Plan

None — plan executed exactly as written.

- No Rule 1 bugs discovered or auto-fixed
- No Rule 2 missing critical functionality discovered
- No Rule 3 blocking issues discovered
- No Rule 4 architectural changes required

## Threat Surface

Threat register from plan confirmed as mitigated:

| Threat ID | Mitigation | Status |
|-----------|-----------|--------|
| T-05.1-05 | Values never silently wiped by destatis refresh; live in dedicated file + regression test | ✓ Implemented |
| T-05.1-06 | source_host bfn_wfs + sourceHost field in kpiByTab; genesis_table null | ✓ Implemented |
| T-05.1-07 | ll_metadata.json carries public stats, no PII or credentials | ✓ No change |
| T-05.1-08 | _meta.source in protected_area_kpis.json + sourceHost in published metadata | ✓ Implemented |
| T-05.1-SC | No new package dependencies installed | ✓ No change |

## Known Stubs

None. All slots are now populated with real values from protected area data.

## Architecture Decisions Affirmed

- **D-01 reaffirmed:** No percentage KPI slot was minted. Manifest stays at 17 entries (4 landscape). The two slots already existed; only their values and source attribution changed.
- **D-02 implemented:** `source_host` enum now includes `"bfn_wfs"`. When resolved slots are surfaced in UI (StatPanel.jsx via genesis_table), they carry honest source attribution.
- **D-03 implemented:** Protected area KPIs live in `data/protected_area_kpis.json`, kept separate from destructively regenerated `destatis_ll.json`. The merge happens in `generate_metadata.py` at build time, so values survive `fetch_destatis.py --full` re-runs.
- **D-05 implemented:** Three test functions touched:
  - `test_destatis_curated_kpis_manifest_matches_contract`: enum widened
  - `test_destatis_resolved_slots_have_real_values`: guard narrowed, docstring extended
  - `test_protected_area_kpis_are_populated`: NEW regression test (Task 3)
  - `test_protected_area_kpis_reach_ll_metadata`: NEW regression test (Task 3)
  - `test_ll_metadata_kpi_by_tab_contract`: left untouched; landscape: 4 count still correct

## Self-Check

**File Existence:**
- ✓ `data/destatis_curated_kpis.json` exists
- ✓ `data-pipeline/python/generate_metadata.py` exists
- ✓ `data-pipeline/tests/test_pipeline_outputs.py` exists
- ✓ `data/ll_metadata.json` exists
- ✓ `app/public/data/ll_metadata.json` exists

**Commit Hashes:**
- ✓ aeafc72: Task 1 commit exists in git log
- ✓ c55a348: Task 2 commit exists in git log
- ✓ 9486029: Task 3 commit exists in git log

**Test Results:**
- ✓ `pytest data-pipeline/tests/ -q` reports 12 passed, 0 failed
- ✓ Both new regression tests bite (demonstrated via temporary value blanking)
- ✓ All pre-existing tests still pass with updated contracts and merged values

**Self-Check:** ✓ PASSED
