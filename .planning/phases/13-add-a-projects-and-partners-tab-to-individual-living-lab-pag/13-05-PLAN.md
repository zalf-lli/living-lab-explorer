---
phase: 13-add-a-projects-and-partners-tab-to-individual-living-lab-pag
plan: 05
type: execute
wave: 3
depends_on: ["13-03", "13-04"]
files_modified:
  - app/src/components/PartnersProjectsTab.jsx
  - app/src/components/LayerTabs.jsx
  - app/src/pages/LLDetail.jsx
autonomous: true
requirements: [D-04, D-05, D-09, D-10, D-14, D-17, D-18]
ui_decisions: [UI-SPEC Layout & Composition, UI-SPEC Component Tree, UI-SPEC Interaction States, UI-SPEC Spacing Scale]

must_haves:
  truths:
    - "A Partners & Projects control sits on the right-hand side of the layer tab row, visually separated from the five thematic tabs"
    - "Selecting it replaces the thematic content with the partner map and the partners/projects overview in all three layouts, including two-column comparison mode"
    - "The partners/projects file is fetched only after the tab is selected, and at most once per page load"
    - "The thematic layer-tab hint copy is not shown while the Partners & Projects tab is active"
  artifacts:
    - path: "app/src/components/PartnersProjectsTab.jsx"
      provides: "Composition root: one fetch, coordinate split, loading/error slot, map + panel stack"
      exports: ["PartnersProjectsTab"]
      min_lines: 60
    - path: "app/src/components/LayerTabs.jsx"
      provides: "Right-side second tab group for 'partners'"
      contains: "layers.partners"
    - path: "app/src/pages/LLDetail.jsx"
      provides: "Three content branch points plus three hint suppressions"
      contains: "PartnersProjectsTab"
  key_links:
    - from: "app/src/components/LayerTabs.jsx"
      to: "app/src/pages/LLDetail.jsx"
      via: "onChange('partners') feeding the existing useLayerState setter"
      pattern: "onChange\\('partners'\\)"
    - from: "app/src/pages/LLDetail.jsx"
      to: "app/src/components/PartnersProjectsTab.jsx"
      via: "layer === 'partners' branch at all three content composition sites"
      pattern: "layer === 'partners'"
    - from: "app/src/components/PartnersProjectsTab.jsx"
      to: "app/src/hooks/usePartnersProjects.js"
      via: "single hook call whose mount lifecycle is the lazy-fetch gate"
      pattern: "usePartnersProjects"
---

<objective>
Wire the feature together: the composition root that owns the single fetch and the D-14 coordinate
split, the right-side tab control that makes the tab reachable, and the three independent content
branch points in `LLDetail.jsx` that must all change together.

Purpose: `LLDetail.jsx` composes the map/KPI/chart/text stack three separate times by design
(13-RESEARCH.md Pitfall 1). Missing one silently breaks the tab in a layout nobody tests by default.
This plan enumerates all three sites, plus a third `layerTabsHint` occurrence in `LayoutCompare`
that 13-PATTERNS.md's two-site count missed.
Output: one new component, one modified tab control, one modified page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-CONTEXT.md
@.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-RESEARCH.md
@.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-PATTERNS.md
@.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-UI-SPEC.md
</context>

<interfaces>
<!-- Consumed contracts, all already landed -->

- `app/src/hooks/usePartnersProjects.js` -> `usePartnersProjects(slug)` returning
  `{ data: { partners, projects } | null, loading, error }`
- `app/src/lib/partnersProjects.js` -> `partitionPartnersByCoordinates(partners)` returning
  `{ mapped, unmapped }`
- `app/src/components/PartnersMap.jsx` -> DEFAULT export, props `{ ll, partners, height }`
- `app/src/components/PartnersOverviewPanel.jsx` -> named export `PartnersOverviewPanel`, props
  `{ partners, projects, lang }`
- `app/src/i18n.js` -> `normalizeLanguage(lang)`
- i18n keys available: `layers.partners`, `common.loading`, `common.loadingMap`,
  `partnersTab.loadErrorTitle`, `partnersTab.loadErrorBody`

<!-- Contract this plan creates -->

`app/src/components/PartnersProjectsTab.jsx` — named export `PartnersProjectsTab`, props
`{ ll, mapHeight = 300 }`. Receives no `layer`, `climateVariable`, `period`, `periodMode` or
`horizon` props — none of the thematic/raster machinery applies to this tab.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PartnersProjectsTab composition root</name>
  <files>app/src/components/PartnersProjectsTab.jsx</files>
  <read_first>
    - `app/src/pages/LLDetail.jsx` lines 1-30 and 1205-1245 — the `lazy(() => import(...))` plus
      `Suspense` convention used for `LLMap` (line 28), the local `MapFallback` (line 1211) and
      `LoadingCard` (line 1230, `padding: 40, color: C.muted, fontSize: 14, display: 'flex',
      justifyContent: 'center'`) treatments this component's status slot reuses.
    - `app/src/App.jsx` — the `ErrorBanner` treatment (`padding: 40`) and the app's
      no-global-error-boundary convention.
    - `app/src/hooks/usePartnersProjects.js` and `app/src/lib/partnersProjects.js` — the exact
      signatures being composed.
    - `app/src/i18n.js` — `normalizeLanguage`, the central helper this component must use.
    - `.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-UI-SPEC.md`
      sections "PartnersProjectsTab internal layout" and "Interaction States".
  </read_first>
  <action>
    Create `app/src/components/PartnersProjectsTab.jsx` with the named export
    `PartnersProjectsTab({ ll, mapHeight = 300 })`.

    Lazy-load the map at module scope with `lazy(() => import('./PartnersMap.jsx'))`. This mirrors
    `LLDetail.jsx` line 28's treatment of `LLMap` and exists to keep the Leaflet bundle out of the
    main chunk. `PartnersOverviewPanel` is imported normally (it has no Leaflet dependency).

    Call `usePartnersProjects(ll.slug)` exactly once. Resolve the language once with
    `normalizeLanguage(i18n.resolvedLanguage)` imported from `../i18n.js` and pass the result down as
    the `lang` prop — do not recompute it per card and do not inline a `startsWith('de')` ternary
    (13-RESEARCH.md Pitfall 5).

    Status slot — one shared slot for both regions, because both come from the single fetch:
    - while `loading`: render one centred block styled `padding: 40`, `color: C.muted`, `fontSize:
      14`, `display: 'flex'`, `justifyContent: 'center'` containing `t('common.loading')`, in place
      of BOTH the map and the panel. The `padding: 40` literal is inherited verbatim from
      `LoadingCard`/`ErrorBanner` and is a named UI-SPEC spacing exception — do not round it.
    - on `error`: render, in that same slot, `t('partnersTab.loadErrorTitle')` at `fontSize: 14`,
      `fontWeight: 700`, `color: C.teal` above `t('partnersTab.loadErrorBody')` at `fontSize: 12`,
      `fontWeight: 400`, `color: C.muted`. Inline only — no page-level banner, no `console.*`.

    Success state: a wrapper `div` with `display: 'flex'`, `flexDirection: 'column'`, `gap: 24`
    containing, in order, (1) a `Suspense` wrapping `PartnersMap` with props `ll={ll}`
    `partners={mapped}` `height={mapHeight}`, using a fallback that reuses the same centred
    `padding: 40` block showing `t('common.loadingMap')`, and (2) `PartnersOverviewPanel` with props
    `partners={data.partners}` `projects={data.projects}` `lang={lang}`.

    `mapped` comes from `partitionPartnersByCoordinates(data.partners).mapped` — this is the single
    D-14 decision point in the whole app. The overview panel receives the FULL `data.partners` array
    (coordinate-less partners still appear in the list, D-14), never the partition's `mapped` half.

    Render the map section unconditionally even when `mapped` is empty — a Living Lab with no
    coordinate-bearing partners still shows its boundary map, which is the tab's primary visual
    anchor.
  </action>
  <verify>
    <automated>cd app && grep -c "export function PartnersProjectsTab" src/components/PartnersProjectsTab.jsx && grep -c "usePartnersProjects" src/components/PartnersProjectsTab.jsx && grep -c "partitionPartnersByCoordinates" src/components/PartnersProjectsTab.jsx && grep -c "normalizeLanguage" src/components/PartnersProjectsTab.jsx && grep -c "PartnersMap.jsx" src/components/PartnersProjectsTab.jsx && grep -c "partnersTab.loadErrorTitle" src/components/PartnersProjectsTab.jsx && test "$(grep -v '^[[:space:]]*//' src/components/PartnersProjectsTab.jsx | grep -c 'console\.')" = "0" && test "$(grep -v '^[[:space:]]*//' src/components/PartnersProjectsTab.jsx | grep -c '\.unmapped')" = "0" && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `PartnersProjectsTab` is a named export with props `{ ll, mapHeight = 300 }` and calls
      `usePartnersProjects` exactly once.
    - `PartnersMap` is imported via `lazy(() => import('./PartnersMap.jsx'))` and rendered inside a
      `Suspense` boundary with a fallback.
    - `PartnersMap` receives `partitionPartnersByCoordinates(...).mapped`; `PartnersOverviewPanel`
      receives the unpartitioned `partners` array — the non-comment source contains zero occurrences
      of `.unmapped`.
    - The loading and error states occupy one shared slot; neither renders alongside the map or the
      panel.
    - Zero `console.` occurrences and zero inline `startsWith('de')` language derivation.
    - `npm run lint` and `npm run build` exit 0.
  </acceptance_criteria>
  <done>One component owns the single fetch, the language resolution, the D-14 split and the shared loading/error slot.</done>
</task>

<task type="auto">
  <name>Task 2: Right-side Partners and Projects group in LayerTabs</name>
  <files>app/src/components/LayerTabs.jsx</files>
  <read_first>
    - `app/src/components/LayerTabs.jsx` — the entire 50-line file: the outer wrapper div
      (`display: 'flex'`, `gap: 0`, `borderBottom: 2px solid ...`), the `LAYERS.map()` block and the
      per-button style object whose `padding: '9px 16px'` and `marginBottom: -2` literals must be
      reused verbatim.
    - `app/src/data/layers.js` lines 155-180 — the `LAYERS`/`OVERLAYS`/`LAYER_INDEX` split, to confirm
      `LAYERS` stays at exactly five entries.
    - `.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-UI-SPEC.md`
      section "Right-side tab group (D-04)" and spacing exceptions 2 and 3.
  </read_first>
  <action>
    Modify `app/src/components/LayerTabs.jsx` only. Add `justifyContent: 'space-between'` and
    `alignItems: 'flex-end'` to the existing outer wrapper div's style, wrap the existing
    `LAYERS.map(...)` output in an inner div styled `display: 'flex'`, `gap: 0`, and append one
    button element after that inner div as a second, visually distinct group (D-04).

    The new button calls `onChange('partners')` and renders `t('layers.partners')`. Its style reuses
    the existing pill's rules with `active === 'partners'` driving the active branch: `padding: '9px
    16px'`, `border: 'none'`, `background: 'none'`, `cursor: 'pointer'`, `fontSize: 13`, `fontWeight`
    700 when active else 500, `color` `C.lime` (dark) / `C.teal` (light) when active else
    `rgba(255,255,255,0.55)` (dark) / `rgba(2,35,34,0.5)` (light), `borderBottom` `2.5px solid` in the
    same active colour else `2.5px solid transparent`, `marginBottom: -2`, `transition: 'all 0.15s'`,
    `whiteSpace: 'nowrap'`. The `padding: '9px 16px'` and `marginBottom: -2` values are copied verbatim
    from the existing pill — they are named UI-SPEC spacing exceptions that keep all six buttons
    pixel-aligned in one row; do NOT round them to grid values.

    Its separator treatment, new to this phase and grid-exact: `borderLeft` of `1px solid` using
    `rgba(255,255,255,0.2)` in the dark variant and `C.mutedLight` in the light variant, plus
    `marginLeft: 8` and `paddingLeft: 16`.

    Do NOT add a `partners` entry to `LAYERS` in `app/src/data/layers.js`. `LayerTabs` maps over
    `LAYERS` and `LLMap` resolves `layerConfig.type`/`available` from it, so a sixth entry would both
    render the tab inline (violating D-04) and route `partners` into `LLMap`'s raster/vector branching
    (13-RESEARCH.md Pitfall 2). `app/src/data/layers.js` must be unchanged by this plan.
  </action>
  <verify>
    <automated>cd app && grep -c "onChange('partners')" src/components/LayerTabs.jsx && grep -c "layers.partners" src/components/LayerTabs.jsx && grep -c "justifyContent: 'space-between'" src/components/LayerTabs.jsx && grep -c "borderLeft" src/components/LayerTabs.jsx && test "$(grep -c "padding: '9px 16px'" src/components/LayerTabs.jsx)" = "2" && test "$(grep -c 'marginBottom: -2' src/components/LayerTabs.jsx)" = "2" && test -z "$(git diff --name-only -- src/data/layers.js)" && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The outer wrapper carries `justifyContent: 'space-between'` and `alignItems: 'flex-end'`, and
      the five thematic pills are wrapped in their own inner flex div.
    - Exactly two occurrences each of `padding: '9px 16px'` and `marginBottom: -2` exist in the file
      (the original pill plus the new button).
    - The new button carries `borderLeft`, `marginLeft: 8` and `paddingLeft: 16`.
    - `git diff` reports no change to `app/src/data/layers.js`, and `LAYERS` still has exactly five
      entries.
    - `npm run lint` and `npm run build` exit 0.
  </acceptance_criteria>
  <done>A visually separated Partners and Projects control sits at the right end of the tab row in both light and dark variants.</done>
</task>

<task type="auto">
  <name>Task 3: Branch all three LLDetail content sites and suppress the thematic hint</name>
  <files>app/src/pages/LLDetail.jsx</files>
  <read_first>
    - `app/src/pages/LLDetail.jsx` lines 400-570 — `LayoutSplit`: the tab header block (440-458), the
      `layerTabsHint` div (455-457), the map slot div wrapping `Suspense`/`LLMap` (459-473), the
      right-column content block `StatPanel` (500) plus chart card (502-531) plus text-block card
      (533-553), and the `CompareCTA`/`DownloadReportCTA` row (555-562).
    - `app/src/pages/LLDetail.jsx` lines 569-750 — `LayoutStacked`: `StatPanel` wrapper (620-622),
      the tabs-plus-map card (624-665) including its `layerTabsHint` div (648-650) and
      `Suspense`/`LLMap` (652-664), the chart card (667-697), the text grid (699-735) and the CTA row
      (737-744).
    - `app/src/pages/LLDetail.jsx` lines 752-905 — `ComparisonColumn`: accent bar (765), header
      (767-804), `StatPanel` (806-808), map card (810-832), chart card (834-864), text blocks
      (866-896) and the trailing spacer (898).
    - `app/src/pages/LLDetail.jsx` lines 907-955 — `LayoutCompare`: the shared tab header and its
      THIRD `layerTabsHint` occurrence (948-950), which 13-PATTERNS.md's "both occurrences" count
      omits.
    - `app/src/pages/LLDetail.jsx` lines 380-395 — `useLayerState`, whose default (`'landscape'`) and
      `startTransition` setter need no change; only the set of valid `layer` values widens.
    - `.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-UI-SPEC.md`
      section "Three LLDetail.jsx branch points" — the locked ternary pattern and the deliberate
      inclusion of comparison mode.
  </read_first>
  <action>
    Add `import { PartnersProjectsTab } from '../components/PartnersProjectsTab.jsx'` alongside the
    existing component imports. No lazy wrapper is needed here — `PartnersProjectsTab` itself
    lazy-loads the Leaflet-bearing `PartnersMap`.

    Apply four edits, all keyed on `layer === 'partners'`. Leave `useLayerState`, the header blocks,
    the LL badge/name/tagline blocks, `ContactManagerButton`, the `CompareCTA`/`DownloadReportCTA`
    rows, `LayoutSwitcher` and the comparison bar untouched.

    (a) `LayoutSplit` — render the map-slot div (and the `Suspense` and `LLMap` inside it) only when
    `layer !== 'partners'`; the left column then shows just the tab header strip. In the right column,
    replace the block running from `StatPanel` through the text-block card with a ternary: when
    `layer === 'partners'` render `PartnersProjectsTab` with prop `ll={ll}`, otherwise render the
    existing three blocks unchanged. The CTA row below stays outside the ternary.

    UI-SPEC resolution note for the executor: 13-UI-SPEC's ASCII diagram parenthetical about
    "LayoutSplit's map column keeps its own 100%-height slot" conflicts with its own explicit prose
    lock ("Single vertical stack, map on top, panel below — in every layout ... stacking avoids
    squeezing card text into a narrow column"). The prose lock wins: in `LayoutSplit` the partner map
    renders inside `PartnersProjectsTab` in the wide right column, and the narrow left column's map
    slot is suppressed. Do not render two maps.

    (b) `LayoutStacked` — render the `StatPanel` wrapper div and the `Suspense`/`LLMap` block only
    when `layer !== 'partners'`; the tabs-plus-map card keeps its wrapper and tab strip in both cases.
    Replace the chart card and the text grid with, when `layer === 'partners'`, a single div styled
    `margin: '16px 32px 0'` containing `PartnersProjectsTab` with prop `ll={ll}` — reusing the
    existing sibling blocks' `margin: '16px 32px 0'` so horizontal alignment with the rest of the
    stacked layout is preserved. The CTA row stays unconditional.

    (c) `ComparisonColumn` — replace everything from the `StatPanel` wrapper through the text-block
    group with, when `layer === 'partners'`, a single div styled `margin: '20px 32px 0'` containing
    `PartnersProjectsTab` with prop `ll={ll}`. The accent bar, the column header and the trailing
    32px spacer stay unconditional. The tab IS available in comparison mode by deliberate decision
    (13-UI-SPEC, resolving 13-RESEARCH.md Open Question 1) — do not suppress or special-case it there.

    (d) Hint suppression at ALL THREE `llDetail.layerTabsHint` sites — `LayoutSplit` (455-457),
    `LayoutStacked` (648-650) and `LayoutCompare` (948-950) — by wrapping each identical hint div
    (`fontSize: 11`, `color: 'rgba(2,35,34,0.55)'`, `marginTop: 6`) in a `layer !== 'partners'`
    conditional that renders `null` otherwise. That copy describes the five thematic map layers and
    would misdescribe this tab. Add no replacement copy and no new i18n key.

    Do not add a `layer === 'partners'` branch inside `LLMap`, and do not pass `layer="partners"` to
    `LLMap` from any site — every `LLMap` render must stay unreachable while the partners tab is
    active.
  </action>
  <verify>
    <automated>cd app && test "$(grep -c "layer === 'partners'" src/pages/LLDetail.jsx)" -ge 3 && test "$(grep -c "layer !== 'partners'" src/pages/LLDetail.jsx)" -ge 5 && test "$(grep -c '<PartnersProjectsTab' src/pages/LLDetail.jsx)" = "3" && test "$(grep -c 'llDetail.layerTabsHint' src/pages/LLDetail.jsx)" = "3" && test "$(grep -c '<LLMap' src/pages/LLDetail.jsx)" = "3" && test "$(grep -v '^[[:space:]]*//' src/components/LLMap/index.jsx | grep -c "'partners'")" = "0" && npm run lint && npm run build && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `PartnersProjectsTab` is rendered at exactly three call sites in `LLDetail.jsx`: once inside
      `LayoutSplit`, once inside `LayoutStacked`, once inside `ComparisonColumn`.
    - All three `llDetail.layerTabsHint` occurrences (in `LayoutSplit`, `LayoutStacked` and
      `LayoutCompare`) are guarded by a `layer !== 'partners'` conditional.
    - All three `LLMap` call sites are guarded so `LLMap` never mounts while `layer === 'partners'`;
      `app/src/components/LLMap/index.jsx` contains zero occurrences of the string `'partners'` in
      non-comment source.
    - The `CompareCTA`/`DownloadReportCTA` rows, the LL header blocks, the accent bar and the
      comparison bar are unchanged (verify via `git diff` — record in the SUMMARY that no lines in
      those blocks changed).
    - `useLayerState`'s default value and setter are unchanged.
    - `npm run lint`, `npm run build` and `npm run format:check` exit 0.
  </acceptance_criteria>
  <done>The tab renders correctly in split, stacked and comparison layouts, and the thematic hint is suppressed at all three of its sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL/UI state -> component tree | The active `layer` string decides which component subtree mounts |
| tab mount -> network | Mounting `PartnersProjectsTab` is what triggers the partners/projects fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Denial of Service | `usePartnersProjects` mount lifecycle | mitigate | Exactly one hook call in one component; the module-scoped cache in the hook bounds the request count to one per page load even though the component mounts and unmounts on every tab switch, and in comparison mode two columns mount it simultaneously |
| T-13-12 | Tampering | `LLMap` `LAYER_INDEX.get(layer)` lookup | mitigate | All three `LLMap` call sites are guarded so `layer === 'partners'` never reaches it; asserted by a negative grep for `'partners'` in `LLMap/index.jsx` and by `LAYERS` staying at five entries (13-RESEARCH.md Pitfall 2) |
| T-13-13 | Information Disclosure | Comparison-mode partner rendering | accept | Both columns render the same public, hand-authored content that is already reachable on each Living Lab's own page; no cross-Living-Lab data leakage is possible because each column selects strictly by its own slug |
| T-13-01 | Tampering | External URLs reachable from this tab | mitigate | Inherited: every href and `window.open` in the mounted subtree passes through `safeExternalUrl` (plans 13-03 and 13-04); this plan adds no new external-navigation surface |
| T-13-SC | Tampering | npm installs | mitigate | Zero new packages; `app/package.json` must be unchanged by this plan |
</threat_model>

<verification>
- All three task gates pass, including the exact-count assertions on `PartnersProjectsTab` (3),
  `LLMap` (3) and `llDetail.layerTabsHint` (3) call sites.
- `npm run lint`, `npm run build` and `npm run format:check` exit 0.
- `git diff --stat app/package.json app/src/data/layers.js` is empty.
</verification>

<success_criteria>
1. The Partners and Projects tab is reachable from the right-hand side of the tab row in every
   layout, visually separated from the five thematic tabs.
2. Selecting it swaps the content area for the partner map plus overview panel in `LayoutSplit`,
   `LayoutStacked` and `ComparisonColumn`, with no `LLMap` mounted and no thematic hint shown.
3. The runtime JSON is fetched only after the tab is selected and at most once per page load.
</success_criteria>

<output>
Create `.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-05-SUMMARY.md` when done
</output>
