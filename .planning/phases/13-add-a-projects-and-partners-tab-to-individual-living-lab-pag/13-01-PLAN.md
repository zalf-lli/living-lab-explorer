---
phase: 13-add-a-projects-and-partners-tab-to-individual-living-lab-pag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/partners_projects.json
  - app/public/data/partners_projects.json
  - data-pipeline/sync.py
  - data-pipeline/tests/test_pipeline_outputs.py
  - app/src/i18n_resources.js
autonomous: true
requirements: [D-01, D-02, D-06, D-07, D-08, D-15, D-16, D-17, D-18]
ui_decisions: [UI-SPEC Data Schema, UI-SPEC Copywriting Contract]

must_haves:
  truths:
    - "Every one of the five Living Lab slugs resolves to an object with partners[] and projects[] arrays in data/partners_projects.json"
    - "Running the pipeline sync publishes data/partners_projects.json to app/public/data/partners_projects.json byte-identically"
    - "The app's bilingual resource bundle carries a Partners & Projects tab label and every partnersTab string in both EN and DE with exact key parity"
  artifacts:
    - path: "data/partners_projects.json"
      provides: "Hand-authored partner/project source of truth grouped by LL slug"
      contains: "east-brandenburg"
    - path: "app/public/data/partners_projects.json"
      provides: "Runtime copy fetched by the browser"
      contains: "partners"
    - path: "data-pipeline/tests/test_pipeline_outputs.py"
      provides: "Contract + publish-parity regression test for the new static file"
      contains: "test_partners_projects_contract_and_publish_parity"
    - path: "app/src/i18n_resources.js"
      provides: "layers.partners + partnersTab.* strings in EN and DE"
      contains: "partnersTab"
  key_links:
    - from: "data-pipeline/sync.py"
      to: "data/partners_projects.json"
      via: "STATIC_DATA_FILES entry consumed by sync_to_app()'s copy loop"
      pattern: "data/partners_projects\\.json"
---

<objective>
Establish the static data contract for the Partners & Projects tab: the hand-authored
`data/partners_projects.json` source file (D-06/D-07/D-08), its pipeline publish step into
`app/public/data/`, a pytest contract + publish-parity regression test, and the complete bilingual
i18n string set the tab's UI will consume.

Purpose: every later plan in this phase reads either this JSON contract or these i18n keys. Landing
them first removes all schema ambiguity from the component work and closes RESEARCH.md Pitfall 3
(`sync_file()` has no missing-file guard, so the file must exist before `sync.py` references it).
Output: one new committed source file, one published runtime copy, one new pytest test, one
extended i18n resource bundle.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-CONTEXT.md
@.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-RESEARCH.md
@.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-PATTERNS.md
@.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-UI-SPEC.md
</context>

<interfaces>
<!-- The data contract this plan creates. Every downstream plan in Phase 13 codes against it. -->

`data/partners_projects.json` top-level shape (locked by 13-UI-SPEC.md "Data Schema"):

- Top-level object keyed by Living Lab slug. Exactly these five keys, in this order:
  `east-brandenburg`, `havellandisches-luch`, `north-hessian-loess`, `hessian-low-mountain`,
  `rheingau` (source of truth: the top-level keys of `data/ll_content.json`).
- Each slug value is an object with exactly two keys: `partners` (array) and `projects` (array).
- Partner object keys: `id` (optional string), `name` (required string), `type` (optional string),
  `location` (optional string), `website` (optional string URL), `lat` (optional number),
  `lng` (optional number). Absent/`null` `lat`/`lng` means list-only, no map marker (D-14).
- Project object keys: `id` (optional string), `title` (required string), `summary` (optional
  object with `en` and `de` string keys), `partner` (optional plain display string, NOT a foreign
  key id), `website` (optional string URL).
- Only `summary` is bilingual. `name`, `title`, `type`, `location`, `partner` and all URLs are
  shared single strings (D-17).

New i18n keys this plan adds (locked by 13-UI-SPEC.md "Copywriting Contract"):
`layers.partners`, and a new top-level `partnersTab` block with keys `partnersHeading`,
`projectsHeading`, `partnersEmpty`, `projectsEmpty`, `visitWebsite`, `projectPartnerLabel`,
`markerAria`, `loadErrorTitle`, `loadErrorBody`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author data/partners_projects.json with all five Living Lab slugs</name>
  <files>data/partners_projects.json</files>
  <read_first>
    - `data/ll_content.json` — authoritative list of the five Living Lab slugs and the existing
      hand-authored grouped-by-slug file shape, including the `{ en, de }` bilingual field convention
      and the `manager` block naming every Living Lab's ZALF coordinator.
    - `.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-UI-SPEC.md`
      section "Data Schema (UI-facing key names)" — the locked field names this file must match.
    - `.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-RESEARCH.md`
      Pitfall 3 — why every slug key must exist even when its arrays are empty.
  </read_first>
  <action>
    Create `data/partners_projects.json` as a hand-authored, human-owned file (no pipeline script
    may ever write it — same posture CLAUDE.md locks for `data/ll_content.json`).

    Top-level keys, in this exact order: `east-brandenburg`, `havellandisches-luch`,
    `north-hessian-loess`, `hessian-low-mountain`, `rheingau`. Each maps to an object with keys
    `partners` and `projects` in that order.

    Seed content — real, verifiable, no invented organisations. Every one of the five Living Labs
    is coordinated by ZALF (confirmed: all five `manager.email` values in `data/ll_content.json` are
    `@zalf.de`), so ZALF is a factually defensible partner entry for each. Author exactly one
    partner entry per slug with these shared field values: `id` = `zalf`, `name` =
    `ZALF - Leibniz Centre for Agricultural Landscape Research`, `type` = `Research institution`,
    `location` = `Muencheberg, DE` (ASCII per this project's German-string convention), `website` =
    `https://www.zalf.de`.

    Coordinates: only the `east-brandenburg` entry carries `lat` = `52.5153` and `lng` = `14.1235`
    (ZALF's Muencheberg campus, which lies inside the east-brandenburg region). The other four slugs'
    ZALF entries omit `lat` and `lng` entirely — ZALF's campus is outside those Living Labs'
    boundaries, so they are genuinely list-only entries and deliberately exercise the D-14
    no-coordinates path with real rather than fabricated data.

    `projects` is `[]` for all five slugs. Real project content is authored at plan 13-06 Task 3's
    blocking human-verify checkpoint, from entries the developer supplies there; the empty arrays
    exercise the D-18 quiet-empty-state path in the meantime. Do NOT invent project titles,
    summaries or URLs.

    Format the file with two-space indentation and a trailing newline so `prettier --check` passes
    (`app/package.json` script `format:check` covers the repo).
  </action>
  <verify>
    <automated>python -c "import json;d=json.load(open('data/partners_projects.json',encoding='utf-8'));c=json.load(open('data/ll_content.json',encoding='utf-8'));assert list(d)==list(c),(list(d),list(c));assert all(set(v)=={'partners','projects'} for v in d.values());assert sum(len(v['partners']) for v in d.values())==5;assert sum(len(v['projects']) for v in d.values())==0;assert sum(1 for v in d.values() for p in v['partners'] if 'lat' in p)==1;print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `data/partners_projects.json` parses as JSON and its top-level key list equals
      `data/ll_content.json`'s top-level key list exactly (same members, same order).
    - Every slug value has exactly the two keys `partners` and `projects`.
    - Exactly 5 partner entries exist across the file (one per slug), each with
      `name == "ZALF - Leibniz Centre for Agricultural Landscape Research"` and
      `website == "https://www.zalf.de"`.
    - Exactly one partner entry in the whole file carries `lat`/`lng`, and it is the one under
      `east-brandenburg`, with `lat == 52.5153` and `lng == 14.1235`.
    - `projects` is an empty array for all five slugs.
    - No key outside the locked set (`id`, `name`, `type`, `location`, `website`, `lat`, `lng` for
      partners) appears on any partner entry.
  </acceptance_criteria>
  <done>The hand-authored source file exists, covers all five slugs, and matches the locked schema.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the file through sync.py and lock it with a pytest contract test</name>
  <files>data-pipeline/sync.py, data-pipeline/tests/test_pipeline_outputs.py, app/public/data/partners_projects.json</files>
  <read_first>
    - `data-pipeline/sync.py` lines 20-75 — the `STATIC_DATA_FILES` list (line 29) and `sync_file()`
      (line 45), which calls `shutil.copy2` with no existence check.
    - `data-pipeline/sync.py` lines 455-480 — `sync_to_app()`'s copy loop (line 463) and the
      surrounding `sync_pmtiles`/`sync_vector_geojson`/`sync_charts`/`sync_reports` calls that a
      `FileNotFoundError` in that loop would abort.
    - `data-pipeline/tests/test_pipeline_outputs.py` lines 1-33 — the `from conftest import
      LL_SLUGS, repo_root` import convention and module-level helper style.
    - `data-pipeline/tests/test_pipeline_outputs.py` lines 913-937 —
      `test_chart_fixtures_published_to_app_public`, the direct analog for a byte-identical
      published-copy assertion.
  </read_first>
  <action>
    Add exactly one entry, `"data/partners_projects.json"`, to the end of `STATIC_DATA_FILES` in
    `data-pipeline/sync.py`. No other `sync.py` change is required or permitted: `sync_to_app()`'s
    existing `for rel_path in STATIC_DATA_FILES` loop picks it up, and `sync_file()` byte-copies via
    `shutil.copy2` with no JSON serialization, so CLAUDE.md's `sort_keys=True` rule does not apply to
    this path. Task 1 must already be complete before this edit lands — `sync_file()` has no
    existence guard and a missing source aborts the whole `sync_to_app()` run.

    Run the pipeline sync (`cd data-pipeline && python sync.py`, per CLAUDE.md's Development Quick
    Start) to produce `app/public/data/partners_projects.json`, and commit that published copy
    alongside the source, matching the committed-source-plus-committed-published-copy pattern every
    other `STATIC_DATA_FILES` entry follows.

    Add one new test to `data-pipeline/tests/test_pipeline_outputs.py` named
    `test_partners_projects_contract_and_publish_parity`. It must import nothing new beyond `json`
    and the existing `LL_SLUGS`/`repo_root` conftest helpers, and assert, reading committed files
    only (no sync invocation): (a) `data/partners_projects.json` exists and its top-level key set
    equals `set(LL_SLUGS)`; (b) every slug value has exactly the keys `partners` and `projects`, both
    lists; (c) every partner entry has a non-empty string `name`, and any `lat`/`lng` present are
    numbers with `-90 <= lat <= 90` and `-180 <= lng <= 180`; (d) every partner/project `website`
    present starts with `https://` or `http://`; (e) every project entry has a non-empty string
    `title`, and any `summary` present is a dict with both `en` and `de` string keys; (f)
    `app/public/data/partners_projects.json` exists and is byte-identical to the source
    (`read_bytes()` comparison, as in `test_chart_fixtures_published_to_app_public`); (g)
    `"data/partners_projects.json"` appears in `sync.py`'s `STATIC_DATA_FILES` source text, so the
    publish step cannot be silently removed without failing this test.
  </action>
  <verify>
    <automated>cd data-pipeline && python -m pytest tests/ -q 2>&1 | tail -5 && python -c "import pathlib;s=pathlib.Path('sync.py').read_text(encoding='utf-8');assert 'data/partners_projects.json' in s;a=pathlib.Path('../data/partners_projects.json').read_bytes();b=pathlib.Path('../app/public/data/partners_projects.json').read_bytes();assert a==b;print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `data-pipeline/sync.py` contains the literal string `data/partners_projects.json` inside the
      `STATIC_DATA_FILES` list, and `git diff` for `sync.py` shows exactly one added line.
    - `app/public/data/partners_projects.json` exists and `read_bytes()` equals the source file's.
    - `python -m pytest data-pipeline/tests/` passes with zero failures, and the run includes
      `test_partners_projects_contract_and_publish_parity` (test count increases by exactly 1 versus
      the pre-change baseline).
    - The new test fails if the published copy is deleted (verify by temporarily renaming it, running
      the single test, and restoring it — record the observed failure message in the SUMMARY).
  </acceptance_criteria>
  <done>The new static file is published by the pipeline and both the schema and the publish parity are locked by an automated regression test.</done>
</task>

<task type="auto">
  <name>Task 3: Add the bilingual Partners &amp; Projects strings to i18n_resources.js</name>
  <files>app/src/i18n_resources.js</files>
  <read_first>
    - `app/src/i18n_resources.js` lines 1-20 — the module header stating the file is intentionally
      pure and node-importable, and the `resources.en.translation` nesting depth.
    - `app/src/i18n_resources.js` lines 63-76 — the EN `layers` block (ends with `protectedAreas` at
      line 71) and the block that follows it, so the new key lands in the right place.
    - `app/src/i18n_resources.js` lines 288-300 — the DE `layers` block mirror (DE `protectedAreas`
      at line 294), confirming key order parity between the two language sections.
    - `.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-UI-SPEC.md`
      section "Bilingual i18n keys" — the exact locked EN and DE copy for every key.
  </read_first>
  <action>
    Add `partners: 'Partners &amp; Projects'` as the last entry of the EN `layers` block (D-01) and
    `partners: 'Partner &amp; Projekte'` as the last entry of the DE `layers` block (D-02) — the
    literal `&` character, not an HTML entity.

    Add a new top-level `partnersTab` block to both `resources.en.translation` and
    `resources.de.translation`, placed immediately after each section's `layers` block so EN and DE
    key order stays mirrored. Keys and copy, verbatim from 13-UI-SPEC.md's Copywriting Contract:

    EN — `partnersHeading: 'Partners'`, `projectsHeading: 'Projects'`,
    `partnersEmpty: 'No partners listed yet.'`, `projectsEmpty: 'No projects listed yet.'`,
    `visitWebsite: 'Visit website'`, `projectPartnerLabel: 'Partner:'`,
    `markerAria: "Open {{name}}'s website"`,
    `loadErrorTitle: 'Partner and project data is temporarily unavailable.'`,
    `loadErrorBody: 'Reload the page - if this keeps happening, the data source may be temporarily unreachable.'`

    DE — `partnersHeading: 'Partner'`, `projectsHeading: 'Projekte'`,
    `partnersEmpty: 'Noch keine Partner gelistet.'`, `projectsEmpty: 'Noch keine Projekte gelistet.'`,
    `visitWebsite: 'Website besuchen'`, `projectPartnerLabel: 'Partner:'`,
    `markerAria: 'Website von {{name}} oeffnen'`,
    `loadErrorTitle: 'Partner- und Projektdaten sind voruebergehend nicht verfuegbar.'`,
    `loadErrorBody: 'Seite neu laden - falls das Problem bestehen bleibt, ist die Datenquelle moeglicherweise voruebergehend nicht erreichbar.'`

    All German strings are ASCII-transliterated (no umlauts, no eszett) exactly as written above,
    matching the existing convention throughout this file. Add no other keys; in particular do NOT
    add a replacement for `llDetail.layerTabsHint` — plan 13-05 suppresses that hint with a plain
    conditional rather than new copy.
  </action>
  <verify>
    <automated>cd app && node -e "import('./src/i18n_resources.js').then(({resources})=>{const en=resources.en.translation,de=resources.de.translation;const k=['partnersHeading','projectsHeading','partnersEmpty','projectsEmpty','visitWebsite','projectPartnerLabel','markerAria','loadErrorTitle','loadErrorBody'];for(const x of k){if(typeof en.partnersTab?.[x]!=='string')throw new Error('missing EN '+x);if(typeof de.partnersTab?.[x]!=='string')throw new Error('missing DE '+x);}if(JSON.stringify(Object.keys(en.partnersTab))!==JSON.stringify(Object.keys(de.partnersTab)))throw new Error('key order mismatch');if(en.layers.partners!=='Partners & Projects')throw new Error('EN tab label');if(de.layers.partners!=='Partner & Projekte')throw new Error('DE tab label');if(/[^\x00-\x7F]/.test(JSON.stringify(de.partnersTab)))throw new Error('non-ASCII in DE copy');console.log('OK')})" && npm run lint && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `resources.en.translation.layers.partners === 'Partners & Projects'` and
      `resources.de.translation.layers.partners === 'Partner & Projekte'`.
    - `resources.en.translation.partnersTab` and `resources.de.translation.partnersTab` both exist
      and have identical key lists in identical order (all nine locked keys).
    - Every DE `partnersTab` string is pure ASCII (no `ä`/`ö`/`ü`/`ß`).
    - `markerAria` in both languages contains the `{{name}}` interpolation placeholder.
    - `npm run lint` exits 0 and `npm run format:check` exits 0.
  </acceptance_criteria>
  <done>The tab label and all nine partnersTab strings exist in both languages with exact key parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-authored JSON → committed repo | `data/partners_projects.json` content is human-typed; it is reviewed via git diff, not validated by any runtime schema |
| repo file → browser DOM | The published `app/public/data/partners_projects.json` is fetched same-origin and rendered by later plans' components |
| pipeline filesystem copy | `sync_file()` copies an arbitrary repo-relative path into `app/public/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `website` fields in `data/partners_projects.json` | mitigate | Task 2's pytest test asserts every `website` value starts with `https://` or `http://`, blocking a `javascript:`/`data:` URL from ever reaching the committed contract; plan 13-02 adds a runtime scheme allowlist as defence in depth |
| T-13-02 | Denial of Service | `sync_to_app()` copy loop | mitigate | Task 1 lands the source file before Task 2's `STATIC_DATA_FILES` edit, so `shutil.copy2`'s missing-file `FileNotFoundError` (which aborts every later sync step) cannot occur (RESEARCH.md Pitfall 3) |
| T-13-03 | Information Disclosure | Partner/project content | accept | All content is public organisational information deliberately published on a public anonymous site; JSON presence is the declared permission boundary (D-11) |
| T-13-04 | Elevation of Privilege | `sync_file()` destination path | accept | The added path is a hardcoded repo-relative literal in a committed constant, not user input; `sync.py`'s existing repo-root-escape guard already covers the pattern-driven sync paths |
| T-13-SC | Tampering | npm/pip installs | mitigate | Zero packages installed by this plan or this phase (13-RESEARCH.md "Package Legitimacy Audit: Not applicable"); plan 13-06 Task 1 diffs `app/package.json`, `app/package-lock.json` and `data-pipeline/requirements.txt` across the whole phase with `git diff --exit-code`, discharging this per-plan promise phase-wide |
</threat_model>

<verification>
- `python -m pytest data-pipeline/tests/` passes, including the new
  `test_partners_projects_contract_and_publish_parity`.
- `app/public/data/partners_projects.json` is byte-identical to `data/partners_projects.json`.
- `npm run lint` and `npm run format:check` both exit 0 in `app/`.
- The node gate confirms EN/DE `partnersTab` key parity and the two locked tab labels.
</verification>

<success_criteria>
1. All five Living Lab slugs resolve to `{partners, projects}` in both the source and published JSON.
2. `sync.py` publishes the new file and a regression test fails if either the schema or the
   published copy drifts.
3. `layers.partners` and all nine `partnersTab.*` keys exist in EN and DE with matching key order.
</success_criteria>

<output>
Create `.planning/phases/13-add-a-projects-and-partners-tab-to-individual-living-lab-pag/13-01-SUMMARY.md` when done
</output>
