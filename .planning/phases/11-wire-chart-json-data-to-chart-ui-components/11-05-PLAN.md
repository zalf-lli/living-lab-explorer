---
phase: 11-wire-chart-json-data-to-chart-ui-components
plan: 05
type: execute
wave: 4
depends_on: [11-04]
files_modified:
  - .planning/phases/11-wire-chart-json-data-to-chart-ui-components/11-EVIDENCE.md
autonomous: false
requirements: []            # ROADMAP Phase 11 says "Requirements: TBD". This plan records the verdict for every UI-SPEC decision UI-1..UI-8.
ui_decisions: [UI-1, UI-2, UI-3, UI-4, UI-5, UI-6, UI-7, UI-8]

must_haves:
  truths:
    - "Every one of the 25 committed chart files is reachable from the app under the exact filename the hook builds"
    - "No chart file is mock, and every bar file arrives pre-sorted descending by pct (the premise of the client's never-re-sort rule)"
    - "The full automated gate (lint, format, build, join-key, source-token) passes on the finished phase"
    - "A human has confirmed all five tabs in both languages, in all three layouts, against the real numbers"
  artifacts:
    - path: ".planning/phases/11-wire-chart-json-data-to-chart-ui-components/11-EVIDENCE.md"
      provides: "Verdict for UI-1..UI-8 plus the automated gate transcript"
      contains: "UI-8"
  key_links:
    - from: "app/src/hooks/useChartData.js"
      to: "app/public/data/charts/*.json"
      via: "LAYER_SOURCE_INDEX dataset id + Living Lab slug filename"
      pattern: "data/charts/"
---

<objective>
Close the phase: run the full automated gate across the finished integration, record a verdict for each
of the eight locked UI-SPEC decisions, and take a blocking bilingual human verification.

Purpose: every previous plan verified its own file. This plan verifies the phase goal — that opening any
Living Lab on any tab renders that Living Lab's real Phase 9 chart data — and proves the app-side-only
scope constraint held (no pipeline file touched, no package added).
Output: `11-EVIDENCE.md` and a human sign-off.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-wire-chart-json-data-to-chart-ui-components/11-UI-SPEC.md
@.planning/ROADMAP.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated gate</name>
  <files>(no source files modified — this task only runs checks)</files>
  <read_first>
    - .planning/phases/11-wire-chart-json-data-to-chart-ui-components/11-01-SUMMARY.md, 11-02-SUMMARY.md, 11-03-SUMMARY.md, 11-04-SUMMARY.md (all four — every Deviations section must be carried into the evidence record in Task 2)
    - app/src/hooks/useChartData.js (confirm the exact URL expression the join-key check must mirror)
    - app/src/data/layer_sources.js (the `appLayer -> id` pairs the join-key check resolves)
    - app/public/data/ll_metadata.json (the authoritative Living Lab slug set — read only the top-level keys and each entry's `slug`)
  </read_first>
  <action>
Run every check below and capture its exact output for Task 2's evidence record. Fix nothing silently:
if a check fails, stop and report it as a phase blocker rather than adjusting the check.

1. `cd app && npm run lint`
2. `cd app && npm run format:check`
3. `cd app && npm run build`
4. Join-key + data-contract gate — the single node command in the verify block below. It resolves each
   of the five tab layers' dataset ids through `LAYER_SOURCE_INDEX` (never a hardcoded table), pairs
   them with every Living Lab slug from `ll_metadata.json`, and for all 25 files asserts: the file
   exists at the exact path `useChartData` builds; `mock === false`; `layer_id` equals the app tab id;
   `ll_slug` equals the slug in the filename; `chart_type` is `line` for climate and `bar` for the other
   four; climate files carry exactly 2 `x_axis` entries and 4 `lines`; every bar file's `series` is
   non-increasing in `pct` (the premise behind the client's never-re-sort rule); and
   `buildDisplaySeries` renders at most 7 rows per bar file while preserving the total pct.
5. Dead-token gate: `cd app && grep -rn "CHART_DATA\|chart_data\|barChart\.\|charts\." src` must produce
   no output at all (exit status 1 from grep is the passing case).
6. Scope gate: `git status --porcelain data-pipeline` prints nothing, and
   `git diff --stat app/package.json app/package-lock.json` prints nothing — this phase is app-side
   only and adds no dependency.
7. XSS gate: `cd app && grep -rn "dangerouslySetInnerHTML" src` must produce no output.
  </action>
  <verify>
    <automated>cd app && node --input-type=module -e "const {LAYER_SOURCE_INDEX}=await import('./src/data/layer_sources.js');const {buildDisplaySeries}=await import('./src/lib/chartSeries.js');const {readFileSync}=await import('node:fs');const meta=JSON.parse(readFileSync('public/data/ll_metadata.json','utf8'));const slugs=Object.values(meta).map((l)=>l.slug);if(slugs.length!==5)throw new Error('expected 5 Living Labs, got '+slugs.length);const tabs=['agriculture','soil','economic','landscape','climate'];let n=0;for(const tab of tabs){const src=LAYER_SOURCE_INDEX.get(tab);if(!src)throw new Error('no LAYER_SOURCE_INDEX entry for '+tab);for(const slug of slugs){const f='public/data/charts/'+src.id+'-'+slug+'.json';const d=JSON.parse(readFileSync(f,'utf8'));if(d.mock!==false)throw new Error(f+' is still mock');if(d.layer_id!==tab)throw new Error(f+' layer_id '+d.layer_id+' != '+tab);if(d.ll_slug!==slug)throw new Error(f+' ll_slug mismatch');if(tab==='climate'){if(d.chart_type!=='line')throw new Error(f+' must be chart_type line');if(d.x_axis.length!==2)throw new Error(f+' expected 2 x_axis entries');if(d.lines.length!==4)throw new Error(f+' expected 4 lines')}else{if(d.chart_type!=='bar')throw new Error(f+' must be chart_type bar');for(let i=1;i<d.series.length;i++)if(d.series[i].pct>d.series[i-1].pct+1e-9)throw new Error(f+' is not pre-sorted descending at index '+i);const rows=buildDisplaySeries(d.series,{lang:'en',otherLabel:'Other'});if(rows.length>7)throw new Error(f+' would render '+rows.length+' rows');const a=d.series.reduce((x,e)=>x+e.pct,0),b=rows.reduce((x,e)=>x+e.pct,0);if(Math.abs(a-b)>0.06)throw new Error(f+' pct total drift '+a+' vs '+b)}n++}}if(n!==25)throw new Error('expected 25 chart files, checked '+n);console.log('chart join-key + contract gate OK: '+n+' files')" && npm run lint && npm run format:check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The node gate command exits 0 and prints `chart join-key + contract gate OK: 25 files`
    - CLI: `cd app && npm run lint` exits 0
    - CLI: `cd app && npm run format:check` exits 0
    - CLI: `cd app && npm run build` exits 0
    - CLI: `cd app && grep -rn "CHART_DATA\|chart_data\|barChart\.\|charts\." src` prints nothing
    - CLI: `cd app && grep -rn "dangerouslySetInnerHTML" src` prints nothing
    - CLI: `git status --porcelain data-pipeline` prints nothing
    - CLI: `git diff --stat app/package.json app/package-lock.json` prints nothing
  </acceptance_criteria>
  <done>All eight gate checks pass and their exact output is captured for the evidence record.</done>
</task>

<task type="auto">
  <name>Task 2: Write 11-EVIDENCE.md</name>
  <files>.planning/phases/11-wire-chart-json-data-to-chart-ui-components/11-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/11-wire-chart-json-data-to-chart-ui-components/11-UI-SPEC.md (the "Decisions Pre-Populated / Locked This Session" table — the eight rows this evidence record must return a verdict on, referenced as UI-1..UI-8 in plan order)
    - .planning/phases/11-wire-chart-json-data-to-chart-ui-components/11-01-SUMMARY.md through 11-04-SUMMARY.md (each plan's Deviations section)
    - .planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-EVIDENCE.md (the house format for an evidence record: one row per decision, a verdict, and the file/line or command that proves it)
  </read_first>
  <action>
Write `11-EVIDENCE.md` with these sections:

1. **Decision verdicts** — a table with columns `# | Decision (short) | Verdict | Proof`. One row for each
   of the eight UI-SPEC decisions, numbered UI-1..UI-8 in the order they appear in the UI-SPEC's
   "Decisions Pre-Populated / Locked This Session" table. `Verdict` is one of `implemented`,
   `implemented with deviation`, or `not implemented`. `Proof` names the file and symbol or the exact
   command whose output demonstrates it — never a qualitative claim.
2. **Deviations from the UI-SPEC** — one subsection per deviation, each stating what the UI-SPEC said,
   what was built, and why. At minimum record these three, which were resolved during planning and must
   appear here whether or not the executors added more:
   - The UI-SPEC states the wrapping card at every call site already renders a title; only `LayoutSplit`
     did. Plan 11-04 added matching title rows to `LayoutStacked` and `ComparisonColumn`.
   - The UI-SPEC's defensive fallback for climate line colours (matching `CLIMATE_VARIABLES[i].labelKey`
     translated against `lines[i].label[lang]`) is not viable — the i18n labels are abbreviations
     ("GDD", "Mean temp.") while the JSON labels are full names ("Growing degree days", "Mean annual
     temperature"), so the comparison can never match. A length-guarded index mapping was implemented
     instead.
   - The loading state uses this phase's 12px typography scale rather than `MapFallback`'s own 13px,
     resolving the conflict between the UI-SPEC's "matches MapFallback's exact style convention" wording
     and its own Typography table.
3. **Automated gate transcript** — the exact commands from Task 1 and their pass/fail result, including
   the `chart join-key + contract gate OK: 25 files` line.
4. **Known limitations carried forward** — at minimum: value-label crowding in `LineChart` when two
   variables have close values at the same horizon (no collision avoidance is in scope), and the fact
   that the "Other" bucket's summed absolute value is meaningful only because every current bar unit is
   additive (`ha`, `zones`) — a future non-additive unit would need a different rule.

Do not restate the plans. This file is the audit trail for what shipped versus what was locked.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/11-wire-chart-json-data-to-chart-ui-components/11-EVIDENCE.md','utf8');for(let i=1;i<=8;i++)if(!s.includes('UI-'+i))throw new Error('missing verdict row for UI-'+i);for(const sec of ['Deviations','Automated gate','Known limitations'])if(!s.includes(sec))throw new Error('missing section: '+sec);if(!s.includes('25 files'))throw new Error('gate transcript missing the 25-file join-key result');console.log('evidence record OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The node command above exits 0 and prints `evidence record OK`
    - Source: the decision table contains exactly 8 rows, one per `UI-1`..`UI-8`
    - Source: every `Proof` cell names a file path, a symbol, or a runnable command — no cell contains the words "looks", "seems", "correctly" or "properly"
    - Source: all three planning-resolved deviations listed in the action are present
    - Source: the file records the literal string `chart join-key + contract gate OK: 25 files`
  </acceptance_criteria>
  <done>`11-EVIDENCE.md` gives a proof-backed verdict for all eight locked decisions, records every deviation, and carries the full gate transcript.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Blocking bilingual human verification of the real chart data</name>
  <what-built>
Charts across the whole app now render the real per-Living-Lab JSON produced by Phase 9 instead of the
hand-written placeholder that showed every Living Lab the same five fake bars:

- a new `useChartData` hook fetching `data/charts/{dataset-id}-{slug}.json`, with 404 treated as "not
  built yet" rather than an error
- a rewritten `BarChart` for the agriculture, soil, economic and landscape tabs: top 6 categories by
  share plus one grey "Other" bucket, each row showing the category name, a rank-coloured proportional
  bar, its percentage and the raw quantity with its unit
- a new hand-rolled `LineChart` for the climate tab: 4 variables x 2 future horizons of percent change on
  a shared zero-inclusive scale with a dashed zero line
- real source attribution in every chart footer, replacing the literal "Source: placeholder data" line
- the placeholder module and its dead i18n keys deleted
  </what-built>
  <how-to-verify>
Run `cd app && npm run dev` and open the printed URL. Check **in English first, then switch the language
switcher to German and repeat every step** — every string below must be translated, and no raw i18n key
(e.g. `chart.emptyTitle`) may ever appear on screen.

1. **Per-Living-Lab data is real.** Open `/#/ll/rheingau`, Landscape tab. The top bar should be Forest at
   about 60.4% with a caption near `62,369 ha`. Now open `/#/ll/east-brandenburg`, Landscape tab: the top
   bar should still be Forest but at about 43.6% with a different quantity. If the two Living Labs show
   identical bars, the placeholder path is still live — reject.
2. **Truncation reads sensibly.** Socio-economic tab on `/#/ll/hessian-low-mountain` (31 real categories):
   confirm exactly 7 rows, the 7th labelled "Other" / "Sonstige" in grey, and that the percentages look
   like they add up to roughly 100. Hover a truncated category name and confirm the full text appears in
   the native tooltip.
3. **Every tab has a chart.** Cycle Agriculture, Soil, Climate, Landscape, Socio-economic on at least two
   Living Labs. Each must show a chart, a card title, and a footer naming the real provider (DLR, BGR,
   LGB/HLBG, Esri/Impact Observatory, WSL) with a working "View source" / "Quelle ansehen" link.
4. **Climate is a line chart.** Climate tab: 4 coloured lines across `2041-2070` and `2071-2100`, each
   point labelled with a signed percentage, a dashed horizontal zero line, and a legend naming the four
   variables. Confirm the negative line (precipitation of the warmest quarter) sits below the zero line
   and the GDD line well above it. Card title reads "Climate - projected change" / "Klima - projizierte
   Aenderung", not "distribution"/"Verteilung".
5. **Both layouts.** Use the "Change layout" switcher and repeat spot checks in the stacked layout —
   the chart card must have a title there too.
6. **Comparison view.** Click "Add for comparison" and pick a second Living Lab. Both columns must show
   their own chart on every tab, including Climate, at the compact size, with no column collapsing.
7. **Loading and error are visible.** In DevTools, throttle the network to "Slow 3G" and switch tabs —
   a visible "Loading chart data..." / "Diagrammdaten werden geladen..." line must appear rather than a
   blank card. Then block `*/data/charts/*` in the DevTools network request-blocking panel and reload:
   a visible error heading and body must appear, not silence.
8. **Readability judgement calls** (report, do not fix): are the climate value labels crowded where two
   variables sit close together? Are any category labels cut off so short they are unreadable at the
   compact comparison width?
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or list each defect with the tab, Living Lab, language and layout where you saw it</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed chart JSON → app render | The gate re-validates every one of the 25 files against the exact filename, join keys and shape the app assumes, so a pipeline-side rename or regeneration cannot silently half-break the UI. |
| repository working tree → phase scope | The scope gate asserts no `data-pipeline` file and no `package.json`/`package-lock.json` changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | Tampering | Chart JSON contract drift between pipeline and app | mitigate | The Task 1 node gate asserts filename, `layer_id`, `ll_slug`, `chart_type`, `mock: false` and the descending-`pct` premise for all 25 files; any drift fails the phase rather than rendering wrong numbers. |
| T-11-15 | Tampering (XSS) | Whole app surface after this phase | mitigate | `grep -rn dangerouslySetInnerHTML src` must print nothing across the entire `app/src` tree, not just this phase's files. |
| T-11-16 | Repudiation | Undocumented deviation from the locked UI contract | mitigate | `11-EVIDENCE.md` requires a proof-backed verdict for every UI-1..UI-8 decision plus an explicit deviations section; the verify command fails if any row is missing. |
| T-11-SC | Tampering (supply chain) | npm dependencies | mitigate | `git diff --stat app/package.json app/package-lock.json` must print nothing for the whole phase. No package-manager install task exists anywhere in Phase 11, so no package legitimacy audit is required. |
</threat_model>

<verification>
Run from the repository root:

1. The Task 1 node join-key gate — exits 0 printing `chart join-key + contract gate OK: 25 files`
2. `cd app && npm run lint` / `npm run format:check` / `npm run build` — all exit 0
3. `cd app && grep -rn "CHART_DATA\|chart_data\|barChart\.\|charts\." src` — prints nothing
4. `cd app && grep -rn "dangerouslySetInnerHTML" src` — prints nothing
5. `git status --porcelain data-pipeline` — prints nothing
6. `git diff --stat app/package.json app/package-lock.json` — prints nothing
7. `11-EVIDENCE.md` exists with UI-1..UI-8 verdicts
8. Human verification approved
</verification>

<success_criteria>
- All 25 chart files validate against the exact contract the app consumes
- The full automated gate passes with zero dead placeholder tokens left in `app/src`
- No pipeline file and no dependency manifest changed anywhere in the phase
- `11-EVIDENCE.md` records a proof-backed verdict for UI-1..UI-8 and every deviation
- A human has approved all five tabs, both languages, all three layouts
</success_criteria>

<output>
Create `.planning/phases/11-wire-chart-json-data-to-chart-ui-components/11-05-SUMMARY.md` when done
</output>
