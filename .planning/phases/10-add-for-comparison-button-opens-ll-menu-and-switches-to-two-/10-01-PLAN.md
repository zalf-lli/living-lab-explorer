---
phase: 10-add-for-comparison-button-opens-ll-menu-and-switches-to-two-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/i18n.js
  - app/src/components/StatPanel.jsx
  - app/src/components/BarChart.jsx
autonomous: true
requirements: []            # ROADMAP Phase 10 says "Requirements: TBD" — no REQ-IDs exist. Traceability is via `decisions` below.
decisions: [D-10, D-11, D-14, D-28, D-29]

must_haves:
  truths:
    - "Every new comparison string exists in BOTH the EN and DE resource blocks of i18n.js (D-11, D-14, D-10)"
    - "StatPanel caps its KPI grid at 2 columns when maxColumns={2} is passed (D-29)"
    - "StatPanel renders a same-footprint bilingual placeholder instead of null when showEmptyState is passed and the tab has no fields (D-10)"
    - "BarChart renders a same-footprint bilingual placeholder instead of null when minHeightWhenEmpty is passed and the layer has no chart data (D-10)"
    - "The single-LL page is visually unchanged: StatPanel still renders a 4-across grid and still returns null on empty; BarChart still returns null on empty (D-29 last sentence)"
  artifacts:
    - path: "app/src/i18n.js"
      provides: "EN+DE keys for comparison bar, picker heading and both empty states"
      contains: "comparePickerTitle"
    - path: "app/src/components/StatPanel.jsx"
      provides: "maxColumns + showEmptyState props"
      contains: "maxColumns = 4"
    - path: "app/src/components/BarChart.jsx"
      provides: "minHeightWhenEmpty prop"
      contains: "minHeightWhenEmpty"
  key_links:
    - from: "app/src/components/StatPanel.jsx"
      to: "i18n statPanel.compareEmptyTitle / compareEmptyBody"
      via: "t() call in the empty-state branch"
      pattern: "statPanel\\.compareEmpty"
    - from: "app/src/components/BarChart.jsx"
      to: "i18n barChart.compareEmptyTitle / compareEmptyBody"
      via: "t() call in the empty-state branch"
      pattern: "barChart\\.compareEmpty"
---

<objective>
Add every string and every component prop the two-column comparison view needs, without changing a
single pixel of the existing single-LL page.

Purpose: Plans 03-05 compose the comparison view out of existing components. Those components must
first accept the two comparison-only behaviours locked in CONTEXT.md — a 2-column KPI cap (D-29) and
equal-footprint empty states (D-10) — and the new bilingual copy must exist before any consumer
calls `t()` on it.
Output: 10 new i18n keys in both languages; `maxColumns` + `showEmptyState` on `StatPanel`;
`minHeightWhenEmpty` on `BarChart`. All three changes are additive with defaults that preserve
today's behaviour exactly.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-add-for-comparison-button-opens-ll-menu-and-switches-to-two-/10-CONTEXT.md
@.planning/phases/10-add-for-comparison-button-opens-ll-menu-and-switches-to-two-/10-UI-SPEC.md
@.planning/phases/10-add-for-comparison-button-opens-ll-menu-and-switches-to-two-/10-PATTERNS.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md
</context>

<interfaces>
Current signatures the executor is extending (read the files for full bodies):

- `app/src/components/StatPanel.jsx` — `export function StatPanel({ tab, ll })`; `const fields = ll.kpiByTab?.[tab] ?? []`; early return `if (fields.length === 0) return null` (line 31); KPI grid at line 72-78 uses
  `gridTemplateColumns: repeat(${Math.min(fields.length, 4) || 1}, 1fr)`.
- `app/src/components/BarChart.jsx` — `export function BarChart({ layer, compact = false })`; `const data = CHART_DATA[layer]`; early return `if (!data) return null` (line 8).
- `app/src/i18n.js` — `const resources = { en: { translation: { ... } }, de: { translation: { ... } } }`.
  EN namespaces: `statPanel` at line 60, `barChart` at line 167, `llDetail` at line 208 (ends line 228).
  DE namespaces: `statPanel` at line 270, `barChart` at line 377, `llDetail` at line 418.
- Theme tokens are imported as `C` from `../theme.js` in both components already.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add EN + DE comparison strings to i18n.js</name>
  <files>app/src/i18n.js</files>
  <read_first>
    - app/src/i18n.js (read lines 55-75, 160-175 and 200-235 for the EN `statPanel`/`barChart`/`llDetail` blocks, and lines 265-285, 373-385, 415-445 for the DE mirrors — you must see the exact indentation and trailing-comma style before editing)
    - .planning/phases/10-add-for-comparison-button-opens-ll-menu-and-switches-to-two-/10-UI-SPEC.md (section "Component Modifications → app/src/i18n.js" — the locked, pre-vetted string set)
    - .planning/codebase/CONVENTIONS.md (Prettier: `semi: false`, `singleQuote: true`, `printWidth: 100`)
  </read_first>
  <action>
Add exactly ten new keys — six under `llDetail`, two under `statPanel`, two under `barChart` — to BOTH
the `en.translation` and `de.translation` blocks. Insert the `llDetail` keys immediately after the
existing `compareCompactAction` key (EN line 225 / DE line 435) so the whole `compare*` cluster stays
together. Insert the `statPanel` keys after `sourcesToggle` and the `barChart` keys after the
existing `source` key in each language block. Do not create any new top-level namespace.

EN values (exact):

| Namespace | Key | Value |
|---|---|---|
| llDetail | comparePrefix | Comparing |
| llDetail | comparePickerTitle | Compare with |
| llDetail | compareSwap | ⇄ Swap sides |
| llDetail | compareSwapAria | Swap comparison sides |
| llDetail | compareExit | ✕ Exit comparison |
| llDetail | compareChangePartnerAria | Change comparison partner |
| statPanel | compareEmptyTitle | No KPI data yet |
| statPanel | compareEmptyBody | This Living Lab does not have statistics for this tab yet. |
| barChart | compareEmptyTitle | No chart data yet |
| barChart | compareEmptyBody | This Living Lab does not have chart data for this tab yet. |

DE values (exact, ASCII-transliterated per the file's existing convention — `ae`/`oe`/`ue`, never `ß`):

| Namespace | Key | Value |
|---|---|---|
| llDetail | comparePrefix | Vergleich: |
| llDetail | comparePickerTitle | Vergleichen mit |
| llDetail | compareSwap | ⇄ Seiten tauschen |
| llDetail | compareSwapAria | Vergleichsseiten tauschen |
| llDetail | compareExit | ✕ Vergleich beenden |
| llDetail | compareChangePartnerAria | Vergleichspartner aendern |
| statPanel | compareEmptyTitle | Noch keine KPI-Daten |
| statPanel | compareEmptyBody | Fuer dieses Living Lab liegen in diesem Reiter noch keine Statistiken vor. |
| barChart | compareEmptyTitle | Noch keine Diagrammdaten |
| barChart | compareEmptyBody | Fuer dieses Living Lab liegen in diesem Reiter noch keine Diagrammdaten vor. |

The `⇄` (U+21C4) and `✕` (U+2715) glyphs are part of the string values in both languages — this is
the locked UI-SPEC decision (the project has no icon library, so these two controls use unicode
glyphs as text). `app/src/i18n.js` is currently pure ASCII; save it as UTF-8 without a BOM. The
ASCII-transliteration convention applies to German letters only, not to these glyphs.

Do not touch, rename or reword any existing key. `compareTitle`, `compareCompactTitle`,
`compareBody`, `compareAction` and `compareCompactAction` all stay exactly as they are — plan 03
reuses `compareCompactAction` verbatim as the picker trigger label.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/src/i18n.js','utf8');const two=['comparePrefix','comparePickerTitle','compareSwap','compareSwapAria','compareExit','compareChangePartnerAria'];const four=['compareEmptyTitle','compareEmptyBody'];let ok=true;for(const k of two){const n=(s.match(new RegExp('\\\\b'+k+':','g'))||[]).length;if(n!==2){console.error('EXPECTED 2 OCCURRENCES (EN+DE):',k,'got',n);ok=false}}for(const k of four){const n=(s.match(new RegExp('\\\\b'+k+':','g'))||[]).length;if(n!==4){console.error('EXPECTED 4 OCCURRENCES (statPanel+barChart x EN+DE):',k,'got',n);ok=false}}if(!ok)process.exit(1);console.log('i18n EN/DE key parity OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The node parity command above exits 0 and prints `i18n EN/DE key parity OK`
    - Source: `app/src/i18n.js` contains the literal string `Compare with` and the literal string `Vergleichen mit`
    - Source: `app/src/i18n.js` contains `compareSwap: '⇄ Swap sides'` and `compareSwap: '⇄ Seiten tauschen'`
    - Source: `grep -c "compareCompactAction:" app/src/i18n.js` still returns 2 (existing keys untouched)
    - Source: no German string added in this task contains `ß`, `ä`, `ö` or `ü`
    - `cd app && npm run format:check` exits 0
  </acceptance_criteria>
  <done>All ten keys exist in both language blocks with the exact values above; no existing key changed; Prettier reports no formatting drift.</done>
</task>

<task type="auto">
  <name>Task 2: Add maxColumns and showEmptyState props to StatPanel</name>
  <files>app/src/components/StatPanel.jsx</files>
  <read_first>
    - app/src/components/StatPanel.jsx (whole file, 147 lines — you need the hook order, the line-31 early return, and the line-72 grid)
    - app/src/i18n.js (the `statPanel` blocks you added in Task 1, to confirm the key names)
    - .planning/phases/10-add-for-comparison-button-opens-ll-menu-and-switches-to-two-/10-UI-SPEC.md (sections "Empty-state footprint (D-10)" and "Component Modifications → StatPanel.jsx")
  </read_first>
  <action>
Change the signature to `export function StatPanel({ tab, ll, maxColumns = 4, showEmptyState = false })`.

Replace the grid's column count so it reads `Math.min(fields.length, maxColumns) || 1` instead of
`Math.min(fields.length, 4) || 1` (line 75). Nothing else about the grid changes — `gap: 8` stays.

Replace the bare `if (fields.length === 0) return null` (line 31) with a branch: when
`showEmptyState` is false, keep returning `null` exactly as today; when it is true, return a single
placeholder block styled to match a populated KPI tile so the row footprint is preserved (D-10):
`gridColumn: '1 / -1'`, `background: C.white`, `borderRadius: 8`, `padding: '12px 16px'`,
`border: 1px solid ${C.mutedLight}`. Inside it render a title line using `t('statPanel.compareEmptyTitle')`
at `fontSize: 12, fontWeight: 700, color: C.teal, lineHeight: 1.3` and a body line using
`t('statPanel.compareEmptyBody')` at `fontSize: 12, fontWeight: 400, color: C.muted, lineHeight: 1.4,
marginTop: 4`. Those sizes/weights are the UI-SPEC typography contract for this phase — 12px only,
weights 400 and 700 only.

The early return must stay AFTER the existing `useState`/`useRef`/`useEffect` calls (lines 10-29) so
React's hook order is unchanged. `t` is already destructured from `useTranslation()` at line 9 and is
in scope above the early return.

Do not change the sources-disclosure button, the pending-review footnote, or the tile rendering.
Do not add `auto-fit` or any responsive behaviour to the grid.
  </action>
  <verify>
    <automated>cd app && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/src/components/StatPanel.jsx` contains `maxColumns = 4` and `showEmptyState = false` in the destructured props
    - Source: `grep -c "Math.min(fields.length, maxColumns)" app/src/components/StatPanel.jsx` returns 1
    - Source: `grep -c "Math.min(fields.length, 4)" app/src/components/StatPanel.jsx` returns 0
    - Source: `app/src/components/StatPanel.jsx` contains both `statPanel.compareEmptyTitle` and `statPanel.compareEmptyBody`
    - Source: the file still contains a `return null` path reachable when `showEmptyState` is falsy
    - Behavior: `/ll/east-brandenburg` renders the KPI strip exactly as before (4 tiles across for the Agriculture tab) — no caller passes `maxColumns` yet
    - `cd app && npm run lint` exits 0 and `npm run build` exits 0
  </acceptance_criteria>
  <done>StatPanel accepts both new props with behaviour-preserving defaults; the 2-column cap and the empty-state placeholder are reachable only when a caller opts in.</done>
</task>

<task type="auto">
  <name>Task 3: Add minHeightWhenEmpty prop to BarChart</name>
  <files>app/src/components/BarChart.jsx</files>
  <read_first>
    - app/src/components/BarChart.jsx (whole file, 80 lines)
    - app/src/data/chart_data.js (confirm which layer ids have entries — the empty branch is defensive today)
    - .planning/phases/10-add-for-comparison-button-opens-ll-menu-and-switches-to-two-/10-UI-SPEC.md (section "Empty-state footprint (D-10)")
  </read_first>
  <action>
Change the signature to `export function BarChart({ layer, compact = false, minHeightWhenEmpty })` —
no default value, so it is `undefined` for every existing caller.

Replace `if (!data) return null` (line 8) with a branch: when `minHeightWhenEmpty == null`, return
`null` exactly as today; otherwise return a block with `minHeight: minHeightWhenEmpty`,
`display: 'flex'`, `flexDirection: 'column'`, `justifyContent: 'center'` containing a title line
`t('barChart.compareEmptyTitle')` at `fontSize: 12, fontWeight: 700, color: C.teal, lineHeight: 1.3`
and a body line `t('barChart.compareEmptyBody')` at `fontSize: 12, fontWeight: 400, color: C.muted,
lineHeight: 1.4, marginTop: 4`.

Use the loose `== null` check (not `!minHeightWhenEmpty`) so a caller could pass `0` if it ever
wanted a zero-height placeholder. Do not change the `compact` prop, the bar rendering, or the source
footnote. All five layer ids currently have `CHART_DATA` entries, so this branch is defensive against
the per-LL chart data that Phase 9 will introduce — that is intentional and D-10 requires it to exist.
  </action>
  <verify>
    <automated>cd app && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/src/components/BarChart.jsx` signature line contains `minHeightWhenEmpty` with no `=` default
    - Source: `grep -c "minHeightWhenEmpty == null" app/src/components/BarChart.jsx` returns 1
    - Source: file contains both `barChart.compareEmptyTitle` and `barChart.compareEmptyBody`
    - Behavior: `/ll/east-brandenburg` renders every layer tab's chart exactly as before — no caller passes `minHeightWhenEmpty` yet
    - `cd app && npm run lint` exits 0, `npm run build` exits 0, `npm run format:check` exits 0
  </acceptance_criteria>
  <done>BarChart returns an equal-footprint placeholder when and only when a caller opts in via minHeightWhenEmpty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time i18n resources → DOM | Static, developer-authored strings rendered as React text children (auto-escaped). No user input crosses this boundary in this plan. |
| `ll.kpiByTab` (pipeline-produced JSON) → StatPanel render | Data fetched from `./data/ll_metadata.json`, same-origin static asset produced by the Python pipeline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering (XSS) | New empty-state blocks in StatPanel/BarChart | mitigate | Render copy via `t(...)` as JSX text children only. No `dangerouslySetInnerHTML` is introduced in this plan; verify with `grep -c dangerouslySetInnerHTML app/src/components/StatPanel.jsx app/src/components/BarChart.jsx` returning 0 for both. |
| T-10-02 | Denial of Service | StatPanel grid column count | mitigate | `maxColumns` is consumed only inside `Math.min(fields.length, maxColumns)` and every call site in this phase passes a literal (`2`). No URL or user-controlled value reaches it. |
| T-10-SC | Tampering (supply chain) | npm dependencies | mitigate | This plan installs zero packages. `app/package.json` and `app/package-lock.json` must be unchanged after execution (`git diff --stat app/package*.json` prints nothing). No package legitimacy audit is required because no package manager install task exists in this phase. |
</threat_model>

<verification>
Run from the repository root:

1. `node -e "..."` i18n parity command from Task 1 — exits 0
2. `cd app && npm run lint` — exits 0
3. `cd app && npm run format:check` — exits 0
4. `cd app && npm run build` — exits 0
5. `git diff --stat app/package.json app/package-lock.json` — prints nothing
6. `grep -rc "dangerouslySetInnerHTML" app/src/components/StatPanel.jsx app/src/components/BarChart.jsx` — returns 0 for both
7. Manual smoke (dev server): `/ll/east-brandenburg` KPI strip still shows 4 tiles across on the Agriculture tab and the chart renders — this plan must be invisible on the single-LL page
</verification>

<success_criteria>
- Ten new keys present in both EN and DE blocks with the exact locked values
- `StatPanel` accepts `maxColumns` (default 4) and `showEmptyState` (default false)
- `BarChart` accepts `minHeightWhenEmpty` (default undefined)
- Zero visual change to the single-LL page
- lint, format:check and build all exit 0
</success_criteria>

<output>
Create `.planning/phases/10-add-for-comparison-button-opens-ll-menu-and-switches-to-two-/10-01-SUMMARY.md` when done
</output>
