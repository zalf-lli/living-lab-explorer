---
phase: 02.2-soil-semantics-translation
plan: "02"
type: execute
wave: 2
depends_on:
  - "02.2-01"
files_modified:
  - data-pipeline/python/soil_semantics.py
  - data-pipeline/python/build_vector.py
  - data-pipeline/tests/test_pipeline_outputs.py
  - data/geojson/buek250-east-brandenburg.geojson
  - data/geojson/buek250-havelland.geojson
  - data/geojson/buek250-hessian-low-mountain.geojson
  - data/geojson/buek250-north-hessian-loess.geojson
  - data/geojson/buek250-rheingau.geojson
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - The SQLite database is the authoritative source for soil semantics, but the runtime contract must expose provenance and explicit fallback behavior instead of leaking raw table structure into the UI.
    - English soil labels must come from a reproducible, repo-committed translation or normalization layer rather than manual edits inside emitted GeoJSON fixtures.
    - Raw malformed strings and half-empty `soil_type_*` columns must stop being the primary runtime contract once the new semantic export exists.
  artifacts:
    - path: data-pipeline/python/build_vector.py
      provides: Joined and normalized BUEK250 semantic export path
      contains: sqlite
    - path: data-pipeline/tests/test_pipeline_outputs.py
      provides: Contract-level assertions for the richer soil metadata output
      contains: buek250
    - path: data/geojson/buek250-east-brandenburg.geojson
      provides: Representative committed fixture for the new semantic contract
      contains: soil
  key_links:
    - from: data-pipeline/python/build_vector.py
      to: data/buek250_mgm_utm_v60/buek250_sachdatenbank_v10.sqlite
      via: joins across legend, profile, horizon, general legend, and parent-material tables
      pattern: buek250_.+
    - from: data-pipeline/tests/test_pipeline_outputs.py
      to: data/geojson/buek250-{slug}.geojson
      via: fixture contract validation
      pattern: buek250-.+\.geojson
---

<objective>
Emit the improved soil contract and lock it in with refreshed fixtures and tests.

Purpose: make the richer semantic model real by joining the deeper SQLite tables, exporting a normalized bilingual contract, and updating the committed fixtures and smoke tests to enforce it.
Output: rebuilt BUEK250 GeoJSON fixtures and test coverage for the new semantic fields and fallback behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-soil-semantics-translation/02.2-RESEARCH.md
@.planning/phases/02.2-soil-semantics-translation/02.2-01-PLAN.md
@data-pipeline/python/build_vector.py
@data-pipeline/tests/test_pipeline_outputs.py
@data/geojson/buek250-east-brandenburg.geojson
</context>

<notes>
- Favor compact summaries from `PROFIL` and `HORIZONT`; do not serialize raw child-table rows per polygon unless there is a strong runtime need.
- Keep contract evolution test-driven so the old weak field names do not silently linger as accidental requirements.
- Recommit all five GeoJSON fixtures as part of the same contract transition.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Build the richer joined semantic export from SQLite</name>
  <files>data-pipeline/python/soil_semantics.py, data-pipeline/python/build_vector.py</files>
  <read_first>.planning/phases/02.2-soil-semantics-translation/02.2-RESEARCH.md, data-pipeline/python/soil_semantics.py, data-pipeline/python/build_vector.py</read_first>
  <action>Extend the semantic builder so `build_vector.py` joins and summarizes the relevant BUEK250 tables. Use `LEGENDENEINHEIT` for direct polygon labels, `GL_Einheit` and `GL_BAGFlaechentyp` for grouped classifications, and derive a compact profile summary from `PROFIL` and `HORIZONT` that the app can consume without inheriting raw database complexity.</action>
  <verify>.\data-pipeline\.venv\Scripts\python.exe data-pipeline\python\build_vector.py --layer buek250</verify>
  <acceptance_criteria>
    - The emitted contract contains richer grouped semantics than the current legend-only lookup
    - Bilingual labels and source values are both available where traceability matters
    - Special areas retain meaningful metadata without masquerading as ordinary soil units
    - The new output no longer depends on `soil_type_3` existing to be considered complete
  </acceptance_criteria>
  <done>The BUEK250 fixtures carry a real semantic contract rather than a shallow raw lookup.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh fixtures and replace the old test contract</name>
  <files>data-pipeline/tests/test_pipeline_outputs.py, data/geojson/buek250-east-brandenburg.geojson, data/geojson/buek250-havelland.geojson, data/geojson/buek250-hessian-low-mountain.geojson, data/geojson/buek250-north-hessian-loess.geojson, data/geojson/buek250-rheingau.geojson</files>
  <read_first>data-pipeline/tests/test_pipeline_outputs.py, data/geojson/buek250-east-brandenburg.geojson, .planning/phases/02.2-soil-semantics-translation/02.2-RESEARCH.md</read_first>
  <action>Rebuild and recommit the five BUEK250 GeoJSON fixtures, then rewrite the smoke tests so they assert the new semantic contract instead of simply checking for `soil_name` and `soil_type_*`. Keep low-level identifiers like `GEN_ID` and `SYM_NR` under test where they remain intentionally part of provenance.</action>
  <verify>.\data-pipeline\.venv\Scripts\python.exe -m pytest data-pipeline\tests\</verify>
  <acceptance_criteria>
    - The committed fixtures reflect the richer semantic export across all five LLs
    - Tests fail if the new semantic fields disappear or fallback behavior regresses
    - Tests no longer lock the app to the old weak field names as the primary contract
  </acceptance_criteria>
  <done>The stronger soil contract is enforced by committed fixtures and smoke tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\data-pipeline\.venv\Scripts\python.exe data-pipeline\python\build_vector.py --layer buek250` succeeds
- [ ] All five `data/geojson/buek250-*.geojson` fixtures are rebuilt and committed
- [ ] `.\data-pipeline\.venv\Scripts\python.exe -m pytest data-pipeline\tests\` passes
- [ ] The tests validate the new semantic contract instead of the old `soil_name` / `soil_type_*`-only shape
</verification>

<success_criteria>

- All tasks completed
- The runtime GeoJSON assets expose a richer bilingual soil contract
- The committed fixtures and tests enforce the new semantics
- The old weak raw-lookup fields are no longer the primary runtime API
  </success_criteria>

<output>
After completion, create `.planning/phases/02.2-soil-semantics-translation/02.2-02-SUMMARY.md`
</output>
