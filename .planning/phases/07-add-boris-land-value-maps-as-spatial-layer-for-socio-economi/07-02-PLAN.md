---
phase: 07-add-boris-land-value-maps-as-spatial-layer-for-socio-economi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/data/layers.js
  - app/src/i18n.js
autonomous: true
requirements: [D-01, D-03, D-04, D-08, D-10, D-12]
must_haves:
  truths:
    - "The economic tab is registered as a vector layer pointing at data/geojson/boris-{slug}.geojson"
    - "Every BORIS choropleth colour used by the app is a theme.js token, not a re-declared hex literal"
    - "Every new user-facing BORIS string exists in both English and German"
  artifacts:
    - path: "app/src/data/layers.js"
      provides: "economic vector layer entry, BORIS_RAMP (6 buckets), BORIS_NO_DATA_STYLE"
      contains: "BORIS_RAMP"
    - path: "app/src/i18n.js"
      provides: "legend.economic.note/empty and map.economicLoading/economicError/economicTooltip.* in EN and DE"
      contains: "economicTooltip"
  key_links:
    - from: "app/src/data/layers.js"
      to: "app/src/theme.js"
      via: "import { C }"
      pattern: "from '\\.\\./theme\\.js'"
    - from: "app/src/data/layers.js economic entry"
      to: "data/geojson/boris-{slug}.geojson"
      via: "geojsonPathPattern consumed by resolveLayerAsset"
      pattern: "boris-\\{slug\\}\\.geojson"
---

<objective>
Convert the `economic` tab from a placeholder into a real vector layer and add every bilingual string
the BORIS choropleth needs. This is pure static configuration with no dependency on the pipeline, so it
runs in Wave 1 in parallel with the pipeline transport work.

Purpose: 07-UI-SPEC.md locks the exact 6-bucket teal→orange ramp, the single new "no current value"
neutral pair, and the full EN/DE copy table. Landing all of that before the rendering work (plan 07-04)
means the LLMap task imports colours and translation keys rather than inventing them mid-implementation.
Output: an updated `app/src/data/layers.js` and `app/src/i18n.js`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-add-boris-land-value-maps-as-spatial-layer-for-socio-economi/07-CONTEXT.md
@.planning/phases/07-add-boris-land-value-maps-as-spatial-layer-for-socio-economi/07-UI-SPEC.md
@.planning/phases/07-add-boris-land-value-maps-as-spatial-layer-for-socio-economi/07-PATTERNS.md
</context>

<interfaces>
<!-- Current state of the files being modified. Executor must still Read them; this is the contract summary. -->

`app/src/theme.js` exports `C` with the six ramp tokens this plan needs:
`C.tealBg = '#00413f'`, `C.teal = '#005754'`, `C.tealMid = '#008581'`, `C.tealLight = '#00b3ad'`,
`C.orangeDark = '#dc4b14'`, `C.orange = '#eb5b25'`.

`app/src/data/layers.js` current `economic` entry (line 39):
`{ id: 'economic', type: 'placeholder', pmtilesUrl: null, legend: null, available: true },`

The `soil` entry (lines 30-38) is the shape to copy. `resolveLayerAsset()` (lines 69-79) already handles
`type === 'vector'` + `geojsonPathPattern` + `{slug}` substitution generically — it needs no change.

`app/src/components/MapLegend.jsx` line 5: `export function MapLegend({ layer, entries = null, note = null })`
renders any `{value, en, de, color}` array generically — it needs no change either.

`app/src/i18n.js` structure: EN block first, DE block second. Existing anchors —
`legend.economic` EN at line 98, DE at line 306; `map.soilLoading`/`soilLoadError` EN at 175-176, DE at
383-384; `map.soilTooltip` EN at 190, DE at 398. German strings in this file are ASCII-transliterated
(`ae`/`oe`/`ue`/`ss`) — see `fuer`, `Flaeche`, `Gewaesser` throughout.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Convert the economic layer entry and export the BORIS ramp</name>
  <files>app/src/data/layers.js</files>
  <read_first>
    - `app/src/data/layers.js` (full file, 87 lines) — current `economic` placeholder at line 39, the `soil` vector entry at lines 30-38 to copy, the `PROTECTED_AREAS_LEGEND` export convention at lines 11-19, and `LAYER_COLORS` at lines 81-86.
    - `app/src/theme.js` — the exact `C` token names for the six ramp colours.
    - `.planning/phases/07-.../07-UI-SPEC.md` "Choropleth Value Ramp" and "'No current value' style" sections — the locked bucket order and the two new neutral hex values.
  </read_first>
  <action>
Add `import { C } from '../theme.js'` at the top of `app/src/data/layers.js`, alongside the existing
`landuse_legend.js` / `land_cover_legend.js` imports.

Replace the placeholder `economic` entry at line 39 with a vector entry matching the `soil` entry's shape:
`id: 'economic'`, `type: 'vector'`, `pmtilesUrl: null`,
`geojsonPathPattern: 'data/geojson/boris-{slug}.geojson'`, `legend: null`,
`legendNoteKey: 'legend.economic.note'`, `available: true`. `legend` stays `null` deliberately — per D-02
and D-09 the buckets are computed per Living Lab at render time and supplied through `MapLegend`'s
`entries` prop, exactly as `soilLegendEntries` already is; a static array here would be wrong.

Add two new exports, placed next to `PROTECTED_AREAS_LEGEND` and carrying a comment stating that
`LLMap/index.jsx` must import these rather than redeclare any hex code (the same rule already written
above `PROTECTED_AREAS_LEGEND`):

- `export const BORIS_RAMP` — an array of exactly six colour values, low value to high value, referencing
  theme tokens and not literals: `[C.tealBg, C.teal, C.tealMid, C.tealLight, C.orangeDark, C.orange]`.
  Comment it as the D-01/D-03 sequential ramp: teal = cheap, orange = expensive, zero newly invented hues.
- `export const BORIS_NO_DATA_STYLE` — the D-08 "no current value" treatment, the one deliberate new
  colour pair this phase introduces because `theme.js` has no neutral grey:
  `{ fillColor: '#d8d8d2', color: '#9a9a90', weight: 0.4, dashArray: '3,3', fillOpacity: 0.55 }`.
  Comment it as the sole exception to "zero new colours" and explain that a true diagonal hatch is not
  achievable under Leaflet's `L.canvas()` renderer, so a muted flat fill with a dashed border is the
  locked equivalent.
- `export const BORIS_VALUE_STYLE_BASE` — the shared stroke/opacity settings for all six value buckets so
  the style function only has to pick `fillColor`: `{ color: 'rgba(2,35,34,0.35)', weight: 0.4, fillOpacity: 0.78 }`.
  The 0.4 weight is deliberately thinner than soil's 0.6 because zone counts run to tens of thousands.
- `export const BORIS_HOVER_STYLE` — `{ fillOpacity: 0.92, weight: 0.7 }`.

Do NOT delete `LAYER_COLORS.economic` (line 85). It stays as the fallback palette for the still-placeholder
`climate` tab code path; removing it would require touching `MapLegend.jsx`'s fallback branch, which is out
of scope for this phase.
  </action>
  <verify>
    <automated>cd app && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "type: 'placeholder'" app/src/data/layers.js` no longer matches any line containing `id: 'economic'`.
    - `grep -c "boris-{slug}.geojson" app/src/data/layers.js` equals 1.
    - `grep -n "legendNoteKey: 'legend.economic.note'" app/src/data/layers.js` matches exactly once.
    - `grep -v '^\s*//' app/src/data/layers.js | grep -c "BORIS_RAMP\|BORIS_NO_DATA_STYLE\|BORIS_VALUE_STYLE_BASE\|BORIS_HOVER_STYLE"` is at least 4.
    - `BORIS_RAMP` contains exactly six entries and every one is a `C.*` token reference — `grep -A2 "export const BORIS_RAMP" app/src/data/layers.js | grep -c "#"` returns 0.
    - `grep -n "LAYER_COLORS" app/src/data/layers.js` still matches (the fallback palette was not removed).
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>`economic` is a `type: 'vector'` entry pointing at `data/geojson/boris-{slug}.geojson`; four BORIS style exports exist and reference theme tokens for the ramp; lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the bilingual BORIS i18n keys</name>
  <files>app/src/i18n.js</files>
  <read_first>
    - `app/src/i18n.js` lines 60-210 (EN `layers`/`legend`/`map` blocks) and lines 270-420 (the mirrored DE blocks) — the exact nesting and the ASCII-transliteration convention for German.
    - `.planning/phases/07-.../07-UI-SPEC.md` "Copywriting Contract" and "i18n Keys (new)" sections — the copy below is copied verbatim from there; do not paraphrase.
  </read_first>
  <action>
Add the following keys to `app/src/i18n.js`, in both the EN block and the mirrored DE block. EN/DE key
parity is a hard convention in this file — an EN-only key is a defect.

Under `legend.economic` (EN block near line 98, DE block near line 306), **replace** the five existing
placeholder keys (`arable`, `forest`, `grassland`, `settlement`, `water`) with the two real keys below.
Those five were the `LAYER_COLORS` fallback labels for the placeholder tab and become unreachable once
`entries` is supplied at runtime by plan 07-04; leaving them would ship dead copy.

EN `legend.economic`:
- `note`: `"Colors are scaled to this Living Lab's own value range and are not comparable across the five Living Labs."`
- `empty`: `"No land value zones are available for this Living Lab."`

DE `legend.economic`:
- `note`: `'Die Farbskala bezieht sich nur auf dieses Living Lab und ist nicht mit anderen Living Labs vergleichbar.'`
- `empty`: `'Keine Bodenrichtwertzonen sind fuer dieses Living Lab verfuegbar.'`

Under `map` (EN block near line 175, DE block near line 383), add alongside the existing
`soilLoading`/`soilLoadError` pair:

EN:
- `economicLoading`: `'Loading land value zones for this Living Lab...'`
- `economicError`: `'Land value data could not be loaded for this Living Lab.'`

DE:
- `economicLoading`: `'Bodenrichtwertzonen fuer dieses Living Lab werden geladen...'`
- `economicError`: `'Die Bodenrichtwertdaten fuer dieses Living Lab konnten nicht geladen werden.'`

Under `map`, add a new `economicTooltip` object next to `soilTooltip` (EN near line 190, DE near line 398).
Per D-12 the tooltip has exactly three information rows — value, usage type, valuation date — so no
zone-reference-code key and no development-status key may be added:

EN `map.economicTooltip`:
- `usageType`: `'Usage type'`
- `valuationDate`: `'Valuation date'`
- `noCurrentValue`: `'No current value'`
- `historical`: `'(historical)'`

DE `map.economicTooltip`:
- `usageType`: `'Nutzungsart'`
- `valuationDate`: `'Stichtag'`
- `noCurrentValue`: `'Kein aktueller Wert'`
- `historical`: `'(historisch)'`

German strings must stay ASCII-transliterated (`fuer`, not `für`). The `€` and `/m²` symbols are NOT
transliterated and are NOT part of i18n — the value string is composed at render time in plan 07-04 via
`Number(...).toLocaleString(...)`, so no currency key belongs here.

Do not add a `legend.economic.*` key per usage type. Usage-type labels are supplied by the pipeline as
`usage_type_en`/`usage_type_de` properties on each feature (D-11); i18n keys for them would be dead code,
which is the same conclusion Phase 6 reached for `legend.landCover.*`.
  </action>
  <verify>
    <automated>cd app && node -e "import('./src/i18n.js').then(()=>console.log('i18n module loaded'))" 2>/dev/null || npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "economicTooltip" app/src/i18n.js` equals 2 (one EN, one DE).
    - `grep -c "economicLoading" app/src/i18n.js` equals 2; `grep -c "economicError" app/src/i18n.js` equals 2.
    - `grep -c "noCurrentValue" app/src/i18n.js` equals 2; `grep -c "valuationDate" app/src/i18n.js` equals 2.
    - `grep -n "Nutzungsart" app/src/i18n.js` and `grep -n "Usage type" app/src/i18n.js` each match exactly once.
    - `grep -n "arable: 'High output'" app/src/i18n.js` returns no match (the placeholder economic legend keys were replaced).
    - No German string added by this task contains `ä`, `ö`, `ü`, or `ß` — `grep -n "fuer dieses Living Lab" app/src/i18n.js` matches at least twice and `grep -n "für dieses Living Lab" app/src/i18n.js` matches zero times.
    - `npm run lint` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>All ten new keys (2 legend + 2 map messages + 4 tooltip, mirrored EN/DE) exist with the UI-SPEC copy verbatim; the five placeholder economic legend keys are gone; lint and build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline GeoJSON -> browser render | Feature properties fetched from `app/public/data/geojson/boris-{slug}.geojson` are treated as untrusted content in the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering (XSS) | Free-text BORIS attributes rendered into the map UI | mitigate | This plan adds no rendering code. It only registers the layer and the static copy table. The rendering path in plan 07-04 is required to use the existing `createTooltipRow()` helper, which writes via `textContent` and never `innerHTML`. |
| T-07-07 | Spoofing | Numeric `bodenrichtwert` presented as currency | mitigate | No currency string is stored in i18n; the formatted value is composed at render time from `Number(value).toLocaleString(...)` in plan 07-04, so a non-numeric payload cannot smuggle markup through a translation string. |
| T-07-SC | Tampering | npm package installs | accept | This plan adds zero npm dependencies; `app/package.json` is untouched. No registry or component library is introduced (07-UI-SPEC.md "Registry Safety": not applicable). |
</threat_model>

<verification>
- `cd app && npm run lint` — exits 0.
- `cd app && npm run build` — exits 0 (proves no import cycle from the new `theme.js` import in `layers.js`).
- `git diff --stat app/package.json` — empty (no dependency added).
</verification>

<success_criteria>
1. `LAYERS`' `economic` entry is `type: 'vector'` with `geojsonPathPattern: 'data/geojson/boris-{slug}.geojson'` and `legendNoteKey: 'legend.economic.note'`.
2. `BORIS_RAMP`, `BORIS_NO_DATA_STYLE`, `BORIS_VALUE_STYLE_BASE`, and `BORIS_HOVER_STYLE` are exported from `app/src/data/layers.js`; the ramp references six `C.*` tokens with no inline hex.
3. Ten new i18n keys exist in both EN and DE with verbatim UI-SPEC copy; the five placeholder `legend.economic.*` keys are removed.
4. `npm run lint` and `npm run build` both pass.
</success_criteria>

<output>
Create `.planning/phases/07-add-boris-land-value-maps-as-spatial-layer-for-socio-economi/07-02-SUMMARY.md` when done
</output>
