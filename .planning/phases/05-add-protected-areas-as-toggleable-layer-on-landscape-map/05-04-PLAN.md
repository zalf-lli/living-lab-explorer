---
phase: 05-add-protected-areas-as-toggleable-layer-on-landscape-map
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - data-pipeline/tests/test_pipeline_outputs.py
  - .planning/phases/05-add-protected-areas-as-toggleable-layer-on-landscape-map/05-VERIFICATION.md
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]
requirements_addressed: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]

must_haves:
  truths:
    - "Every decision D-01 through D-08 has a recorded, reproducible piece of evidence"
    - "The overlay works in both English and German on a data-heavy and a data-light Living Lab"
    - "A human has confirmed the map is usable at full geometric fidelity"
  artifacts:
    - path: ".planning/phases/05-add-protected-areas-as-toggleable-layer-on-landscape-map/05-VERIFICATION.md"
      provides: "D-01..D-08 evidence table with pass/fail and the command or observation behind each row"
      contains: "D-08"
  key_links:
    - from: "05-VERIFICATION.md"
      to: "05-CONTEXT.md decisions"
      via: "one evidence row per decision ID"
      pattern: "D-0[1-8]"
---

<objective>
Prove the phase actually delivered what D-01 through D-08 specify, in both languages, and get a
human's sign-off on the two judgement calls this phase makes (Canvas rendering at full fidelity, and
the repository size the committed GeoJSON adds).

Purpose: Phase 5 has no REQUIREMENTS.md IDs, so the CONTEXT decisions are the spec and need an
explicit closure record.
Output: `05-VERIFICATION.md` plus one cross-cutting regression test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-add-protected-areas-as-toggleable-layer-on-landscape-map/05-CONTEXT.md
@.planning/phases/05-add-protected-areas-as-toggleable-layer-on-landscape-map/05-RESEARCH.md
@.planning/phases/05-add-protected-areas-as-toggleable-layer-on-landscape-map/05-01-SUMMARY.md
@.planning/phases/05-add-protected-areas-as-toggleable-layer-on-landscape-map/05-02-SUMMARY.md
@.planning/phases/05-add-protected-areas-as-toggleable-layer-on-landscape-map/05-03-SUMMARY.md

@data-pipeline/tests/test_pipeline_outputs.py
@app/src/components/LLMap/index.jsx
@app/src/data/layers.js
@app/src/i18n.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the automated D-01..D-08 evidence record</name>
  <read_first>
    .planning/phases/05-add-protected-areas-as-toggleable-layer-on-landscape-map/05-CONTEXT.md
    .planning/phases/05-add-protected-areas-as-toggleable-layer-on-landscape-map/05-RESEARCH.md
    data-pipeline/tests/test_pipeline_outputs.py
    app/src/components/LLMap/index.jsx
    app/src/i18n.js
  </read_first>
  <files>data-pipeline/tests/test_pipeline_outputs.py, .planning/phases/05-add-protected-areas-as-toggleable-layer-on-landscape-map/05-VERIFICATION.md</files>
  <action>
    Add `test_protected_areas_app_public_copies_match_source` to
    `data-pipeline/tests/test_pipeline_outputs.py`: for each slug in `LL_SLUGS`, assert that
    `app/public/data/geojson/protected-areas-{slug}.geojson` exists and that its bytes are identical
    to `data/geojson/protected-areas-{slug}.geojson`. This is the regression guard against someone
    editing one copy of the pipeline–app file contract and not the other.

    Add `test_protected_areas_i18n_keys_exist_in_both_languages`: read `app/src/i18n.js` as text and
    assert that each of the fourteen key names added in plan 05-02 (`protectedAreas`,
    `protectedAreasLoading`, `protectedAreasError`, `protectedAreasTooltip`, `designation`, `area`,
    `areaUnit`, `established`, `authority`, `natura2000sci`, `natura2000spa`, `naturschutzgebiet`,
    plus `note` and `empty` inside a `protectedAreas` legend block) appears at least twice — once in
    the `en` tree and once in the `de` tree. Additionally assert the file contains zero bytes above
    U+007F, locking the project's ASCII-only i18n convention. Keep this test text-based; the pipeline
    test suite has no JS runtime.

    Then write `05-VERIFICATION.md` in the phase directory as a table with one row per decision
    D-01 through D-08. Each row records: the decision text, the evidence type (automated test,
    shell command, or human observation), the exact command or observation, and the result. Use
    these evidence sources:
    - D-01 and D-02: `test_protected_areas_geojson_fixtures_exist_and_match_contract` asserts all
      three designation values are present in every Living Lab file.
    - D-03: the same test's assertion that the union of protected-area geometries is not contained
      within the Living Lab boundary, plus a screenshot of polygons crossing the outline.
    - D-04: `data-pipeline/python/fetch_protected_areas.py` performs live `GetFeature` calls;
      quote the recorded per-LL fetch timings from `05-01-SUMMARY.md`.
    - D-05: the toggle url memo depends on `showProtectedAreas` and `ll.slug` only, never on
      `layer`; confirm with a grep and with the human check in Task 2.
    - D-06: the protected-areas `<GeoJSON>` is added after the raster layer and after the mask;
      the raster lives in Leaflet's tilePane and vectors in the overlayPane, so the ordering is
      structural. Confirm with the human check in Task 2.
    - D-07: `resolveLayerAsset` is only called when the toggle is on, so `useGeoJSON` receives
      `null` and issues no request; confirm with the network-panel observation in Task 2.
    - D-08: no `simplify`, `simplifyFactor`, `smoothFactor` or tolerance appears in either the
      pipeline script or `LLMap`; record the actual vertex counts from `05-01-SUMMARY.md` against
      the 05-RESEARCH.md prediction of 1,012,740 total; state explicitly that
      `set_precision(1e-6)` is roughly 11 cm coordinate rounding that removes zero vertices and zero
      features, and that `L.canvas()` changes only the rasterisation backend.
    Mark any row you cannot evidence as FAIL with the specific gap; do not write PASS on the
    strength of an assumption.
  </action>
  <verify>
    <automated>cd data-pipeline && .venv/Scripts/python.exe -m pytest tests -q</automated>
  </verify>
  <done>
    - `05-VERIFICATION.md` exists with eight decision rows, each carrying a command or an observation.
    - `pytest tests -q` passes, now including the two new cross-cutting tests.
    - `cd app && npm run lint && npm run build` exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bilingual manual verification on a data-heavy and a data-light Living Lab</name>
  <action>Start the dev server, walk the developer through the verification script below, and wait
  for the resume signal plus answers to the two judgement calls. Record both answers verbatim in the
  plan SUMMARY and reflect any resulting change as a follow-up gap item rather than an ad-hoc edit.</action>
  <what-built>
    A protected-areas overlay toggle on the Living Lab map. When switched on it lazily fetches a
    per-LL GeoJSON produced from live BfN WFS queries and draws every Natura 2000 SCI, Natura 2000
    SPA and Naturschutzgebiet polygon that intersects the region, at full geometric fidelity, on a
    Leaflet Canvas renderer, above whatever thematic layer is selected. Hovering shows a bilingual
    tooltip; a second legend block lists the designation types present; the map info control credits
    BfN.
  </what-built>
  <how-to-verify>
    Run `cd app && npm run dev` and open the printed local URL.

    1. Open the Living Lab detail page for **East Brandenburg** (the heaviest region, roughly 355
       features and 311,616 vertices). With the Land Use tab active, open the browser network panel
       and confirm that NO `protected-areas-east-brandenburg.geojson` request has been made.
    2. Click the "Protected Areas" toggle at the top-right of the map. Confirm the loading badge
       appears, exactly one request for `data/geojson/protected-areas-east-brandenburg.geojson`
       fires, and polygons appear.
    3. Confirm the polygons are drawn ON TOP OF the land-use raster, and that sites extend past the
       Living Lab outline rather than being cut at it.
    4. Pan and zoom. Report whether interaction stays usable. If it is unacceptably slow, say so —
       the fallback is a decision for you, not a silent simplification, because D-08 forbids
       reducing geometry.
    5. Hover several polygons of different colours. Confirm the tooltip shows a name, a designation,
       an area in hectares, and where available a year and an authority; confirm the polygon
       brightens on hover and reverts on mouse-out.
    6. Switch the language to German. Repeat step 5 and confirm the tooltip labels, the toggle
       label, the legend labels and the legend note are all German. Confirm the loading and error
       copy is German by toggling off and hard-reloading with the toggle on.
    7. Switch the tab to Soil while the overlay stays on. Confirm the overlay stays visible on top
       of the soil polygons and that the toggle state is unaffected by the tab change.
    8. Switch the tab to Climate (a placeholder tab). Confirm the toggle is still present and the
       overlay still renders.
    9. Open the map info control (the "i" button, bottom-right). Confirm it lists a protected-areas
       row crediting `Bundesamt fuer Naturschutz (BfN)` with the GeoNutzV licence, and that LAWA is
       nowhere on screen.
    10. Repeat steps 2, 3, 5 and 9 for **Rheingau** (the lightest region, roughly 78 features), in
        both languages.
    11. Toggle the overlay off on both regions. Confirm the polygons and the second legend block
        disappear and the base layer legend is unchanged.

    Two judgement calls need your explicit answer:
    - **Mask ordering.** The overlay currently draws ABOVE the white 60 percent mask that dims
      everything outside the Living Lab, so out-of-region parts of a site stay fully legible. That
      was chosen because D-03 exists precisely to show those parts. If you would rather they be
      dimmed like the rest of the surroundings, say so — it is a one-line reordering.
    - **Repository size.** These files add roughly 23.6 MB to `data/` and the same again to
      `app/public/`, about 47 MB total, on a repository whose `.git` is already around 725 MB.
      Confirm you accept that, or ask for a coarser coordinate precision (1e-5, about 1.1 m,
      brings the total to roughly 41.7 MB and still removes zero vertices and zero features).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues, and answer the two judgement calls.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline output -> app/public copy | Two tracked copies of the same bytes can drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering | Duplicated GeoJSON in `data/` and `app/public/` | mitigate | `test_protected_areas_app_public_copies_match_source` byte-compares both copies, so a hand edit to either one fails the suite |
| T-05-15 | Repudiation | Decision closure | mitigate | `05-VERIFICATION.md` records one evidence row per decision with the reproducing command, so a later reader can re-run rather than trust |
| T-05-SC | Tampering | Dependency installs | accept | No installs in this plan. |
</threat_model>

<must_haves>
- All eight decisions have evidence rows, none marked PASS on assumption.
- Both copies of every GeoJSON are byte-identical and test-enforced.
- The i18n ASCII convention and both-language coverage are test-enforced.
- A human has exercised the overlay on the heaviest and lightest Living Labs in EN and DE.
</must_haves>

<verification>
- `cd data-pipeline && .venv/Scripts/python.exe -m pytest tests -q` passes.
- `cd app && npm run lint && npm run build` exits 0.
- `05-VERIFICATION.md` contains no FAIL rows, or the FAIL rows are carried into a gap-closure plan.
</verification>

<success_criteria>
Phase 5 is closed with reproducible evidence for D-01 through D-08 and explicit human sign-off on
the two judgement calls the phase makes.
</success_criteria>

<output>
Create `.planning/phases/05-add-protected-areas-as-toggleable-layer-on-landscape-map/05-04-SUMMARY.md` when done.
Record the human's answers to the mask-ordering and repository-size questions verbatim.
</output>
