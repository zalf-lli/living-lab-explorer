---
phase: 14-add-soil-yield-potential-sqr-as-a-switchable-type-yield-pote
plan: 11
type: execute
wave: 6
depends_on: ["14-04", "14-06", "14-08"]
files_modified:
  - app/src/pages/LLDetail.jsx
  - app/src/components/LLMap/index.jsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-07, D-10, D-12, D-13, D-20]

must_haves:
  truths:
    - "The soil tab opens on Type and shows exactly today's BUEK map until the visitor switches (D-01)."
    - "A Type / Yield potential row sits directly under the layer tabs, rendered by VariablePicker (D-02, D-03)."
    - "One picker instance drives both comparison columns (D-04)."
    - "Switching to Yield swaps the map, the legend, the legend note, the map attribution and the chart together (D-07, D-12, D-20)."
    - "The yield raster is fetched only when the soil tab is active and the mode is yield, and the BUEK GeoJSON is not fetched at the same time."
  artifacts:
    - path: "app/src/pages/LLDetail.jsx"
      provides: "useSoilControlState, the soil branch in LayerBar, and soilMode threaded to all three layouts"
      contains: "useSoilControlState"
    - path: "app/src/components/LLMap/index.jsx"
      provides: "soilMode prop, the yield raster branch, mode-resolved legend/note/status badges, mode-aware MapInfoControl"
      contains: "soilMode"
  key_links:
    - from: "app/src/pages/LLDetail.jsx"
      to: "app/src/components/LLMap/index.jsx"
      via: "soilMode prop threaded through sharedControls into all three layouts"
      pattern: "soilMode"
    - from: "app/src/components/LLMap/index.jsx"
      to: "app/src/data/layer_source_lookup.js"
      via: "resolveLayerSource(layer, mode) inside MapInfoControl"
      pattern: "resolveLayerSource"
---

<objective>
Wire the Type / Yield control into the page: one piece of state, one picker instance, and a map that swaps
its raster, legend, note and attribution together.

Purpose: this is where D-01 through D-05, D-07, D-12 and D-20 become visible. `14-CONTEXT.md` flags D-02 as
"a mid-discussion reversal and the most likely thing for a downstream agent to get wrong" — the ROADMAP says
the control mirrors the climate tab's Baseline/Change switcher, which is the on-map `PeriodSwitcher`, and the
user explicitly retracted that in favour of a sub-tab row under `LayerTabs` matching how the climate
*variables* are presented. `PeriodSwitcher.jsx` must not be touched by this plan.

Output: two modified files. Everything they depend on already exists and defaults to today's behaviour.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-add-soil-yield-potential-sqr-as-a-switchable-type-yield-pote/14-CONTEXT.md
@.planning/phases/14-add-soil-yield-potential-sqr-as-a-switchable-type-yield-pote/14-UI-SPEC.md
@.planning/phases/14-add-soil-yield-potential-sqr-as-a-switchable-type-yield-pote/14-PATTERNS.md

<interfaces>
Available from earlier plans, all defaulting to today's behaviour when no mode is passed:
  app/src/data/layers.js          SOIL_MODES ([{id:'type',labelKey:'soil.mode.type'},{id:'yield',labelKey:'soil.mode.yield'}]),
                                  LAYER_INDEX.get('soil').modes.{type,yield},
                                  resolveLayerAsset(layerId, { slug, variable, period, mode })
  app/src/data/layer_source_lookup.js  resolveLayerSource(appLayer, mode), SOIL_MODE_IDS
  app/src/components/VariablePicker.jsx  ({ variables, active, onChange, disabled, ariaLabelKey })
  app/src/components/BarChart.jsx        ({ layer, ll, compact, minHeightWhenEmpty, mode })
  i18n keys: soil.modeRowLabel, soil.mode.type, soil.mode.yield, legend.soilYield.note,
             map.soilYieldLoading, map.soilYieldError

`app/src/pages/LLDetail.jsx` structure:
  line 50-51   const [layer, setLayer] = useLayerState()
               const [climateVariable, setClimateVariable, periodMode, setPeriodMode, horizon, setHorizon] = useClimateControlState()
  line 130-145 const sharedControls = { layer, setLayer, climateVariable, setClimateVariable, periodMode,
                 setPeriodMode, horizon, setHorizon, period, isMobile, isNarrow }
  line 196-229 function LayerBar({ layer, setLayer, climateVariable, setClimateVariable, isMobile, sticky })
                 renders <LayerTabs .../> then a `layer === 'climate'` VariablePicker branch then the hint row
  line 396-415 useLayerState() and useClimateControlState(), both wrapping setters in startTransition
  LayerBar call sites: lines 440, 674, 1007 (LayoutSplit, LayoutStacked, LayoutCompare)
  LLMap call sites:    lines 471, 700, 882
  BarChart call sites: lines 550, 750, 932
  StatPanel call sites: lines 519, 685, 867  <- D-05: these must NOT gain a mode prop

`app/src/components/LLMap/index.jsx` structure:
  line 168     function RasterPmtilesLayer({ layerId, slug, variable, period, onStatus })
                 const layerUrl = resolveLayerAsset(layerId, { slug, variable, period })
  line 496     function SoilStatusBadge({ message, tone = 'info' })
  line 616     function MapInfoControl({ layer, slug, overlayIds = [] })
                 const layerSource = LAYER_SOURCE_INDEX.get(layer) ?? null
                 lines 630-632 resolve llStates -> providersByState -> spread override (the BORIS precedent)
  line 921     export default function LLMap({ ll, layer, height, variable, period, periodMode, horizon,
                                               onPeriodModeChange, onHorizonChange })
  line 940     const layerConfig = LAYER_INDEX.get(layer)
  line 942     const [climateState, setClimateState] = useState({ loading: false, error: false })
  line 943-947 soilUrl = useMemo(() => (layer === 'soil' ? resolveLayerAsset(layer, { slug: ll.slug }) : null), ...)
               const soilState = useGeoJSON(soilUrl)
  line 971     soilLegendEntries = useMemo(() => buildSoilLegendEntries(soilFeatureCollection), ...)
  line 1007    maskFeature = useMemo(() => (layerConfig?.available ? buildMaskFeature(boundaryFeature) : null), ...)
  line 1054    the `layerConfig?.type === 'raster'` RasterPmtilesLayer mount, keyed per layer/slug (+variable/period for climate)
  line 1067    the `layer === 'soil' && soilFeatureCollection` GeoJSON mount
  line 1097-1110 the SoilStatusBadge status rows for soil / economic / climate
  line 1117    the ComingSoonBadge, gated on layerConfig?.available
  line 1119    the `layer === 'climate'` PeriodSwitcher mount  <- not used by soil (D-02)
  line 1132    <MapInfoControl layer={layer} slug={ll.slug} overlayIds={...} />
  line 1136-1156 the <MapLegend layer entries note /> ternaries

`app/src/components/MapLegend.jsx` falls back to `LAYER_INDEX.get(layer)`'s own `legend` / `legendNoteKey`
when `entries` / `note` are null — so the yield entries and note must be passed explicitly from the call site,
exactly as climate's already are.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the soil mode state and the sub-tab row in LLDetail</name>
  <files>app/src/pages/LLDetail.jsx</files>
  <read_first>
    - app/src/pages/LLDetail.jsx (lines 40-60 for the state block, 120-150 for sharedControls, 190-232 for LayerBar, 396-420 for the two state hooks, and each of the three layout components around lines 420-460, 660-700 and 990-1060 for how climateVariable is threaded)
    - app/src/components/VariablePicker.jsx (the props it takes and the ariaLabelKey default added by plan 14-02)
    - app/src/data/layers.js (SOIL_MODES)
    - .planning/phases/14-add-soil-yield-potential-sqr-as-a-switchable-type-yield-pote/14-UI-SPEC.md (the "Type/Yield sub-tab row" contract: two entries, Type first, one shared instance, initial useState value must be 'type')
    - .planning/phases/14-add-soil-yield-potential-sqr-as-a-switchable-type-yield-pote/14-CONTEXT.md (D-02's explicit reversal away from PeriodSwitcher)
  </read_first>
  <action>
Add a `useSoilControlState()` hook beside `useLayerState()` and `useClimateControlState()`, in the same
one-liner shape: `useState('type')` for the initial value with a setter wrapped in `startTransition`, and
nothing else. SQR has no time axis, so there is no period or horizon analogue here. Give it the same
explanatory comment the climate hook carries — the state lives once, at this level, because D-04 requires one
shared instance to drive both of Phase 10's comparison columns identically. The initial value must be the
literal `'type'` and must not be derived from anything that could momentarily render Yield first (D-01).

Call it beside the existing two hooks and add `soilMode` and `setSoilMode` to the `sharedControls` object, so
all three layouts receive them through the existing spread rather than through three separate prop lists.

Extend `LayerBar`'s props with `soilMode` and `setSoilMode`, and add a second, mutually exclusive
`layer === 'soil'` branch beside the existing `layer === 'climate'` VariablePicker branch. It renders
`VariablePicker` with `variables={SOIL_MODES}`, `active={soilMode}`, `onChange={setSoilMode}` and
`ariaLabelKey="soil.modeRowLabel"`. Same component, same slot, same styling — D-03 forbids a new component,
a rename or copied styling. Carry the D-04 comment the climate branch already has: exactly one instance
governs both comparison columns below, because `LayerBar` renders once above them.

Thread `soilMode` / `setSoilMode` into all three `LayerBar` call sites (in `LayoutSplit`, `LayoutStacked` and
`LayoutCompare`) exactly as `climateVariable` / `setClimateVariable` already are, and pass `soilMode` down to
each layout's `LLMap` and `BarChart` call sites — three of each. `BarChart` takes it as `mode`, and it must be
passed as `layer === 'soil' ? soilMode : undefined` so no other tab's chart URL or colour resolution changes.

Do not add a mode prop to any of the three `StatPanel` call sites. D-05 is explicit: the KPI bar does not
react to the sub-tab, and both soil modes show the identical three tiles. Add a short comment at one
`StatPanel` call site recording that this omission is deliberate.

Do not import or render `PeriodSwitcher` for soil. D-02 replaced that reading; the ROADMAP's
"mirroring the climate tab's Baseline/Change control" wording is superseded.
  </action>
  <verify>
    <automated>cd app && npm run lint && npm run build && node -e "
import('node:fs').then(fs=>{
const s=fs.readFileSync('src/pages/LLDetail.jsx','utf8');
const need=['useSoilControlState',\"useState('type')\",'SOIL_MODES','ariaLabelKey=\"soil.modeRowLabel\"'];
for(const n of need) if(!s.includes(n)) throw new Error('missing: '+n);
const count=(re)=>(s.match(re)||[]).length;
if(count(/soilMode=\{soilMode\}/g)<3) throw new Error('soilMode not threaded to all three LayerBar call sites');
if(count(/mode=\{layer === 'soil' \? soilMode : undefined\}/g)<3) throw new Error('BarChart/LLMap mode guard not applied at every call site');
if(/<StatPanel[^>]*mode=/.test(s)) throw new Error('D-05: StatPanel must not receive a mode prop');
if(/PeriodSwitcher/.test(s.split('function LayerBar')[1]||'')) throw new Error('D-02: no PeriodSwitcher in the soil control row');
console.log('OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - `useSoilControlState()` exists beside the other two state hooks, initialises to the literal `'type'`, and wraps its setter in `startTransition`.
    - `soilMode` and `setSoilMode` are members of `sharedControls`.
    - `LayerBar` renders `VariablePicker` with `SOIL_MODES`, `soilMode`, `setSoilMode` and `ariaLabelKey="soil.modeRowLabel"` under a `layer === 'soil'` branch that is mutually exclusive with the climate branch.
    - All three `LayerBar`, all three `LLMap` and all three `BarChart` call sites receive the mode, guarded so non-soil tabs pass `undefined`.
    - No `StatPanel` call site receives a `mode` prop, and a comment records that as deliberate (D-05).
    - `PeriodSwitcher` is not rendered for the soil tab anywhere.
    - `npm run lint` and `npm run build` exit 0.
  </acceptance_criteria>
  <done>One piece of state, one picker instance in the right slot, threaded to every surface that reacts to it and to none that must not.</done>
</task>

<task type="auto">
  <name>Task 2: Render the yield raster, legend, note and status badges in LLMap</name>
  <files>app/src/components/LLMap/index.jsx</files>
  <read_first>
    - app/src/components/LLMap/index.jsx (RasterPmtilesLayer at 168-200 including its remount-key and cancelled-guard comment; the LLMap props signature at 921; soilUrl/soilState at 943-947; soilLegendEntries at 971; maskFeature at 1007; the raster and soil GeoJSON mounts at 1054-1075; the status badges at 1097-1110; the availability badge at 1117; MapInfoControl at 616-640 and its call site at 1132; the MapLegend ternaries at 1136-1156)
    - app/src/data/layers.js (the soil modes map and resolveLayerAsset's mode parameter from plan 14-06)
    - app/src/data/layer_source_lookup.js (resolveLayerSource, from plan 14-02)
    - app/src/components/MapLegend.jsx (full file — the {value,en,de,color} entry shape, the entry.value keying and the LAYER_INDEX fallback that makes passing entries/note explicitly necessary)
    - .planning/phases/14-add-soil-yield-potential-sqr-as-a-switchable-type-yield-pote/14-UI-SPEC.md (the "Map render branch" and "Legend" component contracts)
  </read_first>
  <action>
Add a `soilMode` prop to `LLMap`, defaulting to `'type'` so any caller that has not been updated behaves
exactly as today (D-01). Derive one local `effectiveSoilConfig` from `LAYER_INDEX.get('soil').modes[soilMode]`
when the active layer is soil, and use it wherever the render needs to know the active soil asset's kind,
legend and note. Leave the module-level `layerConfig` lookup itself alone so every non-soil branch is
untouched.

Make the BUEK GeoJSON fetch conditional on the mode as well as the tab: `soilUrl` must be non-null only when
`layer === 'soil' && soilMode === 'type'`, and `soilMode` must be in its `useMemo` dependency array. This
extends the established "tabs lazy-load their asset only when active" pattern; the two soil assets must never
be in flight at once just because the tab is open.

Mount `RasterPmtilesLayer` for the yield case. The existing mount is gated on `layerConfig?.type === 'raster'`,
which is false for soil; widen that condition to also fire when the active layer is soil and the resolved mode
config's `type` is `'raster'`. Pass `layerId={layer}` and `slug={ll.slug}` with `variable` and `period` null —
SQR has no variable or period axis — and add a `mode` prop to `RasterPmtilesLayer` that it forwards into
`resolveLayerAsset(layerId, { slug, variable, period, mode })`. The remount `key` must include the mode
(`soil-<slug>-<mode>` shape) for the same reason the climate key includes variable and period: the component's
own comment states that exactly one overlay is ever in flight because the caller's key tears the previous one
down, and a mode change alters the URL without changing layer or slug.

Give the yield raster its own status state and badges. Add a second `useState({loading, error})` alongside
`climateState` and pass its setter as the raster's `onStatus` when the yield case is active, then render
`SoilStatusBadge` with `t('map.soilYieldLoading')` and, with `tone="error"`, `t('map.soilYieldError')` — the
same component the BUEK branch already uses, per the UI-SPEC's "reuse SoilStatusBadge verbatim, no new badge".
Gate the two existing BUEK badges on `soilMode === 'type'` so the two modes never show each other's status.

Make the map attribution mode-aware (D-07). Add a `mode` prop to `MapInfoControl`, defaulting to undefined,
and replace its `LAYER_SOURCE_INDEX.get(layer)` lookup with `resolveLayerSource(layer, mode)` imported from
`../../data/layer_source_lookup.js`. Leave the `llStates` / `providersByState` spread below it untouched — it
is the BORIS per-state override and is orthogonal. At the call site pass
`mode={layer === 'soil' ? soilMode : undefined}` so every other tab resolves through the flat key exactly as
before. Drop the now-unused `LAYER_SOURCE_INDEX` import if nothing else in the file uses it.

Make the legend and its note mode-resolved (D-10, D-11, D-12). Inside the existing `layer === 'soil'` arms of
the `MapLegend` `entries` and `note` ternaries, nest a `soilMode` branch: in yield mode pass the resolved mode
config's `legend` (the six-row `SOIL_YIELD_LEGEND`) and `t('legend.soilYield.note')`; in type mode pass
`soilLegendEntries` and `t('legend.soil.note')` exactly as today. Passing both explicitly matters because
`MapLegend` falls back to `LAYER_INDEX.get(layer)`'s own `legend` and `legendNoteKey`, which for the soil id
are still BUEK's.

Check the availability badge and the outer mask while you are in this file: `layerConfig?.available` currently
drives both the `ComingSoonBadge` and `maskFeature`. Confirm they behave correctly in yield mode and, if the
resolved mode config is the right thing to consult, use it — state the outcome either way in the plan summary
rather than leaving it unexamined.
  </action>
  <verify>
    <automated>cd app && npm run lint && npm run build && node -e "
import('node:fs').then(fs=>{
const s=fs.readFileSync('src/components/LLMap/index.jsx','utf8');
const need=['soilMode','resolveLayerSource','map.soilYieldLoading','map.soilYieldError','legend.soilYield.note'];
for(const n of need) if(!s.includes(n)) throw new Error('missing: '+n);
if(!/soilMode = 'type'/.test(s)) throw new Error('D-01: soilMode prop must default to type');
if(/LAYER_SOURCE_INDEX\.get\(layer\)/.test(s)) throw new Error('MapInfoControl must resolve through resolveLayerSource');
if(!/soilMode === 'type'/.test(s)) throw new Error('BUEK fetch/badges must be gated on type mode');
console.log('OK')})" && node -e "
import('./src/data/layers.js').then(({LAYER_INDEX,resolveLayerAsset})=>{
const y=LAYER_INDEX.get('soil').modes.yield;
if(y.type!=='raster')throw new Error('yield mode must resolve as raster');
if(resolveLayerAsset('soil',{slug:'rheingau',mode:'yield'})!=='data/pmtiles/soil-yield-rheingau.pmtiles')throw new Error('yield url');
if(resolveLayerAsset('soil',{slug:'rheingau',mode:'type'})!=='data/geojson/buek250-rheingau.geojson')throw new Error('type url');
console.log('OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - `LLMap` accepts `soilMode` defaulting to `'type'`.
    - `soilUrl` is non-null only when `layer === 'soil' && soilMode === 'type'`, with `soilMode` in its dependency array.
    - `RasterPmtilesLayer` mounts for soil in yield mode, receives `mode`, forwards it into `resolveLayerAsset`, and its remount key includes the mode.
    - A dedicated status state drives `SoilStatusBadge` with `map.soilYieldLoading` / `map.soilYieldError`, and the two BUEK badges are gated on `soilMode === 'type'`.
    - `MapInfoControl` takes a `mode` prop and resolves through `resolveLayerSource`; the string `LAYER_SOURCE_INDEX.get(layer)` no longer appears in the file; the `llStates`/`providersByState` spread is unchanged.
    - The `MapLegend` `entries` and `note` ternaries branch on `soilMode` inside the soil arm, passing the six-row yield legend and `legend.soilYield.note` in yield mode.
    - No non-soil branch changed: `git diff` shows no edits inside the climate, economic, landscape or protected-areas code paths other than the `MapInfoControl` lookup swap.
    - `PeriodSwitcher` remains mounted only for `layer === 'climate'`.
    - The summary states what was decided about `layerConfig?.available` for the `ComingSoonBadge` and `maskFeature` in yield mode.
    - `npm run lint` and `npm run build` exit 0.
  </acceptance_criteria>
  <done>Flipping the sub-tab swaps the raster, the legend, the note, the status badges and the attribution together, and only one soil asset is ever fetched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI state (`soilMode`) -> asset URL and source lookup | A React-controlled string selects which static asset is fetched and which provenance is displayed |
| pipeline-generated legend entries and attribution -> rendered DOM | Bilingual labels, licence and citation text render inside the map surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-38 | Tampering | `soilMode` reaching a URL pattern | mitigate | The value originates from `SOIL_MODES` ids bound to a two-button picker and is validated again inside `resolveLayerAsset` and `resolveLayerSource` against `SOIL_MODE_IDS`; it is never free text |
| T-14-39 | Spoofing | The map crediting the wrong BGR product | mitigate | `MapInfoControl` resolves through `resolveLayerSource(layer, mode)`, so the panel shows SQR1000's provenance whenever the SQR raster is on screen (D-07); the verify command asserts the old flat lookup is gone |
| T-14-40 | Information disclosure | A stale overlay from the previous mode staying mounted | mitigate | The `RasterPmtilesLayer` remount key includes the mode, reusing the same one-overlay-in-flight guarantee the climate three-axis key already provides |
| T-14-41 | Denial of service | Fetching both soil assets at once | mitigate | `soilUrl` is gated on `soilMode === 'type'`, so the BUEK GeoJSON and the SQR PMTiles are never in flight together |
| T-14-42 | Information disclosure | XSS via legend or attribution strings | accept | All values render as React text children; `MapLegend` keys on `entry.value` and renders `entry[lang]` as text, and no `dangerouslySetInnerHTML` exists in this file |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages; `app/package.json` unchanged |
</threat_model>

<verification>
1. `npm run lint` and `npm run build` exit 0.
2. Both node assertion blocks pass, including the D-01 default and the removal of the flat `MapInfoControl` lookup.
3. `git diff --quiet app/src/components/PeriodSwitcher.jsx app/src/components/MapLegend.jsx app/src/components/StatPanel.jsx` — none of the three is touched by this plan.
4. `git status --porcelain` lists only `app/src/pages/LLDetail.jsx` and `app/src/components/LLMap/index.jsx`.
</verification>

<success_criteria>
The soil tab opens on the unchanged BUEK map with a Type / Yield row beneath the layer tabs; selecting Yield
swaps the raster, the six-band legend with its grey Not-rated row, the SQR legend note, the map attribution
and the chart together, while the KPI tiles and every other tab stay exactly as they were.
</success_criteria>

<output>
Create `.planning/phases/14-add-soil-yield-potential-sqr-as-a-switchable-type-yield-pote/14-11-SUMMARY.md` when done
</output>
