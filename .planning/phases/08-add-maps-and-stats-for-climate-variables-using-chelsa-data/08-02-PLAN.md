---
phase: 08-add-maps-and-stats-for-climate-variables-using-chelsa-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/data/layers.js
  - app/src/components/StatPanel.jsx
autonomous: true
requirements: [D-11, D-13, D-20, D-21]
user_setup: []

must_haves:
  truths:
    - "The two climate ramp families and the diverging ramp exist as named exports built only from existing theme.js tokens, so no later plan invents a hex code (D-13)"
    - "resolveLayerAsset can resolve a three-placeholder raster URL from {slug, variable, period} without breaking any existing single-placeholder caller"
    - "A KPI tile can render a second, independently-empty-able delta line beneath its value line (D-20)"
    - "The delta line carries its own unit, so a percent-change delta can sit under an absolute-value baseline (D-11)"
  artifacts:
    - path: "app/src/data/layers.js"
      provides: "CLIMATE_HEAT_RAMP, CLIMATE_WATER_RAMP, CLIMATE_DIVERGING_RAMP exports and a three-placeholder resolveLayerAsset"
      contains: "CLIMATE_HEAT_RAMP"
    - path: "app/src/components/StatPanel.jsx"
      provides: "Two-line KPI tile with an optional delta row"
      contains: "field.delta"
  key_links:
    - from: "app/src/data/layers.js"
      to: "app/src/theme.js"
      via: "named C.* token imports only, zero literal hex codes in the ramp arrays"
      pattern: "C\\.(orangeGhost|orange|orangeDark|orangeDeep|tealLight|tealMid|teal|tealBg|bg)"
    - from: "app/src/components/StatPanel.jsx"
      to: "ll.kpiByTab[tab][].delta"
      via: "optional field read with em-dash fallback"
      pattern: "field\\.delta"
---

<objective>
Land the two frontend contract changes that do not depend on the 08-03 checkpoint's choice of the
fourth variable: the climate colour-ramp constants plus a three-placeholder
`resolveLayerAsset`, and `StatPanel.jsx`'s new two-line tile shape.

Both are pure additions. `layers.js`'s `climate` entry stays `type: 'placeholder'` in this plan —
flipping it to a real raster before the 60 PMTiles exist would 404 the Climate tab. That flip
happens in `08-10`.

Purpose: `08-PATTERNS.md` flags the two-line tile as one of only two surfaces in this phase with no
analog anywhere in the codebase. Doing it early and in isolation keeps it from being squeezed into a
plan that is also wiring maps.
Output: extended `app/src/data/layers.js` and `app/src/components/StatPanel.jsx`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-CONTEXT.md
@.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-UI-SPEC.md
@.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-PATTERNS.md

<interfaces>
From app/src/theme.js — the only permitted source of ramp hex values (zero new colours, D-13):

  export const C = {
    orange: '#eb5b25', orangeDark: '#dc4b14', orangeDeep: '#bb3f11', orangeGhost: '#fce3da',
    teal: '#005754', tealMid: '#008581', tealLight: '#00b3ad', tealBg: '#00413f',
    bg: '#f9fef9', muted: ..., mutedLight: ..., white: '#ffffff', greenMid: ...,
  }

Current signature in app/src/data/layers.js:

  export function resolveLayerAsset(layerId, { slug } = {}) -> string | null

Existing callers (app/src/components/LLMap/index.jsx lines 148, 890, 899, 922) all pass `{ slug }`
only. The extension must be additive so those four call sites keep working untouched.

Current KPI entry shape produced by data-pipeline/python/generate_metadata.py::_build_kpi_by_tab():

  { key: string, value: number | null, unit: { en: string, de: string },
    genesisTable: string | null, sourceHost: string | null }

The shape this phase adds (delivered by 08-09, consumed optionally here):

  { ...as above, delta: number | null, deltaUnit: { en: string, de: string } | null,
    deltaHorizon: string | null }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export the climate ramps and extend resolveLayerAsset to three placeholders</name>

  <read_first>
    - app/src/data/layers.js (the file being modified — see the `BORIS_RAMP` export at lines 22-31 for the ramp-constant convention and `resolveLayerAsset` at lines 89-99 for the current resolver)
    - app/src/theme.js (the `C` palette — the only permitted source of ramp hex values)
    - .planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-UI-SPEC.md (the "Ramp contract (D-11, D-12, D-13)" section, which locks the exact stops and band counts)
  </read_first>

  <files>app/src/data/layers.js</files>

  <action>
Add three ramp exports to `app/src/data/layers.js`, placed directly after the existing
`BORIS_HOVER_STYLE` export and preceded by a comment in the same voice as the `BORIS_RAMP` comment
(single source of truth, LLMap must import rather than redeclare, zero newly invented hues):

  `CLIMATE_HEAT_RAMP` = [C.orangeGhost, C.orange, C.orangeDark, C.orangeDeep] — 4 stops,
  light-to-dark = low-to-high, used by the heat-family variables (D-13).
  `CLIMATE_WATER_RAMP` = [C.tealLight, C.tealMid, C.teal, C.tealBg] — 4 stops, light-to-dark =
  low-to-high, used by the water-family variables (D-13).
  `CLIMATE_DIVERGING_RAMP` = [C.orangeDark, C.orange, C.bg, C.tealMid, C.teal] — 5 stops, centred
  on `C.bg` as the near-zero neutral, used only for a change map whose sign varies across the five
  Living Labs (D-12).

Every element must be a `C.*` token reference; no literal hex string may appear in any of the three
arrays.

Then extend `resolveLayerAsset` from `(layerId, { slug } = {})` to
`(layerId, { slug, variable, period } = {})`. In the `type === 'raster'` branch, when
`layer.pmtilesUrlPattern` is present, replace every placeholder the pattern actually contains:
`{slug}` from `slug`, `{variable}` from `variable`, `{period}` from `period`. If the pattern
contains a placeholder for which no value was supplied, return `null` rather than emitting a URL
with an unresolved `{...}` token in it — a 404 with a literal brace in the path is far harder to
diagnose than a null. Leave the `type === 'vector'` branch (soil, economic, protected-areas)
completely unchanged, and leave the `layer.pmtilesUrl` fallback for the single-file `agriculture`
layer unchanged.

Do not touch the `climate` entry in `LAYERS` and do not touch `LAYER_COLORS` in this plan — both
are 08-10's work, and changing them here would break the Climate tab before any raster exists.
  </action>

  <verify>
    <automated>cd app && npm run lint && node --input-type=module -e "import('./src/data/layers.js').then(m=>{const hex=/#[0-9a-fA-F]{6}/;for(const n of ['CLIMATE_HEAT_RAMP','CLIMATE_WATER_RAMP','CLIMATE_DIVERGING_RAMP']){if(!Array.isArray(m[n]))throw new Error(n+' missing');if(m[n].some(v=>typeof v!=='string'||!hex.test(v)))throw new Error(n+' bad stops')}if(m.CLIMATE_HEAT_RAMP.length!==4||m.CLIMATE_WATER_RAMP.length!==4||m.CLIMATE_DIVERGING_RAMP.length!==5)throw new Error('band counts wrong');if(m.resolveLayerAsset('landscape',{slug:'rheingau'})!=='data/pmtiles/land-cover-rheingau.pmtiles')throw new Error('regression: landscape resolver');if(m.resolveLayerAsset('soil',{slug:'rheingau'})!=='data/geojson/buek250-rheingau.geojson')throw new Error('regression: soil resolver');console.log('ok')})"</automated>
  </verify>

  <acceptance_criteria>
    - `app/src/data/layers.js` exports `CLIMATE_HEAT_RAMP` (4 entries), `CLIMATE_WATER_RAMP` (4 entries) and `CLIMATE_DIVERGING_RAMP` (5 entries)
    - No literal hex string appears inside any of the three ramp array literals: `grep -n 'CLIMATE_\(HEAT\|WATER\|DIVERGING\)_RAMP = \[' app/src/data/layers.js` shows lines containing only `C.` references
    - `resolveLayerAsset('landscape', { slug: 'rheingau' })` still returns `data/pmtiles/land-cover-rheingau.pmtiles`
    - `resolveLayerAsset('soil', { slug: 'rheingau' })` still returns `data/geojson/buek250-rheingau.geojson`
    - `resolveLayerAsset` returns `null` for a raster layer whose `pmtilesUrlPattern` contains a placeholder with no supplied value; no returned string ever contains `{` or `}`
    - `app/src/data/layers.js` still contains `{ id: 'climate', type: 'placeholder'` unchanged
    - `npm run lint` exits 0
  </acceptance_criteria>

  <done>Three ramp constants exist as theme-token-only exports, and the resolver handles three placeholders with all existing callers unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two-line delta row to the StatPanel KPI tile</name>

  <read_first>
    - app/src/components/StatPanel.jsx (the file being modified — the current single-line tile is lines 101-131)
    - .planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-UI-SPEC.md (the "KPI tile — new two-line shape (D-19, D-20, D-21, D-22)" section and the Typography table row "Stat delta (KPI tile secondary line — new, D-20)")
    - app/src/theme.js (the `C.muted` token the delta row uses)
  </read_first>

  <files>app/src/components/StatPanel.jsx</files>

  <action>
Extend the tile rendered inside the `fields.map(...)` loop with a third line, rendered only when the
field carries a `delta` property at all (`'delta' in field`), so every existing non-climate tile in
Agriculture / Soil / Landscape / Socio-economic renders byte-identically to today.

Delta row specification, taken verbatim from 08-UI-SPEC.md and not re-derived:
  - `fontSize: 12`, `fontWeight: 400`, `color: C.muted`, `lineHeight: 1.3`
  - `marginTop: 4` — the `xs` spacing token; no bespoke value
  - sits inside the same tile container; the container's `background`, `borderRadius: 8`,
    `padding: '12px 16px'` and `border` are unchanged

Content: when `field.delta` is a number, render the locale-formatted value with an explicit sign for
positive numbers (so `+2.8` reads as an increase, not an absolute), then the delta unit, then the
horizon label. Format as `{signed value} {deltaUnit[lang]} {t('statPanel.byHorizon', { horizon })}`
where `horizon` comes from `field.deltaHorizon`. Use `Number(field.delta).toLocaleString(locale, {
signDisplay: 'exceptZero' })` so the sign is locale-correct rather than string-concatenated. The
delta unit comes from `field.deltaUnit?.[lang]` and falls back to `field.unit?.[lang]` when absent
— this is what lets a percent-change delta (`%`, for the precipitation family, D-11) sit under an
absolute baseline value while a temperature delta reuses the baseline unit.

When `'delta' in field` but `field.delta == null`, render the existing em-dash glyph `–` in the same
12px/400/`C.muted` style. The two lines empty independently: a tile may show a real baseline with an
em-dash delta or the reverse, per 08-UI-SPEC.md.

Add the one new i18n key this needs to `app/src/i18n.js`? No — do not touch `i18n.js` in this plan.
Instead reference `t('statPanel.byHorizon', { horizon: field.deltaHorizon })` and note in the
SUMMARY that the key itself is delivered by `08-05`; until then react-i18next renders the key name,
which is visible only on climate tiles that do not exist yet.

Leave the `hasPendingReview` computation keyed on `field.value == null` only — a missing delta must
not trigger the "pending review" footnote.
  </action>

  <verify>
    <automated>cd app && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `app/src/components/StatPanel.jsx` contains a conditional keyed on `'delta' in field` guarding the third line
    - The delta row's inline style object contains exactly `fontSize: 12`, `fontWeight: 400`, `color: C.muted`, `lineHeight: 1.3`, `marginTop: 4`
    - The delta value is formatted with `signDisplay: 'exceptZero'`
    - The delta unit resolution falls back from `field.deltaUnit?.[lang]` to `field.unit?.[lang]`
    - `hasPendingReview` is still computed from `field.value == null` and does not reference `field.delta`
    - The tile container styles (`background: C.white`, `borderRadius: 8`, `padding: '12px 16px'`, `border`) are unchanged from the current file
    - `npm run build` exits 0 and `npm run lint` exits 0
    - Rendering any existing tab's StatPanel produces no delta row, because no current KPI entry carries a `delta` key
  </acceptance_criteria>

  <done>A KPI tile renders an optional, independently-empty-able, unit-aware delta line and every existing tab's tiles are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ll_metadata.json` (build-time artifact) -> React render | Pipeline-produced JSON is read and rendered; it is repo-controlled, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | `resolveLayerAsset` URL construction from `variable`/`period` | mitigate | The resolver only substitutes into a pattern declared in `layers.js` (repo-controlled) and returns `null` rather than emitting a URL containing an unresolved `{...}` token; `variable`/`period` originate from a closed React state machine in 08-10, never from the URL bar or user text |
| T-08-07 | Tampering | delta value rendered into the DOM | accept | Values pass through `Number(...).toLocaleString(...)`, which cannot produce markup; React escapes text children by default; no `dangerouslySetInnerHTML` is introduced |
| T-08-04 | Information Disclosure | KPI tiles | accept | All rendered values are public open-data figures already published on the other four tabs |
| T-08-SC | Tampering | npm installs | mitigate | This plan installs zero packages — no `package.json` change. Any future dependency addition in this phase requires slopcheck plus a blocking human checkpoint |
</threat_model>

<verification>
- `cd app && npm run lint` exits 0
- `cd app && npm run build` exits 0
- The three ramp exports resolve to 4/4/5 hex stops and every stop is a `C.*` token reference
- `resolveLayerAsset` regression checks for `landscape` and `soil` return their pre-existing values
- The Agriculture, Soil, Landscape and Socio-economic tabs render exactly as before (no delta rows appear, because no current KPI entry carries a `delta` key)
</verification>

<success_criteria>
1. `CLIMATE_HEAT_RAMP`, `CLIMATE_WATER_RAMP` and `CLIMATE_DIVERGING_RAMP` are exported from `layers.js` with 4/4/5 stops, built only from `theme.js` tokens (D-13, D-12)
2. `resolveLayerAsset` accepts `{ slug, variable, period }`, resolves all three placeholders, never emits an unresolved brace, and leaves all four existing call sites working
3. `StatPanel.jsx` renders an optional 12px/400/`C.muted` delta row 4px below the value line, with its own unit and an explicit horizon label (D-20, D-21, D-11)
4. Both lines empty independently via the existing em-dash convention, and no existing tab's tiles change
</success_criteria>

<output>
Create `.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-02-SUMMARY.md` when done
</output>
