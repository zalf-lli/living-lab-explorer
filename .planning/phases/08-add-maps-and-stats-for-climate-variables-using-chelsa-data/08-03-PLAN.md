---
phase: 08-add-maps-and-stats-for-climate-variables-using-chelsa-data
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-SPIKE.md
autonomous: false
requirements: [D-05, D-06, D-07, D-08]
user_setup: []

must_haves:
  truths:
    - "A human, not an agent, has chosen which variable occupies the fourth (summer-heat) slot: true GDD via chelsa-cmip6, true GDD hand-rolled from monthly tas, or D-07's pre-agreed bio10 fallback"
    - "The decision was made with the measured costs from 08-SPIKE.md in front of the human, including the library's non-textbook GDD formula"
    - "Each option was presented with its own downstream re-planning cost stated in the option text, not just its dependency cost"
    - "Only the `bio10` outcome permits 08-04 through 08-10 to execute as written; `gdd-light` and `gdd-heavy` are re-planning triggers that halt the phase after this plan and return to `/gsd:plan-phase 8 --gaps`, because no plan in the current set implements either acquisition shape"
    - "If the outcome is not `bio10`, 08-SPIKE.md carries an explicit phase-halt note and the SUMMARY says the phase stops here pending re-planning"
    - "The chosen variable's id, KPI variable_key, unit and legend-note wording are written down before any code names them"
    - "The acquisition budget cap and the confirmed URL template are signed off, so fetch_climate.py has a locked contract to implement"
    - "The sources.yaml provenance text asserts only the license this phase actually verified"
    - "If the true-GDD heavy path is chosen, the chelsa-cmip6 dependency and its transitive chain pass a package-legitimacy gate before install"
  artifacts:
    - path: ".planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-SPIKE.md"
      provides: "A `## Locked decisions` section recording W-05..W-08 as decided, dated and human-approved"
      contains: "## Locked decisions"
  key_links:
    - from: "08-SPIKE.md `## Locked decisions`"
      to: "08-04 sources.yaml `chelsa-climate` entry"
      via: "variable id, variable_key, unit, URL template, license text and budget cap all transcribed, never re-derived"
      pattern: "W-05|W-06|W-07|W-08"
    - from: "08-SPIKE.md `### W-05` outcome"
      to: "08-04's execution precondition"
      via: "08-04 assumes the `bio10` acquisition shape; a non-bio10 outcome halts the phase for re-planning rather than proceeding"
      pattern: "bio10"
---

<objective>
Put the phase's one research-gated decision in front of the human with real numbers attached, and
record the answer so every downstream plan transcribes rather than re-derives it.

`08-CONTEXT.md` D-07 named this "the one research-gated decision in the phase" and pre-agreed
`bio10` as the fallback. `08-RESEARCH.md` then found that true GDD *is* reachable — but only via
`chelsa-cmip6==1.4`'s live cloud-compute path (~10 heavy transitive dependencies, network calls to
Google Cloud/ESGF at build time), and that the library's `growing_degree_days()` sums raw
temperatures on days at or above the threshold rather than the textbook `sum(max(T - 5, 0))`.
D-08 makes this variable the tab's default and the most audience-facing choice in the phase.

This is the Phase 7 `07-05` blocking-checkpoint precedent applied verbatim. No agent resolves this.

**The three options are not equally cheap, and this plan says so out loud.** Waves 3-7 (`08-04`
onwards) implement exactly one acquisition shape: static per-variable GeoTIFF windows fetched from a
URL template with a `chelsa_variable` token. That is the `bio10` shape. Neither the `gdd-heavy`
library-call path nor the `gdd-light` monthly-`tas`-plus-interpolation path is implemented anywhere
in the current plan set, so selecting either one **stops the phase here** and sends it back to
`/gsd:plan-phase 8 --gaps` to re-plan the acquisition wave. The human's choice stays genuinely free;
what this plan refuses to do is pretend all three answers cost the same.

Purpose: lock the variable list, the acquisition contract and the provenance text so `08-04`
onwards implement against fixed strings.
Output: a `## Locked decisions` section appended to `08-SPIKE.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-CONTEXT.md
@.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-RESEARCH.md
@.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-UI-SPEC.md
@.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-SPIKE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the decision brief from measured spike findings</name>

  <read_first>
    - .planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-SPIKE.md (the file being extended — W-01..W-04 and its `## Recommendation for the 08-03 checkpoint` section carry every measured figure this brief must quote)
    - .planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-RESEARCH.md (Summary; Standard Stack "Supporting" table; Package Legitimacy Audit; Pitfall 2 on the non-standard GDD formula; Assumptions A2/A4)
    - .planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-CONTEXT.md (D-05 through D-08, and the "Claude's Discretion" note on the 5 C base temperature)
    - .planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-04-PLAN.md (the plan whose acquisition shape the `bio10` outcome presupposes — Task 1's `climate.variables` schema and Task 2's `fetch_climate.py` contract are what the downstream-cost column must describe accurately)
    - .planning/phases/07-add-boris-land-value-maps-as-spatial-layer-for-socio-economi/07-05-PLAN.md (the Phase 7 blocking-decision format this checkpoint mirrors)
  </read_first>

  <files>.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-SPIKE.md</files>

  <action>
Append a `## Decision brief (for the 08-03 checkpoint)` section to `08-SPIKE.md`. It must be
readable on its own, quoting figures from W-01..W-04 rather than referring to them.

State plainly, at the top, the one fact that may already settle the matter: if W-02 recorded
`NOT PUBLISHED` for future-period monthly `tas`, then true GDD cannot be derived on the light path
at all — not even by hand-rolling the textbook formula — and the only route to GDD is the heavy
`chelsa-cmip6` cloud-compute path. If W-02 recorded `PUBLISHED`, a third option exists that the
research did not cost: hand-roll the textbook `sum(max(T - 5, 0))` formula from statically
published monthly `tas` with zero new dependencies. Present that third option only if W-02 supports
it, and cost it using W-04's measured per-read figures multiplied by 12 months x 5 GCMs x 2 horizons.

State second, immediately beneath, the downstream-cost fact that the rest of the brief must keep
visible: only the `bio10` outcome lets Waves 3-7 execute as already planned. `08-04` Task 1's
`climate.variables` schema carries a `chelsa_variable` URL-interpolation token and nothing else — no
field for a library-computed variable, no branch for a variable assembled from twelve monthly
grids — and `08-04` Task 2's `fetch_climate.py` implements a single acquisition mechanism whose
dry-run assertion of 44 planned remote reads (4 x 1 baseline + 4 x 2 x 5 GCMs) is arithmetic that
holds under `bio10` alone. `08-06` through `08-10` all consume the twelve uniform
`data/climate_source/chelsa-{variable}-{period}.tif` rasters `08-04` produces. Choosing `gdd-light`
or `gdd-heavy` therefore halts the phase after this plan and returns to `/gsd:plan-phase 8 --gaps`
to re-plan the acquisition wave. Say this as a fact about the plan set, not as a discouragement.

Then lay out a comparison table with one row per option and these columns: option name; new
`requirements.txt` lines; transitive dependency count; whether the build makes live calls to Google
Cloud / ESGF; measured or projected acquisition transfer and wall time (from W-04); GDD formula
fidelity (textbook `sum(max(T - 5, 0))` versus the library's `sum(T where T >= 5)`); Python 3.12
compatibility confidence; **downstream re-planning cost** (either "Waves 3-7 execute as planned" or
"halts the phase, return to `/gsd:plan-phase 8 --gaps` before `08-04` can run"); and the resulting
variable id, KPI `variable_key`, EN/DE unit, and D-14 legend-note wording.

Use these concrete identifier proposals so the human is approving strings, not concepts:
  - true-GDD option (either path): variable id `gdd`, `variable_key` `gdd5_degc_days`, unit EN
    `degC-day` / DE `degC-Tag`, heat ramp family, legend note per 08-UI-SPEC.md's GDD row
  - bio10 fallback: variable id `bio10`, `variable_key` `warm_quarter_temp_degc`, unit EN/DE
    `degC`, heat ramp family, legend note needing fresh EN/DE wording ("Mean air temperature of the
    warmest three-month quarter" / "Mittlere Lufttemperatur des waermsten Vierteljahres") since
    08-UI-SPEC.md only drafted GDD's

Record explicitly that the other three variables are fixed regardless of the outcome: `bio1`
(`mean_annual_temp_degc`, EN/DE unit `degC`, heat family), `bio12` (`annual_precip_mm`, EN/DE unit
`mm`, water family), `bio18` (`warm_quarter_precip_mm`, EN/DE unit `mm`, water family). D-05's count
of four variables and 60 rasters is unchanged by either choice.

Also surface, as separate sign-off items in the same brief:
  - the literal URL template(s) from W-01 that `sources.yaml` will carry
  - the verbatim license / attribution / citation text from W-03, with an explicit flag if it
    differs from the CHELSA V2.1 baseline's CC0 — `sources.yaml` must not assert a license this
    phase has not verified
  - the acquisition budget cap from W-04 (max seconds per windowed read, max total transfer bytes)
    that `fetch_climate.py` will enforce
  - the D-06 base temperature: 5 degC, matching `chelsa_cmip6`'s own default; research recommends
    no change, and the human is asked to confirm rather than re-open

Note that W-06, W-07 and W-08 are worth locking regardless of the W-05 outcome: they describe the
baseline product and the transfer budget, which every option needs, so a re-planning outcome does
not waste them.

If the true-GDD heavy option is on the table, note that choosing it triggers a mandatory package
legitimacy gate before install: `chelsa-cmip6==1.4` itself already carries a live `slopcheck [OK]`
verdict recorded in 08-RESEARCH.md's Package Legitimacy Audit, but its ~10 transitive dependencies
(`xarray`, `dask`, `zarr`, `gcsfs`, `netcdf4`, `h5netcdf`, `esgf-pyclient`, `siphon`, the
`google-cloud-storage` family, `aiohttp`) were not individually audited and must be before any
`pip install` runs.
  </action>

  <verify>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-SPIKE.md').read_text(encoding='utf-8'); need=['## Decision brief','gdd5_degc_days','warm_quarter_temp_degc','mean_annual_temp_degc','annual_precip_mm','warm_quarter_precip_mm','slopcheck','--gaps']; missing=[n for n in need if n not in t]; sys.exit(('missing: '+str(missing)) if missing else 0)"</automated>
  </verify>

  <acceptance_criteria>
    - `08-SPIKE.md` contains a `## Decision brief (for the 08-03 checkpoint)` section
    - The brief states the W-02 consequence explicitly (whether true GDD is reachable on the light path at all)
    - The brief states, before the comparison table, that only the `bio10` outcome lets Waves 3-7 execute as planned, and names `08-04`'s `chelsa_variable` schema token and its 44-read dry-run assertion as the specific reasons
    - The comparison table has one row per option and includes columns for new requirements lines, transitive dependency count, live-network-at-build-time, measured acquisition cost, GDD formula fidelity, and downstream re-planning cost
    - The downstream re-planning cost cell for `gdd-light` and for `gdd-heavy` names `/gsd:plan-phase 8 --gaps` explicitly
    - The brief names all five candidate `variable_key` strings: `gdd5_degc_days`, `warm_quarter_temp_degc`, `mean_annual_temp_degc`, `annual_precip_mm`, `warm_quarter_precip_mm`
    - The brief quotes the W-01 URL template, the W-03 license/attribution/citation text, and the W-04 budget cap as separate sign-off items
    - The brief names `slopcheck` and the unaudited transitive dependency list as a precondition of the true-GDD heavy option
    - Every numeric figure in the brief also appears in W-01..W-04 (no figure introduced here that the probe did not measure)
  </acceptance_criteria>

  <done>A human can read one section and choose between fully-costed options — dependency cost and re-planning cost both stated — without opening the research document.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Blocking decision — fourth variable, acquisition contract and provenance text</name>

  <read_first>
    - .planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-SPIKE.md (the `## Decision brief` section written in Task 1)
  </read_first>

  <files>.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-SPIKE.md</files>

  <action>
Present the `## Decision brief (for the 08-03 checkpoint)` section written in Task 1 to the user in
full — the W-02 consequence line, the downstream re-planning cost paragraph, the option comparison
table with its measured costs and its re-planning-cost column, the four concrete `variable_key`
proposals, and the three separate sign-off items W-06, W-07 and W-08 — then halt and wait.

Do not resolve W-05 by inference from the research recommendation. Do not steer the human toward
`bio10` because it is the cheaper outcome for the plan set; state the cost and let them decide. Do
not proceed to Task 3 on a partial reply: all four verdicts are needed because `08-04` transcribes
all four, and W-06/W-07/W-08 are worth locking even if W-05 triggers re-planning.
  </action>

  <verify>
    <automated>python -c "print('checkpoint: awaiting human reply on W-05, W-06, W-07, W-08')"</automated>
    <human-check>The user has replied with a W-05 selection plus explicit verdicts on W-06, W-07 and W-08, having been shown each option's downstream re-planning cost.</human-check>
  </verify>

  <decision>
Four sign-offs, taken together:

**W-05 — Which variable occupies the fourth (summer-heat) slot?** This is the Climate tab's default
variable on first open (D-08), so it is the most audience-facing choice in the phase. It is also the
only one of the four sign-offs that can change what happens next: one answer lets the phase run to
completion as planned, the other two send it back for re-planning of the acquisition wave. Both
outcomes are legitimate; pick the variable you want, not the one that is cheapest to plan.

**W-06 — Confirmed source URL template.** Approve the literal template(s) from W-01 that
`sources.yaml` will carry for baseline and future periods.

**W-07 — Provenance text.** Approve the verbatim `license`, `attribution`, `citation` and `note`
strings from W-03 for the new `chelsa-climate` `sources.yaml` entry. If W-03 could not confirm the
CMIP6-derived product's license independently, approve conservative wording that states what was
verified and what was not, rather than inheriting the baseline product's CC0 by assumption.

**W-08 — Acquisition budget cap.** Approve the max-seconds-per-read and max-total-transfer figures
from W-04 that `fetch_climate.py` will enforce as hard limits.
  </decision>

  <context>
D-07 pre-agreed `bio10` as the fallback; research found true GDD reachable but only through a
materially heavier dependency chain whose own GDD formula is non-standard. D-08 bets the tab's
default on GDD being the most on-brand variable for an agricultural Living Lab audience, and
D-08's own commentary calls it "the one choice in this phase most worth revisiting". The 60-raster
count (D-05) and the other three variables are identical either way — only the fourth slot moves.

What differs between the options beyond dependency footprint is how much of this phase is already
planned for them. Plans `08-04` through `08-10` were written against one acquisition shape: static
per-variable GeoTIFF windows pulled from a URL template that interpolates a `chelsa_variable` token.
`bio10` is that shape. `gdd-light` needs a monthly-`tas` fetch plus a monthly-to-daily interpolation
step; `gdd-heavy` needs a library install plus a live cloud-compute call. Neither exists in any
current plan, so neither can be executed without going back to `/gsd:plan-phase 8 --gaps` first.
  </context>

  <options>
    <option id="gdd-heavy">
      <name>True GDD via `chelsa-cmip6==1.4` (heavy path) — RE-PLANNING TRIGGER</name>
      <pros>Keeps D-06/D-08's intended default variable; GDD is the most directly meaningful index for an agricultural audience; the library exposes `.gdd()` with a 5 degC default matching D-06 exactly</pros>
      <cons>Adds ~10 heavy transitive dependencies against PROJECT.md's "no new heavy dependencies without a clear forcing function"; makes the build depend on live Google Cloud / ESGF calls; the library's formula is `sum(T where T >= 5)`, not the textbook `sum(max(T - 5, 0))`, so the published numbers will not match agronomic GDD figures unless reimplemented — a standing correctness caveat on this branch that must be disclosed in the legend note if the formula is kept as-is; Python 3.12 only claimed compatible via a `>=3.6` metadata floor; transitive chain not yet slopcheck-audited</cons>
      <downstream-cost>Halts the phase after `08-03`. No plan in the current set installs `chelsa-cmip6`, calls `GetClim`/`BioClim`, or carries a `climate.variables` field for a library-computed variable — `08-04` Task 1's schema has only a `chelsa_variable` URL token, and `08-04` Task 2's `fetch_climate.py` implements a single URL-fetch mechanism whose 44-planned-read dry-run assertion is `bio10` arithmetic. Selecting this means execution stops at the end of this plan and the developer runs `/gsd:plan-phase 8 --gaps` to plan a new acquisition wave (library install behind a transitive-dependency slopcheck gate, a cloud-compute fetch path, and a formula-fidelity decision) before `08-04` can run in any form.</downstream-cost>
    </option>
    <option id="gdd-light">
      <name>True GDD hand-rolled from statically published monthly `tas` — RE-PLANNING TRIGGER (only selectable if 08-01 recorded W-02 = PUBLISHED)</name>
      <pros>Textbook `sum(max(T - 5, 0))` formula with no fidelity caveat; zero new dependencies; static files only, no live cloud compute; keeps D-06/D-08's default variable</pros>
      <cons>Only available if `08-01` recorded W-02 = PUBLISHED for future-period monthly `tas` — if W-02 recorded NOT PUBLISHED this option does not exist and must not be selected; requires 12 months x 5 GCMs x 2 horizons additional remote reads, materially raising the W-04 acquisition cost and pressing on the W-08 transfer cap; adds a monthly-to-daily interpolation step with no prior precedent in this codebase</cons>
      <downstream-cost>Halts the phase after `08-03`. No plan in the current set fetches monthly `tas` or interpolates twelve monthly grids into a daily series — `08-04` Task 1's `climate.variables` schema has no field or branch for a variable assembled from multiple source grids, and `08-04` Task 2's `fetch_climate.py` reads exactly one window per (variable, period, GCM). Selecting this means execution stops at the end of this plan and the developer runs `/gsd:plan-phase 8 --gaps` to plan the monthly-`tas` fetch plus interpolation step and a revised transfer budget before `08-04` can run.</downstream-cost>
    </option>
    <option id="bio10">
      <name>D-07's pre-agreed `bio10` fallback (mean temperature of the warmest quarter) — PHASE PROCEEDS AS PLANNED</name>
      <pros>Zero new dependencies; identical acquisition path to bio1/bio12/bio18; ships from the same static product as everything else in the phase; no formula-fidelity caveat to explain to users</pros>
      <cons>Loses the deliberately-chosen agricultural framing of D-08; requires fresh EN/DE legend-note copy since 08-UI-SPEC.md only drafted GDD's; the tab's default becomes a second temperature variable alongside bio1, which is less differentiated</cons>
      <downstream-cost>None. `08-04` through `08-10` are written for exactly this acquisition shape and execute as planned with no re-planning step: `08-04` fetches 44 windows into twelve `data/climate_source/chelsa-{variable}-{period}.tif` rasters and every later plan consumes them.</downstream-cost>
    </option>
  </options>

  <resume-signal>
Reply with the W-05 selection (`gdd-heavy`, `gdd-light`, or `bio10`) plus explicit approval or
correction for W-06 (URL template), W-07 (license/attribution/citation/note text) and W-08 (budget
cap).

Be aware of what each W-05 answer does next. **`bio10` is the only answer that lets this phase keep
running**: Task 3 records the locked decisions and `08-04` proceeds. **`gdd-light` and `gdd-heavy`
are re-planning triggers, not transcribe-and-proceed outcomes**: Task 3 will still record your
decision and the W-06/W-07/W-08 sign-offs, then the phase halts and you run
`/gsd:plan-phase 8 --gaps` to plan the acquisition wave your answer requires before `08-04` can
execute. That is a legitimate outcome, not a rejection of your choice — pick the variable you
actually want.

If you pick `gdd-heavy`, also confirm whether you accept the non-textbook `sum(T where T >= 5)`
formula as-is or want it reimplemented, and acknowledge that the transitive dependency chain must
pass slopcheck before install. If you pick `gdd-light`, confirm that `08-01` recorded W-02 =
PUBLISHED; if it did not, this option is unavailable and you must choose between `gdd-heavy` and
`bio10`.
  </resume-signal>

  <acceptance_criteria>
    - Execution halts here and does not proceed to Task 3 without a human reply
    - The reply names one of `gdd-heavy`, `gdd-light`, `bio10`
    - If the reply names `gdd-light`, `08-01`'s W-02 verdict is PUBLISHED; if W-02 recorded NOT PUBLISHED, the executor says so and re-asks rather than accepting the selection
    - The reply gives an explicit verdict on each of W-06, W-07 and W-08
    - The presented options each carried a downstream re-planning cost statement, and no option was omitted or discouraged in the presentation
  </acceptance_criteria>

  <done>A human has selected the fourth variable with both its dependency cost and its re-planning cost in view, and approved the acquisition and provenance contract.</done>
</task>

<task type="auto">
  <name>Task 3: Record the locked decisions and, if the outcome is not bio10, halt the phase</name>

  <read_first>
    - .planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-SPIKE.md (the file being extended)
    - .planning/phases/07-add-boris-land-value-maps-as-spatial-layer-for-socio-economi/07-SPIKE.md (how Phase 7 recorded its W-01..W-03 locked checkpoint decisions — "locked YYYY-MM-DD", "do not re-derive; transcribed as-is")
  </read_first>

  <files>.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-SPIKE.md</files>

  <action>
Append a `## Locked decisions` section to `08-SPIKE.md` recording exactly what the human approved,
in the Phase 7 voice ("locked <date>", "do not re-derive; transcribed as-is"). One subsection per
decision:

  `### W-05 — Fourth variable` — the chosen option id, the resulting variable id, `variable_key`,
  EN and DE unit strings, ramp family (heat), and the EN/DE legend-note wording. If `gdd-heavy` was
  chosen, also record the formula verdict (library formula as-is versus reimplemented textbook
  formula) and that the transitive dependency slopcheck gate is still outstanding. End this
  subsection with a one-line `Downstream:` verdict reading either `bio10 — 08-04 onwards execute as
  written` or `<option> — re-planning required before 08-04; see ## Phase halt below`.
  `### W-06 — Source URL templates` — the literal baseline and future-period template strings.
  `### W-07 — Provenance text` — the verbatim `license`, `attribution`, `citation` and `note`
  strings destined for the `chelsa-climate` `sources.yaml` entry.
  `### W-08 — Acquisition budget cap` — the max seconds per windowed read and max total transfer
  bytes that `fetch_climate.py` must enforce.

Also restate, in the same section, the full locked four-variable table (id, `variable_key`, EN unit,
DE unit, ramp family, i18n label key, legend-note key) covering the three fixed variables plus the
chosen fourth — this table is the single string source every later plan copies from.

Lock the shared naming contract here too, so no later plan invents it:
  - PMTiles pattern `data/pmtiles/climate-{variable}-{period}-{slug}.pmtiles`
  - period tokens `baseline`, `2041_2070`, `2071_2100`
  - Germany-extent intermediate rasters at `data/climate_source/chelsa-{variable}-{period}.tif`
  - new `source_host` enum value `chelsa` (D-23)
  - computed KPI file `data/climate_kpis.json`, shared colour breaks `data/climate_color_breaks.json`
  - `sources.yaml` layer id `chelsa-climate`, `app_layer: climate`, `classification: continuous`

If the human's reply differed from Task 1's proposals in any respect, record what they said, not
what was proposed.

Then branch on the W-05 outcome:

  If W-05 = `bio10`: append a one-line `## Phase status` note reading that `08-04` may proceed as
  written, and finish the plan normally.

  If W-05 = `gdd-light` or `gdd-heavy`: append a `## Phase halt — re-planning required` section
  naming the chosen option, stating that no plan in the current set implements its acquisition
  shape, listing concretely what a re-planned acquisition wave must add (for `gdd-light`: a monthly
  `tas` fetch across 12 months x 5 GCMs x 2 horizons, a monthly-to-daily interpolation step, a
  revised W-08 transfer budget, and a `climate.variables` schema field for a multi-source-grid
  variable; for `gdd-heavy`: a transitive-dependency slopcheck gate, a `chelsa-cmip6==1.4` install,
  a live cloud-compute acquisition path, a `climate.variables` schema field for a library-computed
  variable, and a formula-fidelity resolution), and instructing the developer to run
  `/gsd:plan-phase 8 --gaps` before any further execution. Do not start `08-04`. Make this the
  headline of the `08-03-SUMMARY.md` — first line of the summary, not a footnote — so the phase
  orchestrator and the developer both see the halt without reading to the end.

W-06, W-07 and W-08 are recorded as locked in either branch; they are not invalidated by a
re-planning outcome.
  </action>

  <verify>
    <automated>python -c "import pathlib,sys,re; t=pathlib.Path('.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-SPIKE.md').read_text(encoding='utf-8'); need=['## Locked decisions','### W-05','### W-06','### W-07','### W-08','data/pmtiles/climate-{variable}-{period}-{slug}.pmtiles','2041_2070','2071_2100','chelsa-climate','classification: continuous','data/climate_kpis.json','data/climate_color_breaks.json','Downstream:']; missing=[n for n in need if n not in t]; sys.exit(('missing: '+str(missing)) if missing else 0); "</automated>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-SPIKE.md').read_text(encoding='utf-8'); locked=t.split('## Locked decisions',1)[1]; is_bio10='bio10' in locked.split('### W-06',1)[0] and 'gdd-heavy' not in locked.split('### W-06',1)[0] and 'gdd-light' not in locked.split('### W-06',1)[0]; ok=('## Phase status' in locked) if is_bio10 else ('## Phase halt' in locked and '--gaps' in locked); sys.exit(0 if ok else 'W-05 outcome and phase-status/halt note disagree')"</automated>
  </verify>

  <acceptance_criteria>
    - `08-SPIKE.md` contains `## Locked decisions` with subsections `### W-05`, `### W-06`, `### W-07`, `### W-08`
    - `### W-05` names one of `gdd-heavy`, `gdd-light`, `bio10` and gives the resulting variable id, `variable_key` and EN/DE unit strings
    - `### W-05` ends with a `Downstream:` line stating either that `08-04` onwards execute as written (`bio10`) or that re-planning is required before `08-04` (the other two)
    - If W-05 is `bio10`: a `## Phase status` line states `08-04` may proceed
    - If W-05 is `gdd-light` or `gdd-heavy`: a `## Phase halt — re-planning required` section exists, names `/gsd:plan-phase 8 --gaps`, lists what the re-planned acquisition wave must add, and the first line of `08-03-SUMMARY.md` states the halt. `08-04` is NOT started under this outcome
    - The locked four-variable table lists exactly four rows, each with id, `variable_key`, EN unit, DE unit, ramp family, i18n label key and legend-note key
    - The naming contract block contains the literal strings `data/pmtiles/climate-{variable}-{period}-{slug}.pmtiles`, `baseline`, `2041_2070`, `2071_2100`, `data/climate_source/chelsa-{variable}-{period}.tif`, `chelsa`, `data/climate_kpis.json`, `data/climate_color_breaks.json`, `chelsa-climate`, `classification: continuous`
    - `### W-06`, `### W-07` and `### W-08` are recorded as locked regardless of the W-05 outcome
    - `### W-07`'s license text does not assert CC0 unless W-03 verified CC0 for the CMIP6-derived product specifically
  </acceptance_criteria>

  <done>Every string `08-04` onwards needs — variable ids, keys, units, URLs, license text, file patterns and budget caps — is written down once, approved, and marked as transcribe-only; and the phase either proceeds or halts explicitly rather than drifting into a plan that cannot execute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human decision -> downstream plan implementation | The locked strings recorded here become URLs and filesystem paths in later plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | pip install of `chelsa-cmip6` and its transitive chain (only under option `gdd-heavy`) | mitigate | Blocking human decision required before any install; `chelsa-cmip6==1.4` carries a recorded live `slopcheck [OK]` verdict, but the ~10 transitive dependencies are unaudited and must pass `slopcheck` before install; pin the exact version rather than a floor constraint. The `gdd-heavy` outcome halts the phase for re-planning, so the install is gated behind both this checkpoint and a fresh `--gaps` planning pass. This checkpoint is never auto-approvable |
| T-08-02 | Tampering | URL templates locked in W-06 | mitigate | Human approves the literal template strings; downstream `fetch_climate.py` validates every interpolated token against allow-lists before formatting, so an approved template cannot be widened by config drift |
| T-08-05 | Spoofing | source host locked in W-06 | mitigate | Approved templates must be `https://`; a plain-http template is rejected at this checkpoint |
| T-08-09 | Repudiation | provenance misstatement in W-07 | mitigate | `sources.yaml` must not assert a license this phase has not verified; the checkpoint explicitly offers conservative wording when W-03 could not confirm the CMIP6 product's license independently |
| T-08-18 | Repudiation | a non-`bio10` decision being silently absorbed and `08-04` executed anyway against an acquisition shape it does not implement | mitigate | Each option carries an explicit `<downstream-cost>` statement; Task 3 writes a `## Phase halt — re-planning required` section and makes it the SUMMARY headline; `08-04`'s objective and `must_haves` carry the reciprocal precondition, so the mismatch is detectable from either end |
</threat_model>

<verification>
- `08-SPIKE.md` contains both `## Decision brief (for the 08-03 checkpoint)` and `## Locked decisions`
- The four W-05..W-08 subsections each record a human verdict, not a proposal
- `### W-05` carries a `Downstream:` line, and exactly one of `## Phase status` (bio10) or `## Phase halt — re-planning required` (gdd-light / gdd-heavy) is present and consistent with it
- The locked four-variable table and the naming-contract block are present verbatim as the single string source for 08-04 onwards
- No code file is modified by this plan (`git status --porcelain -- data-pipeline app data` is empty)
</verification>

<success_criteria>
1. The fourth variable (D-06, D-07) is chosen by a human with measured dependency, network and formula-fidelity costs in front of them, plus each option's downstream re-planning cost, and D-08's default-variable consequence stated
2. The confirmed URL templates, provenance text and acquisition budget cap are approved as literal strings and locked regardless of the W-05 outcome
3. `08-SPIKE.md` carries a `## Locked decisions` section that every later plan transcribes from rather than re-deriving
4. The W-05 outcome is followed by an unambiguous phase verdict: proceed (`bio10`) or halt for `/gsd:plan-phase 8 --gaps` (`gdd-light`, `gdd-heavy`)
5. D-05's four-variable / 60-raster scope is unchanged by the outcome
</success_criteria>

<output>
Create `.planning/phases/08-add-maps-and-stats-for-climate-variables-using-chelsa-data/08-03-SUMMARY.md` when done
</output>
