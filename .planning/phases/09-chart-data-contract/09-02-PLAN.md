---
phase: 09-chart-data-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data-pipeline/sources/sources.yaml
  - data-pipeline/sync.py
autonomous: true
requirements: [CHARTS-02]

must_haves:
  truths:
    - "Running python sync.py with no chart files built names all 25 missing (layer, Living Lab) files individually and exits successfully (D-15)"
    - "Running python sync.py after chart files exist copies each one into app/public/data/charts/ under a [chart] log tag (D-10, D-11)"
    - "Every existing sync.py output line is unchanged for every non-chart layer"
    - "The four climate variables carry pipeline-owned bilingual display names, readable without touching app/src/i18n.js"
  artifacts:
    - path: "data-pipeline/sources/sources.yaml"
      provides: "chart.script + output.chart_pattern on all 5 chart-bearing layers, and label:{en,de} on all 4 climate variables"
      contains: "chart_pattern"
    - path: "data-pipeline/sync.py"
      provides: "sync_charts() plus a tag parameter on sync_file()/_sync_matched_pattern()"
      exports: ["sync_charts"]
  key_links:
    - from: "data-pipeline/sync.py"
      to: "data-pipeline/sources/sources.yaml"
      via: "output.chart_pattern lookup per layer"
      pattern: "chart_pattern"
    - from: "data-pipeline/sync.py::sync_charts"
      to: "data-pipeline/sync.py::_sync_matched_pattern"
      via: "tagged glob copy inheriting the repo-root-escape guard"
      pattern: "_sync_matched_pattern\\(.*tag=\"chart\""
    - from: "data-pipeline/sync.py::sync_to_app"
      to: "data-pipeline/sync.py::sync_charts"
      via: "orchestration call site"
      pattern: "sync_charts\\(\\)"
---

<objective>
Add the declarative `chart:` stanza to all five chart-bearing layers and the `sync_charts()`
plumbing that publishes their output, before any chart file exists.

Purpose: CHARTS-02 is the phase's plumbing requirement, and it is independently verifiable
in its "nothing built yet" state — a `sync.py` run right now must name all 25 missing files.
Landing this in wave 1 means plans 09-03/09-04/09-05 can read their own output path from
`sources.yaml` instead of hardcoding it, and the climate chart script can read D-16's
bilingual variable names from config instead of hardcoding German strings in Python.

Output: 5 `chart:` stanzas + 5 `output.chart_pattern` keys + 4 `climate.variables.*.label`
blocks in `sources.yaml`; `sync_charts()` wired into `sync_to_app()` in `sync.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-chart-data-contract/09-CONTEXT.md
@.planning/phases/09-chart-data-contract/09-RESEARCH.md
@.planning/phases/09-chart-data-contract/09-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The exact config values this plan writes. Plans 09-03/09-04/09-05 and 09-06's tests
     assert these literal strings — they are a cross-plan contract, not a suggestion. -->

sources.yaml, per layer (chart.script is a NEW sibling stanza to build:; chart_pattern is a
NEW key inside the layer's EXISTING output: block):

  id                  | app_layer   | chart.script                          | output.chart_pattern
  landuse-croptypes   | agriculture | python/compute_agriculture_chart.py   | data/charts/landuse-croptypes-{slug}.json
  io-lulc-landcover   | landscape   | python/compute_landscape_chart.py     | data/charts/io-lulc-landcover-{slug}.json
  buek250             | soil        | python/compute_soil_chart.py          | data/charts/buek250-{slug}.json
  boris               | economic    | python/compute_economic_chart.py      | data/charts/boris-{slug}.json
  chelsa-climate      | climate     | python/compute_climate_chart.py       | data/charts/chelsa-climate-{slug}.json

  bfn-schutzgebiete (app_layer: protected-areas) gets NEITHER key — it is an overlay, not
  one of the five chart-bearing map tabs (09-RESEARCH.md, "Exact line ranges" table).

sources.yaml, chelsa-climate climate.variables.{id}.label (NEW key, sibling to the existing
unit / delta_unit blocks) -- D-16:

  gdd   -> en: "Growing degree days"              de: "Wachstumsgradtage"
  bio1  -> en: "Mean annual temperature"          de: "Jahresmitteltemperatur"
  bio12 -> en: "Annual precipitation"             de: "Jahresniederschlag"
  bio18 -> en: "Precipitation of warmest quarter" de: "Niederschlag im waermsten Quartal"

sync.py signatures after this plan:

  sync_file(source: Path, destination: Path, *, tag: str = "sync") -> None
  _sync_matched_pattern(pattern: str, *, tag: str = "sync") -> int
  sync_charts() -> None
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare chart stanzas and climate variable labels in sources.yaml</name>
  <files>data-pipeline/sources/sources.yaml</files>
  <read_first>
    - data-pipeline/sources/sources.yaml (full file — in particular `landuse-croptypes` lines 13-68, `io-lulc-landcover` lines 70-129, `buek250` lines 130-175, `bfn-schutzgebiete` lines 176-211, `boris` lines 213-301, `chelsa-climate` lines 303-467, and the `climate.variables` block at lines 367-413)
    - .planning/phases/09-chart-data-contract/09-RESEARCH.md (`### Recommended sources.yaml chart: stanza shape` and the "Exact line ranges" table)
    - .planning/phases/09-chart-data-contract/09-PATTERNS.md (`### data-pipeline/sources/sources.yaml` — the stanza placement and the D-16 label example)
    - app/src/i18n.js (lines 84-100 and 331-346 — the existing short `climate.variable.*` picker labels, so the executor sees the deliberate divergence described in the action)
  </read_first>
  <action>
    Edit `data-pipeline/sources/sources.yaml` only. Three kinds of change, all additive —
    no existing key's value may change.

    (1) For each of the five layers in this plan's `<interfaces>` table, add a `chart:`
    block holding a single `script:` key, placed as a sibling of the layer's existing
    `build:` block and immediately after it. Use the exact script paths from the table.
    Follow the file's own convention of an inline `#` comment on new fields citing the
    decision that produced them — cite D-05 for agriculture, D-07 for landscape, D-06 for
    soil, D-08 for economic and D-09 for climate, plus D-11 (sync copies, never invokes,
    so this path is documentation for a human running the script by hand, not an
    orchestration hook).

    (2) For each of the same five layers, add a `chart_pattern:` key inside the layer's
    EXISTING `output:` block, using the exact quoted pattern strings from the table. Place
    it last within `output:` so the pre-existing keys keep their current order and line
    positions shift minimally. Cite D-10 in an inline comment on at least the first
    occurrence, noting the key sits under `output:` (not under `chart:`) because every
    other `*_pattern` key in this file lives under `output:`.

    Do NOT add either key to `bfn-schutzgebiete`. Its `app_layer` is `protected-areas`,
    an overlay with no chart tab; `sync_charts()` must skip it by finding no
    `output.chart_pattern`, which is also what Task 2's verification asserts.

    (3) In `chelsa-climate`'s `climate.variables` block, add a `label:` mapping with `en`
    and `de` sub-keys to each of the four variables (`gdd`, `bio1`, `bio12`, `bio18`),
    placed as a sibling immediately after each variable's existing `delta_unit:` block.
    Use the exact eight strings from this plan's `<interfaces>` table. Cite D-16 in an
    inline comment on the first occurrence, and note there that these are deliberately the
    long chart-legend names, distinct from `app/src/i18n.js`'s abbreviated picker labels
    (`GDD`, `Mean temp.`, `Precipitation`, `Summer precip.`), which are space-constrained
    button captions and stay unchanged. Also note the pipeline must never read
    `app/src/i18n.js` (wrong dependency direction — 09-RESEARCH.md Pitfall 4).

    Follow this file's existing German-text convention: transliterate umlauts as ae/oe/ue
    (matching `jaehrliche`, `fuer`, `Wohnbauflaeche`), which is why bio18's German label is
    `Niederschlag im waermsten Quartal`. Do not transliterate the pre-existing `°C·d` /
    `°C` unit strings — those are already correct Unicode and must stay byte-identical.

    Make no change to `sync.py`, to any script, or to any other layer key in this task.
  </action>
  <verify>
    <automated>cd data-pipeline && python -c "import yaml,pathlib; d=yaml.safe_load(pathlib.Path('sources/sources.yaml').read_text(encoding='utf-8')); L={l['id']:l for l in d['layers']}; exp={'landuse-croptypes':('python/compute_agriculture_chart.py','data/charts/landuse-croptypes-{slug}.json'),'io-lulc-landcover':('python/compute_landscape_chart.py','data/charts/io-lulc-landcover-{slug}.json'),'buek250':('python/compute_soil_chart.py','data/charts/buek250-{slug}.json'),'boris':('python/compute_economic_chart.py','data/charts/boris-{slug}.json'),'chelsa-climate':('python/compute_climate_chart.py','data/charts/chelsa-climate-{slug}.json')}; act={i:(L[i]['chart']['script'],L[i]['output']['chart_pattern']) for i in exp}; assert act==exp, act; assert 'chart' not in L['bfn-schutzgebiete'] and 'chart_pattern' not in L['bfn-schutzgebiete']['output'], 'overlay must not gain chart keys'; v=L['chelsa-climate']['climate']['variables']; lab={k:(v[k]['label']['en'],v[k]['label']['de']) for k in ('gdd','bio1','bio12','bio18')}; assert lab=={'gdd':('Growing degree days','Wachstumsgradtage'),'bio1':('Mean annual temperature','Jahresmitteltemperatur'),'bio12':('Annual precipitation','Jahresniederschlag'),'bio18':('Precipitation of warmest quarter','Niederschlag im waermsten Quartal')}, lab; assert v['gdd']['unit']['en']=='°C·d' and v['bio1']['unit']['en']=='°C', 'existing unit strings must be byte-identical'; assert list(v)==['gdd','bio1','bio12','bio18'], list(v); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All five chart-bearing layers have `chart.script` and `output.chart_pattern` set to the exact literal strings in this plan's `<interfaces>` table
    - `bfn-schutzgebiete` has no `chart` key and no `output.chart_pattern` key
    - All four `chelsa-climate` `climate.variables` entries have a `label` mapping with exactly the `en`/`de` strings in the `<interfaces>` table
    - `climate.variables` key order is still `gdd, bio1, bio12, bio18` (the Phase 8 D-08 default-variable ordering must survive the edit)
    - The pre-existing `unit`/`delta_unit` strings for gdd and bio1 are byte-identical (`°C·d`, `°C`) — no accidental ASCII transliteration of units
    - `git diff --stat data-pipeline/sources/sources.yaml` shows insertions only, zero deletions of pre-existing content lines
    - Each new key group carries an inline `#` comment citing its decision id (D-05..D-11, D-16), matching the file's existing comment convention
    - The verify command prints `OK`
  </acceptance_criteria>
  <done>sources.yaml declares every chart script path, every per-Living-Lab chart output pattern, and every climate variable's bilingual chart label, with the protected-areas overlay correctly excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Add tag-aware sync_file/_sync_matched_pattern and the sync_charts() publisher</name>
  <files>data-pipeline/sync.py</files>
  <read_first>
    - data-pipeline/sync.py (full file, 394 lines — especially `sync_file` lines 38-41, `_pattern_to_glob` lines 15-26, `_sync_matched_pattern` lines 320-346 including the repo-root-escape guard at 337-341, `sync_pmtiles_per_ll` lines 349-363, `sync_vector_geojson` lines 366-375, and `sync_to_app` lines 378-390)
    - data-pipeline/sources/sources.yaml (the `output.chart_pattern` keys added in Task 1)
    - data/ll_boundaries.geojson (the file `sync_charts()` reads for the five `ll_slug` values — confirmed shape: a FeatureCollection of 5 features whose only property is `ll_slug`)
    - data-pipeline/tests/conftest.py (the `LL_SLUGS` list `sync_charts()`'s slug set must match; note `sync.py` cannot import this test-only module and must read the boundaries file instead)
    - .planning/phases/09-chart-data-contract/09-RESEARCH.md (Pitfall 1 — the `[sync]`/`[skip]` hardcoding, and Open Question 3's resolution)
    - .planning/phases/09-chart-data-contract/09-PATTERNS.md (`### data-pipeline/sync.py (sync_charts(), modified)` and the `Shared Patterns` entries for the tag and the per-(layer, LL) check)
  </read_first>
  <action>
    Edit `data-pipeline/sync.py` only. Three changes.

    (1) Add a keyword-only `tag: str = "sync"` parameter to `sync_file(source, destination)`
    and use it in that function's print line so it emits `[{tag}] {source} -> {destination}`.
    The default keeps every one of the four existing call sites byte-identical in output.

    (2) Add the same keyword-only `tag: str = "sync"` parameter to
    `_sync_matched_pattern(pattern)`. Thread it into the `sync_file(...)` call inside the
    loop and into the trailing summary print, which becomes
    `[{tag}] {synced}/{len(matches)} files matched {pattern}`. Leave the early-return
    `print(f"[skip] no files matched {pattern}")` line with its `[skip]` tag unchanged —
    `[skip]` is a semantic "nothing happened" tag shared across `sync_pmtiles()` and this
    helper, and D-15's per-file chart reporting is delivered by change (3) instead, so
    nothing is lost by leaving it alone. Do not touch the repo-root-escape guard; it is
    the one security control this phase inherits (09-RESEARCH.md, Security Domain).

    (3) Add a new `sync_charts()` function placed immediately after `sync_vector_geojson()`,
    and call it from `sync_to_app()` on the line immediately after `sync_vector_geojson()`
    and before `generate_landuse_legend()` — keeping the file's existing grouping of all
    `sync_*` calls ahead of all `generate_*` calls.

    `sync_charts()` must:
    - read the five Living Lab slugs from `data/ll_boundaries.geojson` via the existing
      `resolve()` helper and `json.loads`, collecting `feature["properties"]["ll_slug"]`
      into a sorted list. Raise a `RuntimeError` naming the file if the collected set is
      empty. Do not hardcode the slug list and do not import from `conftest.py`.
    - iterate `load_sources()["layers"]`, skipping any layer with no
      `output.chart_pattern` (which is how `bfn-schutzgebiete` and any future
      non-chart layer are excluded — no explicit id allow-list).
    - for each remaining layer, first loop the slugs and, for every
      `pattern.format(slug=slug)` that does not exist on disk, print exactly
      `[chart] skipped - not yet built: {relative-pattern-path}` naming that one specific
      (layer, Living Lab) file. This satisfies D-15's requirement that the message name
      the missing file rather than report an aggregate count.
    - then call `_sync_matched_pattern(pattern, tag="chart")` to do the actual copying, so
      the copy path keeps inheriting `_pattern_to_glob()` and the repo-root-escape guard
      rather than re-implementing them (D-10).

    Do not add `sort_keys=True` to any of `sync.py`'s four pre-existing `json.dumps` calls.
    That gap between CLAUDE.md and this file is real but explicitly out of scope
    (09-CONTEXT.md, Claude's Discretion) — fixing it here would churn four generated JS
    files for no phase benefit.
  </action>
  <verify>
    <automated>cd data-pipeline && python -c "import inspect,sys; sys.path.insert(0,'.'); import sync; assert 'tag' in inspect.signature(sync.sync_file).parameters and inspect.signature(sync.sync_file).parameters['tag'].default=='sync'; assert 'tag' in inspect.signature(sync._sync_matched_pattern).parameters; assert callable(sync.sync_charts); src=inspect.getsource(sync.sync_to_app); assert 'sync_charts()' in src and src.index('sync_charts()') > src.index('sync_vector_geojson()') and src.index('sync_charts()') < src.index('generate_landuse_legend()'), src; cs=inspect.getsource(sync.sync_charts); assert 'tag=\"chart\"' in cs and 'skipped - not yet built' in cs and 'll_boundaries.geojson' in cs; print('OK')" && mkdir -p ../data/_cache && python sync.py > ../data/_cache/sync09.log 2>&1 && test "$(grep -c '^\[chart\] skipped - not yet built: ' ../data/_cache/sync09.log)" -eq 25 && test "$(grep -c '^\[chart\] .* -> ' ../data/_cache/sync09.log)" -eq 0 && grep -q 'generated app/src/data/climate_legend.js' ../data/_cache/sync09.log && python -m pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `sync.sync_file` and `sync._sync_matched_pattern` both accept a keyword-only `tag` parameter defaulting to `"sync"`
    - `sync.sync_charts` exists and is called in `sync_to_app()` after `sync_vector_geojson()` and before `generate_landuse_legend()`
    - `sync_charts()` reads slugs from `data/ll_boundaries.geojson` and contains no hardcoded slug literals and no import of `conftest`
    - `sync_charts()` calls `_sync_matched_pattern(..., tag="chart")` — the copy is not re-implemented, so the repo-root-escape guard at `sync.py:337-341` still runs for chart files
    - Running `python sync.py` with no chart files built prints exactly 25 lines matching `^\[chart\] skipped - not yet built: ` and 0 lines matching `^\[chart\] .* -> `
    - Each of those 25 lines names one specific file path containing both a layer id and a Living Lab slug — no aggregate "no files matched" line stands in for them
    - `python sync.py` exits 0 and still prints `generated app/src/data/climate_legend.js`
    - `git status --porcelain app/src/data/` is empty after the sync run (no generated JS file changed content)
    - `python -m pytest data-pipeline/tests/` still passes 20/20
  </acceptance_criteria>
  <done>sync.py publishes chart outputs under a [chart] tag, names each not-yet-built (layer, Living Lab) file individually, and leaves every pre-existing sync path byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo filesystem -> `app/public/` | `sync_charts()` resolves a glob from `sources.yaml` and copies matches into the published web root. A wildcard match is a wider surface than a literal path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | `sync_charts()` glob -> `app/public/data/charts/` copy | mitigate | Copying is delegated to `_sync_matched_pattern()`, which already skips any match whose resolved path escapes the repo root (`sync.py:337-341`, T-08-16). This plan explicitly forbids re-implementing the copy loop, so the guard cannot be bypassed. |
| T-09-04 | Tampering | `data/ll_boundaries.geojson` parsed by `sync_charts()` | accept | Committed, repo-owned, pipeline-generated file; `json.loads` performs no code execution. A missing/empty `ll_slug` set raises a `RuntimeError` rather than silently syncing nothing. |
| T-09-05 | Denial of service | `sync.py` run time | accept | Adds at most 25 `Path.exists()` checks and 25 file copies of small JSON files to an already-file-copy-bound script. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task in this plan or phase; `requirements.txt` unchanged. 09-RESEARCH.md's Package Legitimacy Audit records "Not applicable — zero new external packages". |
</threat_model>

<verification>
- `python sync.py` from `data-pipeline/` exits 0, prints 25 individually-named `[chart] skipped - not yet built:` lines, and changes no generated JS file
- `python -m pytest data-pipeline/tests/` passes 20/20 (this plan adds no tests)
- `git diff` on `sources.yaml` is insertions-only
</verification>

<success_criteria>
1. All five chart-bearing layers declare `chart.script` and `output.chart_pattern` at the exact locked values; `bfn-schutzgebiete` declares neither
2. All four climate variables carry a `label: {en, de}` block readable from Python without touching `app/src/i18n.js`
3. `sync_charts()` names every one of the 25 missing (layer, Living Lab) chart files individually, and routes its copying through `_sync_matched_pattern(..., tag="chart")`
4. Every pre-existing `sync.py` log line and generated artifact is unchanged
</success_criteria>

<output>
Create `.planning/phases/09-chart-data-contract/09-02-SUMMARY.md` when done
</output>
