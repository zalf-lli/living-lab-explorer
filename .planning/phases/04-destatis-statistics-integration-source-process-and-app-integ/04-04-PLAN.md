---
phase: 04-destatis-statistics-integration-source-process-and-app-integ
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - app/src/data/layers.js
  - app/src/i18n.js
autonomous: true
requirements: [P4-SCOPE-3, D-01, D-02, D-03, D-04, D-06, D-07, D-08]
must_haves:
  truths:
    - "The tab labeled 'Land Use' now reads 'Agriculture' (EN) / 'Landwirtschaft' (DE) (D-01)"
    - "The tab labeled 'Economic' now reads 'Socio-economic' (EN) / 'Sozioökonomie' (DE) (D-02)"
    - "A new Landscape tab exists and is available (not gated behind a coming-soon state) (D-03)"
    - "Climate and Socio-economic tabs are available (no longer gated behind a coming-soon state) (D-04)"
    - "17 new kpi.* i18n keys exist with copy sourced from the Destatis variable catalogue, ready for StatPanel to consume in Plan 04-05"
  artifacts:
    - path: "app/src/data/layers.js"
      provides: "landscape LAYERS entry; climate/economic available: true"
    - path: "app/src/i18n.js"
      provides: "Renamed layers.* labels, new layers.landscape key, 17 new kpi.* keys, new statPanel.* namespace"
  key_links:
    - from: "app/src/components/LayerTabs.jsx"
      to: "app/src/data/layers.js"
      via: "LAYERS.map((l) => t(`layers.${l.id}`))"
      pattern: "layers\\.landscape"
---

<objective>
Update the tab registry and i18n resources so the app's tab structure matches CONTEXT.md's D-01
through D-08 decisions: rename the Land Use and Economic tabs, add a KPI-only Landscape tab, flip
Climate/Socio-economic/Landscape to available, and add the 17 new per-tab KPI label keys (plus the new
`statPanel.*` copy namespace) that Plan 04-05's `StatPanel` component will consume. This plan is purely
additive/renaming — it does not delete `KPIStrip` or its 4 legacy `kpi.*` keys yet, so the app keeps
building and running after this plan lands.

Purpose: separates the "what the tabs are called and which ones are open" decision from the "how a
tab's KPI panel is built and wired" work in Plan 04-05, keeping each plan's file-conflict surface
small and the app buildable at every step.
Output: `app/src/data/layers.js` and `app/src/i18n.js` reflect the final D-01 through D-08 tab
structure and carry all label copy Plan 04-05 needs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-destatis-statistics-integration-source-process-and-app-integ/04-CONTEXT.md
@.planning/phases/04-destatis-statistics-integration-source-process-and-app-integ/04-UI-SPEC.md
@.planning/phases/04-destatis-statistics-integration-source-process-and-app-integ/04-PATTERNS.md
@data/destatis_variables_catalogue.csv
</context>

<interfaces>
Current `app/src/data/layers.js` `LAYERS` array (full file, 47 lines):
```javascript
export const LAYERS = [
  { id: 'landuse', type: 'raster', pmtilesUrl: 'data/pmtiles/landuse-croptypes.pmtiles', legend: LANDUSE_LEGEND, available: true },
  { id: 'climate', type: 'placeholder', pmtilesUrl: null, legend: null, available: false },
  { id: 'soil', type: 'vector', pmtilesUrl: null, geojsonPathPattern: 'data/geojson/buek250-{slug}.geojson', legend: SOIL_LEGEND, legendNoteKey: 'legend.soil.note', available: true },
  { id: 'economic', type: 'placeholder', pmtilesUrl: null, legend: null, available: false },
]
```

Current `app/src/i18n.js` `layers.*` namespace (EN lines 48-53, DE lines 211-216):
```javascript
layers: { landuse: 'Land Use', climate: 'Climate', soil: 'Soil', economic: 'Economic' },   // EN
layers: { landuse: 'Landnutzung', climate: 'Klima', soil: 'Boden', economic: 'Oekonomie' }, // DE
```

Current `app/src/i18n.js` `kpi.*` namespace (EN lines 42-47, DE lines 205-210) — leave these 4 keys
untouched in this plan (removed in Plan 04-05 alongside KPIStrip's deletion):
```javascript
kpi: { totalArea: 'Total area', activeFarms: 'Active farms', avgTemp: 'Avg. temp.', dominantSoil: 'Dominant soil' },
```

`data/destatis_variables_catalogue.csv` rows for the 17 curated keys (columns:
`group,variable_key,genesis_table,label_en,label_de,unit_en,unit_de,...`) — reuse `label_en`/`label_de`
verbatim as the new `kpi.*` values: `land_area_cropland_ha` ("Cropland area"/"Ackerland"),
`farms_count` ("Number of farms"/"Anzahl Betriebe"), `farm_avg_size_ha` ("Average farm size"/
"Durchschnittliche Betriebsgroesse"), `organic_pct` ("Share of organic farming"/"Anteil oekologischer
Landbau"), `n_surplus_kg_ha` ("Nitrogen surplus"/"Stickstoffueberschuss"), `p_surplus_kg_ha`
("Phosphorus surplus"/"Phosphorueberschuss"), `groundwater_nitrate_mg_l` ("Nitrate in groundwater"/
"Nitrat im Grundwasser"), `agr_ch4_kt` ("Agricultural CH4 emissions"/"CH4-Emissionen Landwirtschaft"),
`agr_n2o_kt` ("Agricultural N2O emissions"/"N2O-Emissionen Landwirtschaft"), `forest_area_ha`
("Forest area"/"Waldflaeche"), `natura2000_ha` ("Natura 2000 area"/"Natura-2000-Flaeche"),
`nature_reserves_ha` ("Nature reserves area"/"Naturschutzgebietsflaeche"), `sealed_surface_pct`
("Share of sealed surface"/"Anteil versiegelter Flaeche"), `population_total` ("Total population"/
"Bevoelkerung insgesamt"), `gdp_per_capita_eur` ("GDP per capita"/"Bruttoinlandsprodukt je Einwohner"),
`unemployment_rate_pct` ("Unemployment rate"/"Arbeitslosenquote"), `household_income_eur`
("Disposable household income per capita"/"Verfuegbares Einkommen je Einwohner"). If Plan 04-02's
D-14 fallback substituted a different variable for any tab slot, use `data/destatis_curated_kpis.json`'s
actual `label_en`/`label_de` values for that slot instead of the list above (the manifest is the
final source of truth after any substitution).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the Landscape tab and flip availability per D-01 through D-04</name>
  <files>app/src/data/layers.js</files>
  <read_first>
    app/src/data/layers.js
  </read_first>
  <action>
    In `app/src/data/layers.js`, change the `climate` entry's `available: false` to `available: true`
    and the `economic` entry's `available: false` to `available: true` (D-04 — both flip once their
    KPI panel has real Destatis data, which Plan 04-03 already produced). Add a new entry to the
    `LAYERS` array, after the `economic` entry: `{ id: 'landscape', type: 'placeholder', pmtilesUrl: null, legend: null, available: true }`
    (D-03 — KPI-only, no map layer, same shape as `climate`/`economic`). Do not rename the `landuse`
    or `economic` internal `id` values — only their i18n display labels change (handled in Task 2),
    per CONTEXT.md's discretion note to minimize churn in `geojsonPathPattern`, `LAYER_COLORS`, and
    `pmtilesUrl` keys. Do not add a `landscape` key to `LAYER_COLORS` — its absence is intentional
    (KPI-only tabs render no map-legend color swatches, which is the correct behavior for a tab with
    no map layer).
  </action>
  <verify>
    <automated>cd app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "id: 'landscape'" app/src/data/layers.js` matches exactly once.
    - `grep -n "id: 'climate'" -A3 app/src/data/layers.js` shows `available: true`.
    - `grep -n "id: 'economic'" -A3 app/src/data/layers.js` shows `available: true`.
    - `cd app && npm run build` succeeds (no import/reference errors from the new LAYERS entry).
  </acceptance_criteria>
  <done>LAYERS contains 5 entries (landuse, climate, soil, economic, landscape); climate, economic, and landscape are all available: true.</done>
</task>

<task type="auto">
  <name>Task 2: Rename tab labels, add landscape label, add 17 kpi.* keys and statPanel.* namespace</name>
  <files>app/src/i18n.js</files>
  <read_first>
    app/src/i18n.js
    .planning/phases/04-destatis-statistics-integration-source-process-and-app-integ/04-UI-SPEC.md
  </read_first>
  <action>
    In `app/src/i18n.js`'s EN `layers` object (lines 48-53), change `landuse: 'Land Use'` to
    `landuse: 'Agriculture'` (D-01), change `economic: 'Economic'` to `economic: 'Socio-economic'`
    (D-02), add `landscape: 'Landscape'` (D-03); leave `climate`/`soil` unchanged. Mirror in the DE
    `layers` object (lines 211-216): `landuse: 'Landwirtschaft'`, `economic: 'Sozioökonomie'`, add
    `landscape: 'Landschaft'`. Add 17 new keys to the existing EN `kpi` object (lines 42-47, additive —
    do not remove `totalArea`/`activeFarms`/`avgTemp`/`dominantSoil` yet) using the `label_en` values
    listed in this plan's `<interfaces>` block, keyed by each curated `variable_key` exactly (e.g.
    `landAreaCroplandHa: 'Cropland area'` is WRONG — use the literal snake_case variable_key as the
    object key, e.g. `land_area_cropland_ha: 'Cropland area'`, to match `generate_metadata.py`'s
    `kpiByTab[...].key` values 1:1 per the project's existing `t(\`kpi.${key}\`)` convention). Mirror
    all 17 with `label_de` values into the DE `kpi` object (lines 205-210). Add a new top-level
    `statPanel` namespace to both EN and DE `translation` objects (place it after the `kpi` namespace):
    EN — `statPanel: { pendingReviewTitle: 'Pending review', pendingReviewBody: 'Some indicators for this Living Lab are still pending expert review and data verification.', source: 'Source: Destatis GENESIS-Online, table {{tableId}}, retrieved {{date}}', viewSource: 'View source', errorTitle: 'Statistics are temporarily unavailable.', errorBody: 'Reload the page — if this keeps happening, the data source may be temporarily unreachable.' }`;
    DE — `statPanel: { pendingReviewTitle: 'In Prüfung', pendingReviewBody: 'Einige Indikatoren für dieses Living Lab befinden sich noch in der fachlichen Prüfung.', source: 'Quelle: Destatis GENESIS-Online, Tabelle {{tableId}}, abgerufen am {{date}}', viewSource: 'Quelle ansehen', errorTitle: 'Statistiken sind vorübergehend nicht verfügbar.', errorBody: 'Seite neu laden — falls das Problem bestehen bleibt, ist die Datenquelle möglicherweise vorübergehend nicht erreichbar.' }`.
  </action>
  <verify>
    <automated>cd app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "landuse: 'Agriculture'" app/src/i18n.js` matches (EN).
    - `grep -n "economic: 'Socio-economic'" app/src/i18n.js` matches (EN).
    - `grep -n "landscape: 'Landscape'" app/src/i18n.js` and `grep -n "landscape: 'Landschaft'" app/src/i18n.js` each match once.
    - `grep -c "land_area_cropland_ha:\|farms_count:\|farm_avg_size_ha:\|organic_pct:\|n_surplus_kg_ha:\|p_surplus_kg_ha:\|groundwater_nitrate_mg_l:\|agr_ch4_kt:\|agr_n2o_kt:\|forest_area_ha:\|natura2000_ha:\|nature_reserves_ha:\|sealed_surface_pct:\|population_total:\|gdp_per_capita_eur:\|unemployment_rate_pct:\|household_income_eur:" app/src/i18n.js` reports at least 34 (17 keys x 2 languages).
    - `grep -n "statPanel:" app/src/i18n.js | wc -l` reports 2 (EN + DE).
    - `cd app && npm run build` succeeds.
  </acceptance_criteria>
  <done>layers.* labels are renamed/added per D-01/D-02/D-03; 17 new kpi.* keys and a new statPanel.* namespace exist in both languages, ready for Plan 04-05's StatPanel component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static config -> rendered UI | `layers.js`/`i18n.js` are static, build-time, developer-controlled config — no runtime/user input reaches these files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | N/A — static config file, no injection surface | accept | No dynamic/user-supplied content is interpolated into these files; both are committed source |
</threat_model>

<verification>
`cd app && npm run build` succeeds. `cd app && npm run lint` passes with no new warnings on the two
modified files.
</verification>

<success_criteria>
1. Tab labels read "Agriculture" and "Socio-economic" in EN (and their DE equivalents).
2. A Landscape tab exists, is available, and has no map layer.
3. Climate, Socio-economic, and Landscape tabs are all `available: true`.
4. 17 new `kpi.*` i18n keys and a new `statPanel.*` namespace exist in both languages, keyed to match `generate_metadata.py`'s field names exactly.
</success_criteria>

<output>
Create `.planning/phases/04-destatis-statistics-integration-source-process-and-app-integ/04-04-SUMMARY.md` when done
</output>
