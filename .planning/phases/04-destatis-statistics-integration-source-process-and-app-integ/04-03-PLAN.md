---
phase: 04-destatis-statistics-integration-source-process-and-app-integ
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - data-pipeline/python/generate_metadata.py
  - data/ll_content.json
  - data-pipeline/tests/test_pipeline_outputs.py
autonomous: true
requirements: [P4-SCOPE-2, P4-SCOPE-3, D-09, D-10, D-11, D-12]
must_haves:
  truths:
    - "app/public/data/ll_metadata.json contains real Destatis-sourced KPI values grouped per tab, for every Living Lab (D-09 — the 17 curated KPI fields)"
    - "No legacy '-' placeholder strings from ll_content.json's old kpi/production/socio blocks leak into or shadow the new computed Destatis values (D-10 — every existing value in those blocks is confirmed placeholder-only and safe to overwrite)"
    - "The one-time restructure of data/ll_content.json is a direct human/executor edit, never a pipeline script write (D-11)"
    - "D-12: generate_metadata.py's _deep_merge authored-wins policy is left unchanged — rewriting ll_content.json directly removes the stale placeholders the merge would otherwise fight with, so no merge-logic change is needed"
  artifacts:
    - path: "data-pipeline/python/generate_metadata.py"
      provides: "kpiByTab computed field sourced from data/destatis_ll.json + data/destatis_curated_kpis.json + data/destatis_meta.json; sort_keys=True fix"
    - path: "data/ll_content.json"
      provides: "Restructured per-LL blocks with legacy kpi/production/socio sub-blocks removed"
  key_links:
    - from: "generate_metadata.py:_build_computed_record"
      to: "data/destatis_ll.json"
      via: "_build_kpi_by_tab() lookup by variable_key"
      pattern: "kpiByTab"
    - from: "data/ll_metadata.json"
      to: "app/public/data/ll_metadata.json"
      via: "sync.py's sync_to_app() write_metadata() call"
      pattern: "write_metadata"
---

<objective>
Extend `generate_metadata.py`'s computed/authored merge so every Living Lab's `ll_metadata.json`
record carries a `kpiByTab` field built from the real Destatis data committed in Plan 04-02, and
resolve Pitfall 3 (04-RESEARCH.md) by directly rewriting `data/ll_content.json` to strip its legacy
placeholder `kpi`/`production`/`socio` blocks (D-11) so the authored-wins merge (D-12, unchanged) no
longer has anything to silently override the new computed values with.

Purpose: this is the pipeline-to-app handoff point. Without this plan, real Destatis numbers exist on
disk but never reach `ll_metadata.json`, and the existing `"-"` placeholders would keep winning the
merge even if they did.
Output: `data/ll_metadata.json` / `app/public/data/ll_metadata.json` contain a `kpiByTab` object per
LL with real values, units, and GENESIS table provenance; `data/ll_content.json` no longer contains any
of the placeholder fields flagged as safe-to-overwrite in CONTEXT.md D-10.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-destatis-statistics-integration-source-process-and-app-integ/04-CONTEXT.md
@.planning/phases/04-destatis-statistics-integration-source-process-and-app-integ/04-RESEARCH.md
@.planning/phases/04-destatis-statistics-integration-source-process-and-app-integ/04-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Current `generate_metadata.py` (full file, 56 lines):
```
CONTENT_FILE = DATA / "ll_content.json"
METADATA_FILE = DATA / "ll_metadata.json"

def load_ll_content(path=CONTENT_FILE) -> dict: ...
def _deep_merge(computed, authored): ...          # authored wins on key conflict — DO NOT CHANGE (D-12)
def _build_computed_record(slug, authored) -> dict:
    return {"slug": slug, "contact": authored.get("contact", ""), "nuts3": authored.get("nuts3", []), "mock": authored.get("mock", False)}
def build_metadata(ll_content=None) -> dict:
    content = load_ll_content() if ll_content is None else ll_content
    metadata = {}
    for slug, authored in content.items():
        metadata[slug] = _deep_merge(_build_computed_record(slug, authored), authored)
    return metadata
def write_metadata(output_path=METADATA_FILE, ll_content=None) -> dict:
    metadata = build_metadata(ll_content)
    output_path.write_text(json.dumps(metadata, ensure_ascii=False, indent=2), encoding="utf-8")  # MISSING sort_keys=True
    return metadata
```

`data/destatis_ll.json` (from Plan 04-02): `{slug: {variable_key: number|null, ...}, ...}` — the 17
curated field names (post-rename) are direct keys inside each per-slug dict.

`data/destatis_curated_kpis.json` (from Plan 04-02): list of 17
`{tab, variable_key, genesis_table, label_en, label_de, unit_en, unit_de}` entries — `tab` is one of
`landuse`, `soil`, `climate`, `landscape`, `economic`.

`data/destatis_meta.json` (from Plan 04-02): `{"fetched_at": "YYYY-MM-DD"}`.

`data/ll_content.json`'s current per-LL shape (repeated identically for all 5 slugs) — the blocks to
remove: the top-level `"kpi": {"area": ..., "farms": ..., "tempRange": ..., "precip": ..., "soil": ...}`
key, and, inside both `"en"` and `"de"` objects, the `"production": {...}` and `"socio": {...}`
sub-blocks (10 and 5 fields respectively, all currently `"-"` placeholder strings per D-10). Every
other key (`slug`, `contact`, `nuts3`, `mock`, `num`, `order`, `region`, `color`, `colorDark`,
`outlineColor`, `icon`, and inside `en`/`de`: `name`, `tagline`, `soil_climate`, `description`,
`delineation`) stays untouched.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend generate_metadata.py with a kpiByTab computed field</name>
  <files>data-pipeline/python/generate_metadata.py</files>
  <read_first>
    data-pipeline/python/generate_metadata.py
    data/destatis_ll.json
    data/destatis_curated_kpis.json
    data/destatis_meta.json
  </read_first>
  <action>
    In `data-pipeline/python/generate_metadata.py`, add three new module-level path constants
    alongside `CONTENT_FILE`/`METADATA_FILE`: `DESTATIS_LL_FILE = DATA / "destatis_ll.json"`,
    `CURATED_KPIS_FILE = DATA / "destatis_curated_kpis.json"`, `DESTATIS_META_FILE = DATA / "destatis_meta.json"`.
    Add a `_load_json_or_empty(path: Path) -> dict | list`helper returning `{}` (or `[]` for the
    curated-kpis file) if the path does not exist, otherwise `json.loads(path.read_text(encoding="utf-8"))`
    — mirrors the existing `authored.get(key, default)` defensive-lookup idiom already used in
    `_build_computed_record`. Add `_build_kpi_by_tab(slug: str, destatis_ll: dict, curated_kpis: list) -> dict`:
    group `curated_kpis` entries by their `tab` key into a dict of lists; for each entry, build
    `{"key": entry["variable_key"], "value": destatis_ll.get(slug, {}).get(entry["variable_key"]),
    "unit": {"en": entry["unit_en"], "de": entry["unit_de"]}, "genesisTable": entry["genesis_table"]}`;
    return the resulting `{tab_id: [field_dict, ...]}` dict (expected tabs:
    `landuse`, `soil`, `climate`, `landscape`, `economic`). Change `_build_computed_record`'s signature
    to `_build_computed_record(slug: str, authored: dict, destatis_ll: dict, curated_kpis: list,
    destatis_meta: dict) -> dict`, and add two keys to its returned dict:
    `"kpiByTab": _build_kpi_by_tab(slug, destatis_ll, curated_kpis)` and
    `"destatisRetrievedAt": destatis_meta.get("fetched_at")`. Update `build_metadata()` to load
    `destatis_ll = _load_json_or_empty(DESTATIS_LL_FILE)`, `curated_kpis = _load_json_or_empty(CURATED_KPIS_FILE) or []`,
    `destatis_meta = _load_json_or_empty(DESTATIS_META_FILE)` once at the top (not per-slug), and pass
    them into every `_build_computed_record(slug, authored, destatis_ll, curated_kpis, destatis_meta)`
    call. Finally, fix `write_metadata()`'s `json.dumps(metadata, ensure_ascii=False, indent=2)` call
    to add `sort_keys=True` (CLAUDE.md compliance gap flagged in 04-PATTERNS.md).
  </action>
  <verify>
    <automated>cd data-pipeline && python -c "import sys; sys.path.insert(0,'python'); from generate_metadata import build_metadata; m = build_metadata(); rec = next(iter(m.values())); assert 'kpiByTab' in rec"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sort_keys=True" data-pipeline/python/generate_metadata.py` matches at least once (inside `write_metadata`).
    - `grep -n "kpiByTab" data-pipeline/python/generate_metadata.py` matches at least twice (definition + usage in `_build_computed_record`).
    - `grep -n "destatisRetrievedAt" data-pipeline/python/generate_metadata.py` matches.
    - Running `cd data-pipeline && python -c "import sys; sys.path.insert(0,'python'); from generate_metadata import build_metadata; m = build_metadata(); rec = next(iter(m.values())); assert 'kpiByTab' in rec; assert set(rec['kpiByTab']) <= {'landuse','soil','climate','landscape','economic'}"` exits 0.
  </acceptance_criteria>
  <done>build_metadata() produces a kpiByTab field per LL sourced from real Destatis data, and write_metadata() writes with sort_keys=True.</done>
</task>

<task type="auto">
  <name>Task 2: One-time direct edit — strip legacy placeholder blocks from ll_content.json</name>
  <files>data/ll_content.json</files>
  <read_first>
    data/ll_content.json
    .planning/phases/04-destatis-statistics-integration-source-process-and-app-integ/04-CONTEXT.md
  </read_first>
  <action>
    This is a direct hand-edit of `data/ll_content.json` performed by you, the executor — NOT a
    pipeline script write, per D-11 and CLAUDE.md's "never write ll_content.json from a pipeline
    script" rule. For each of the 5 top-level LL blocks (`east-brandenburg`, `havellandisches-luch`,
    `north-hessian-loess`, `hessian-low-mountain`, `rheingau`), remove the top-level `"kpi"` key
    entirely (the object containing `area`/`farms`/`tempRange`/`precip`/`soil`), and remove the
    `"production"` and `"socio"` sub-objects from inside both the `"en"` and `"de"` objects. Every
    LL's `production`/`socio` values are currently the literal placeholder string `"-"` per D-10 —
    confirmed safe to delete entirely, no value needs preserving. Do not touch any other key in the
    file (`slug`, `contact`, `nuts3`, `mock`, `num`, `order`, `region`, `color`, `colorDark`,
    `outlineColor`, `icon`, `en.name`, `en.tagline`, `en.soil_climate`, `en.description`,
    `en.delineation`, and the `de` equivalents all stay exactly as they are).
  </action>
  <verify>
    <automated>python -c "import json; d = json.load(open('data/ll_content.json', encoding='utf-8')); assert all('kpi' not in v and 'production' not in v.get('en', {}) for v in d.values())"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json; d = json.load(open('data/ll_content.json', encoding='utf-8')); assert all('kpi' not in v for v in d.values())"` exits 0.
    - `python -c "import json; d = json.load(open('data/ll_content.json', encoding='utf-8')); assert all('production' not in v['en'] and 'socio' not in v['en'] and 'production' not in v['de'] and 'socio' not in v['de'] for v in d.values())"` exits 0.
    - `python -c "import json; d = json.load(open('data/ll_content.json', encoding='utf-8')); assert all('name' in v['en'] and 'tagline' in v['en'] and 'slug' in v and 'nuts3' in v for v in d.values())"` exits 0 (confirms narrative/identity fields are untouched).
  </acceptance_criteria>
  <done>data/ll_content.json's 5 LL blocks no longer contain the legacy kpi/production/socio placeholder blocks; all other fields are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate ll_metadata.json and assert the kpiByTab contract</name>
  <files>data-pipeline/tests/test_pipeline_outputs.py</files>
  <read_first>
    data-pipeline/tests/test_pipeline_outputs.py
    data-pipeline/python/generate_metadata.py
  </read_first>
  <action>
    Run `cd data-pipeline && python sync.py` from repo root to regenerate both `data/ll_metadata.json`
    and `app/public/data/ll_metadata.json` with the new `kpiByTab`/`destatisRetrievedAt` fields (using
    the Task 1 code and Task 2 content). Then add a new test function
    `test_ll_metadata_kpi_by_tab_contract()` to `data-pipeline/tests/test_pipeline_outputs.py`: load
    `app/public/data/ll_metadata.json`, assert it is non-empty, and for every LL record assert
    `"kpiByTab" in record`, `set(record["kpiByTab"]) <= {"landuse", "soil", "climate", "landscape", "economic"}`,
    `len(record["kpiByTab"].get("landuse", [])) == 4`, `len(record["kpiByTab"].get("soil", [])) == 3`,
    `len(record["kpiByTab"].get("climate", [])) == 2`, `len(record["kpiByTab"].get("landscape", [])) == 4`,
    `len(record["kpiByTab"].get("economic", [])) == 4`, and `"destatisRetrievedAt" in record` with a
    non-empty string value. Also assert no record contains a literal `"-"` string value anywhere
    inside its `kpiByTab` field values (proves Pitfall 3 is resolved — real values or `None`, never
    the old placeholder sentinel).
  </action>
  <verify>
    <automated>cd data-pipeline && python -m pytest tests/test_pipeline_outputs.py -k kpi_by_tab -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd data-pipeline && python -m pytest tests/test_pipeline_outputs.py -k kpi_by_tab -v` passes.
  </acceptance_criteria>
  <done>app/public/data/ll_metadata.json contains the kpiByTab contract for every LL, verified by an automated pytest assertion, with no leftover "-" placeholder leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline -> `data/ll_content.json` | Human-authored file, read-only from all pipeline scripts (CLAUDE.md rule); this plan's Task 2 edit is a direct executor edit, not a script write |
| pipeline -> app static assets | `ll_metadata.json` is the only channel Destatis data reaches the browser through; no direct API access from the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | `_deep_merge` authored-wins semantics silently reintroducing stale placeholders | mitigate | D-11's one-time strip (Task 2) removes the only fields that could conflict; `_deep_merge` itself (D-12) is intentionally left unchanged |
| T-04-09 | Repudiation | No record of which GENESIS table backs a displayed value | mitigate | `kpiByTab` entries carry `genesisTable` per field, sourced from the verified `destatis_curated_kpis.json` manifest, preserving provenance end-to-end |
</threat_model>

<verification>
`cd data-pipeline && python -m pytest tests/test_pipeline_outputs.py -k "kpi_by_tab or destatis" -v` passes.
</verification>

<success_criteria>
1. `app/public/data/ll_metadata.json` contains a `kpiByTab` object per LL with the correct per-tab field counts (4/3/2/4/4).
2. No legacy `"-"` placeholder strings remain in `data/ll_content.json` or leak into `kpiByTab`.
3. `data/ll_content.json` was edited directly by the executor, never by a pipeline script.
</success_criteria>

<output>
Create `.planning/phases/04-destatis-statistics-integration-source-process-and-app-integ/04-03-SUMMARY.md` when done
</output>
