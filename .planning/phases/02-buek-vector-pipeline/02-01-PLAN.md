---
phase: 02-buek-vector-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - data-pipeline/sources/sources.yaml
  - data-pipeline/python/build_vector.py
  - data-pipeline/requirements.txt
  - data-pipeline/tests/conftest.py
  - data-pipeline/tests/test_pipeline_outputs.py
  - data/geojson/buek250-east-brandenburg.geojson
  - data/geojson/buek250-havelland.geojson
  - data/geojson/buek250-north-hessian-loess.geojson
  - data/geojson/buek250-hessian-low-mountain.geojson
  - data/geojson/buek250-rheingau.geojson
autonomous: true
requirements:
  - PIPELINE-01
  - PIPELINE-02
  - PIPELINE-03
user_setup:
  - Install or allow installation of `pytest` into `data-pipeline/.venv` before running the verification step if it is still missing.
must_haves:
  truths:
    - The vector layer is declared once in `data-pipeline/sources/sources.yaml` with `kind: vector`, BUEK250 input metadata, simplification settings, and a per-LL GeoJSON output pattern.
    - Running `python data-pipeline/python/build_vector.py --layer buek250` creates five non-empty GeoJSON files in `data/geojson/`, all written in `EPSG:4326`, and the script fails loudly if any LL clip returns zero features.
    - `python -m pytest data-pipeline/tests/` passes from a clean checkout by validating committed PMTiles and GeoJSON outputs without rebuilding them.
  artifacts:
    - path: data-pipeline/python/build_vector.py
      provides: Canonical vector build entrypoint for `kind: vector` layers declared in `sources.yaml`
      exports: ["parse_args", "list_layers", "build_layer", "main"]
    - path: data-pipeline/sources/sources.yaml
      provides: Source-of-truth declaration for the `buek250` vector layer and its output contract
      contains: "kind: vector"
    - path: data-pipeline/tests/test_pipeline_outputs.py
      provides: Smoke coverage for committed raster and vector pipeline outputs
      contains: buek250
    - path: data/geojson/buek250-east-brandenburg.geojson
      provides: Representative committed fixture proving the vector build output contract
      contains: FeatureCollection
  key_links:
    - from: data-pipeline/python/build_vector.py
      to: data-pipeline/sources/sources.yaml
      via: layer lookup and vector config parsing
      pattern: buek250
    - from: data-pipeline/python/build_vector.py
      to: data/ll_boundaries.geojson
      via: per-LL clipping masks keyed by `ll_slug`
      pattern: ll_slug
    - from: data-pipeline/tests/test_pipeline_outputs.py
      to: data/geojson/buek250-{slug}.geojson
      via: committed fixture assertions for existence, CRS, and feature count
      pattern: buek250-.+\.geojson
---

<objective>
Create the phase 2 vector pipeline and fixture-backed smoke test contract.

Purpose: Add a reproducible BUEK250 GeoJSON build path beside the existing raster PMTiles path, while keeping the first validation loop cheap enough to run without a full rebuild.
Output: A new `build_vector.py`, a `buek250` entry in `sources.yaml`, five committed per-LL GeoJSON fixtures, and `pytest` smoke tests covering both raster and vector outputs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-buek-vector-pipeline/02-CONTEXT.md
@.planning/phases/02-buek-vector-pipeline/02-DISCUSSION-LOG.md
@.planning/phases/02-buek-vector-pipeline/02-RESEARCH.md
@data-pipeline/python/build_pmtiles.py
@data-pipeline/python/_sources.py
@data-pipeline/sources/sources.yaml
@data-pipeline/requirements.txt
@data/ll_boundaries.geojson
</context>

<notes>
- Treat the phase context and research as authoritative over stale roadmap wording: this plan targets the BUEK250 GeoPackage at `data/buek250_mgm_utm_v60/buek250_mgm_utm_v60.gpkg`, not the older BUEK1000/Shapefile wording still visible in some top-level docs.
- Use the verified GeoPackage layer name `buek250_mgm_utm_v60_poly`.
- Keep `sync.py` and frontend integration out of scope for this plan.
- Prefer `simplify_tolerance: 0.0005` and `coordinate_precision: 0.0001` unless execution discovers an output exceeding the accepted per-LL size envelope.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Declare the BUEK250 vector layer contract in `sources.yaml`</name>
  <files>data-pipeline/sources/sources.yaml</files>
  <read_first>.planning/phases/02-buek-vector-pipeline/02-CONTEXT.md, .planning/phases/02-buek-vector-pipeline/02-RESEARCH.md, data-pipeline/sources/sources.yaml, data-pipeline/python/_sources.py</read_first>
  <action>Add a new `layers` entry with `id: buek250` and `kind: vector`. Declare the BUEK250 GeoPackage input path and source CRS, the `python/build_vector.py` build script, the vector-specific config (`gpkg_layer`, `keep_fields`, `simplify_tolerance`, `coordinate_precision`, `output_dir`), and an output contract that tells downstream code and tests how to locate each per-LL GeoJSON fixture. Keep it aligned with the existing `sources.yaml` style and do not add `sync_to` yet.</action>
  <verify>rg -n "id: buek250|kind: vector|geojson_pattern|simplify_tolerance|coordinate_precision" data-pipeline/sources/sources.yaml</verify>
  <acceptance_criteria>
    - `sources.yaml` contains exactly one `buek250` layer entry
    - The entry declares the BUEK250 GeoPackage path and `EPSG:25832`
    - The entry declares `kind: vector`, `python/build_vector.py`, and a per-LL output pattern under `data/geojson/`
    - The vector config keeps only `SYM_NR`, `GEN_ID`, and `BEMERKUNG`
  </acceptance_criteria>
  <done>The repo has a stable declarative contract for the new vector layer.</done>
</task>

<task type="auto">
  <name>Task 2: Implement `build_vector.py` using the established pipeline script pattern</name>
  <files>data-pipeline/python/build_vector.py</files>
  <read_first>data-pipeline/python/build_pmtiles.py, data-pipeline/python/_sources.py, .planning/phases/02-buek-vector-pipeline/02-RESEARCH.md, data/ll_boundaries.geojson</read_first>
  <action>Create `data-pipeline/python/build_vector.py` mirroring the CLI and logging structure of `build_pmtiles.py`: `--list`, `--layer`, `list_layers()`, `build_layer()`, and `main()`. Load the `buek250` config from `sources.yaml`, read the GeoPackage with `layer='buek250_mgm_utm_v60_poly'`, call `make_valid()` immediately after load, retain only `SYM_NR`, `GEN_ID`, `BEMERKUNG`, reproject LL boundary masks into the source CRS before clipping, abort with a clear error if any clipped GeoDataFrame is empty, then reproject to `EPSG:4326`, simplify, precision-round, and write one GeoJSON per LL to `data/geojson/`. Log successful outputs with feature count and file size so the build result is inspectable from the terminal.</action>
  <verify>python data-pipeline/python/build_vector.py --list</verify>
  <acceptance_criteria>
    - `build_vector.py` exposes the same high-level CLI shape as `build_pmtiles.py`
    - The script only lists `kind: vector` layers when invoked with `--list`
    - The clip loop is keyed by `ll_slug` from `data/ll_boundaries.geojson`
    - The script raises a clear error when CRS alignment or clipping would otherwise yield zero features
    - Each successful build log line includes the output path, feature count, and output size
  </acceptance_criteria>
  <done>The repo can build BUEK250 GeoJSON outputs through a first-class pipeline entrypoint.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the per-LL GeoJSON fixtures produced by the vector build</name>
  <files>data/geojson/buek250-east-brandenburg.geojson, data/geojson/buek250-havelland.geojson, data/geojson/buek250-north-hessian-loess.geojson, data/geojson/buek250-hessian-low-mountain.geojson, data/geojson/buek250-rheingau.geojson</files>
  <read_first>data-pipeline/python/build_vector.py, .planning/phases/02-buek-vector-pipeline/02-RESEARCH.md, data/ll_boundaries.geojson</read_first>
  <action>Run the new vector build once for `buek250`, create `data/geojson/` if needed, and commit the five resulting GeoJSON files as fixtures. Preserve deterministic output characteristics as much as practical: stable field selection, stable LL-derived filenames, and no extra properties beyond the approved minimal set. If `GEN_ID` requires normalization for cleaner JSON values, do it in the build script rather than hand-editing fixtures.</action>
  <verify>Get-ChildItem data/geojson/buek250-*.geojson</verify>
  <acceptance_criteria>
    - Exactly five `buek250-*.geojson` files exist under `data/geojson/`
    - Each filename matches an `ll_slug` from `data/ll_boundaries.geojson`
    - Fixtures are generated by the script, not manually assembled
    - No fixture is empty or missing the GeoJSON `FeatureCollection` structure
  </acceptance_criteria>
  <done>The repo contains committed vector fixtures suitable for cheap smoke testing.</done>
</task>

<task type="auto">
  <name>Task 4: Add fixture-backed smoke tests for vector and raster outputs</name>
  <files>data-pipeline/requirements.txt, data-pipeline/tests/conftest.py, data-pipeline/tests/test_pipeline_outputs.py</files>
  <read_first>.planning/phases/02-buek-vector-pipeline/02-RESEARCH.md, data-pipeline/requirements.txt, data-pipeline/sources/sources.yaml, app/public/data/pmtiles/landuse-croptypes.pmtiles</read_first>
  <action>Create `data-pipeline/tests/` and add a lightweight pytest setup that does not rebuild artifacts. Add `pytest` to `data-pipeline/requirements.txt`. Write smoke tests that assert the committed PMTiles file exists and is non-zero, and that each committed BUEK250 GeoJSON exists at the declared path, opens successfully with GeoPandas, reports `EPSG:4326`, and contains at least one feature. Keep the tests intentionally shallow so they run quickly from a clean checkout and validate the output contract rather than re-exercising the full build.</action>
  <verify>python -m pytest data-pipeline/tests/</verify>
  <acceptance_criteria>
    - `data-pipeline/tests/` exists with `conftest.py` and at least one test module
    - `requirements.txt` includes pytest
    - Tests validate both the existing PMTiles artifact and the new GeoJSON fixtures
    - Tests pass without invoking `build_pmtiles.py`, `build_vector.py`, or `sync.py`
  </acceptance_criteria>
  <done>The repo has a cheap regression net for committed raster and vector outputs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python data-pipeline/python/build_vector.py --layer buek250` succeeds from the repo root
- [ ] The build writes five `data/geojson/buek250-{slug}.geojson` files in `EPSG:4326`
- [ ] At least one build log line reports output file size in addition to feature count
- [ ] `python -m pytest data-pipeline/tests/` succeeds without rebuilding any artifacts
- [ ] `rg -n "id: buek250|kind: vector" data-pipeline/sources/sources.yaml` shows the new declarative layer entry
</verification>

<success_criteria>

- All tasks completed
- `PIPELINE-01`, `PIPELINE-02`, and `PIPELINE-03` are satisfied by committed code and fixtures
- The repo has a first-class vector build path parallel to the raster PMTiles path
- Output verification is cheap enough to run in CI or local clean-state checks without a full rebuild
  </success_criteria>

<output>
After completion, create `.planning/phases/02-buek-vector-pipeline/02-01-SUMMARY.md`
</output>
