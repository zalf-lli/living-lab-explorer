# Wave 1 Summary: Catalogue Format Scaffold

## Completed

- Added `data-pipeline/python/export_source_catalogue.py`, an offline stdlib-only CSV generator.
- Generated `data/source_catalogue.csv` with the locked 21-column source catalogue schema.
- Added 19 seed rows covering the locked candidate sources plus already-integrated DESTATIS GENESIS, DLR croptypes, and BUEK250 soil rows.
- Replaced the legacy `spatial_data` workbook stub with a frozen-header `source_catalogue` tab in `data/variables_catalogue.xlsx`.

## Verification

- `python data-pipeline/python/export_source_catalogue.py`
- CSV schema check: 21 columns, `citation_url` at index 12, `(AI) ll_relevance` at index 13, `reviewer_comments` last.
- Workbook check: `source_catalogue` exists, `spatial_data` is removed, `destatis_variables` is preserved.

## Notes

- `export_source_catalogue.py` never reads or writes `data/variables_catalogue.xlsx`.
- Already-integrated rows are pre-marked with `decision=include`; candidate rows remain undecided for review.
