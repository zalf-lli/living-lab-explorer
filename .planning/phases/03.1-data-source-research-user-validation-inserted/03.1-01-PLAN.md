---
phase: 03.1-data-source-research-user-validation-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data-pipeline/python/export_source_catalogue.py
  - data/source_catalogue.csv
  - data/variables_catalogue.xlsx
autonomous: true
requirements: [TBD]
user_setup: []

must_haves:
  truths:
    - "A reviewable spreadsheet catalogue exists, extending the variables_catalogue.xlsx pattern with one row per source/layer/variable [D-01, D-04]"
    - "A committed, git-diffable CSV mirror of the catalogue exists in data/ [D-02]"
    - "Verified source-fact columns are visually separated from advisory AI-assessment columns via the (AI) header prefix [D-03]"
    - "Already-integrated sources (DESTATIS GENESIS, DLR croptypes, BUeK250 soil) appear as rows flagged already_integrated and pre-decided include [D-09]"
  artifacts:
    - path: "data-pipeline/python/export_source_catalogue.py"
      provides: "Offline CSV-mirror generator modelled on export_variables.py; no network calls; never writes the xlsx"
      contains: "source_catalogue"
      min_lines: 40
    - path: "data/source_catalogue.csv"
      provides: "21-column git-diffable catalogue mirror with header row and seed rows"
      contains: "citation_url"
    - path: "data/variables_catalogue.xlsx"
      provides: "source_catalogue review tab with the full 21-column schema replacing the spatial_data stub"
  key_links:
    - from: "data-pipeline/python/export_source_catalogue.py"
      to: "data/source_catalogue.csv"
      via: "csv.DictWriter writeheader + writerow"
      pattern: "source_catalogue\\.csv"
    - from: "data/source_catalogue.csv header"
      to: "data/variables_catalogue.xlsx source_catalogue tab header"
      via: "identical 21-column order"
      pattern: "\\(AI\\) ll_relevance"
---

<objective>
Define the review-catalogue FORMAT for Phase 3.1 and scaffold it with seed rows.

Purpose: Establish the single shared schema (21 columns: identity, verified facts, advisory AI assessment, human decision) that Wave 2 fills with research and Wave 3 reviews. Without a locked format first, Wave 2 research has no target and the fact/assessment evidence-chain separation (D-03) cannot be enforced.

Output:
- `data-pipeline/python/export_source_catalogue.py` — offline CSV mirror generator (analog of `export_variables.py`).
- `data/source_catalogue.csv` — committed git-diffable mirror with header + seed rows (the locked candidate list from D-07 as empty-fact stub rows, plus the already-integrated rows from D-09 fully filled).
- `data/variables_catalogue.xlsx` — `source_catalogue` review tab carrying the same 21-column schema, replacing the 3-column `spatial_data` stub.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/03.1-data-source-research-user-validation-inserted/03.1-CONTEXT.md
@.planning/phases/03.1-data-source-research-user-validation-inserted/03.1-RESEARCH.md
@.planning/phases/03.1-data-source-research-user-validation-inserted/03.1-PATTERNS.md

<interfaces>
<!-- The locked 21-column schema. Header strings are EXACT (the (AI) prefix is the D-03 separator). -->
<!-- Column order is fixed; Wave 2 and Wave 3 depend on this order. -->

Group A — Identity (cols 1-5, filled by script/AI):
  source_id, source_group, source_name, layer_or_variable, already_integrated

Group B — Verified source-stated facts (cols 6-13, every populated row needs a non-empty citation_url) [D-03]:
  spatial_coverage, temporal_coverage, access_method, access_url, format, license, update_cadence, citation_url

Group C — AI assessment (cols 14-17, advisory only, "(AI)" header prefix is the D-03 visual separator) [D-03]:
  (AI) ll_relevance, (AI) pipeline_path, (AI) integration_complexity, (AI) integration_notes

Group D — Human review decision (cols 18-21, blank in script output except already_integrated rows) [D-06]:
  decision, priority, rationale, reviewer_comments

access_method enum vocabulary (Group B): WFS / WCS / OGC-download / FTP / Zenodo / REST-item / Nextcloud / ArcGIS-Hub-REST / manual-download
(AI) pipeline_path enum vocabulary (Group C): raster / vector / tabular
(AI) integration_complexity enum (Group C): Low / Medium / High
decision enum (Group D): include / defer / reject
priority enum (Group D): High / Med / Low

The 5 LL slugs (for (AI) ll_relevance): east-brandenburg, havelland, north-hessian-loess, hessian-low-mountain, uelzen
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write export_source_catalogue.py and emit the seeded source_catalogue.csv mirror</name>
  <files>data-pipeline/python/export_source_catalogue.py, data/source_catalogue.csv</files>
  <read_first>
    - data-pipeline/python/export_variables.py (the exact analog: docstring format, ROOT/DATA path idiom, csv.DictWriter usage, utf-8-sig encoding, newline="", blank decision columns, single [done] print)
    - data/destatis_variables_catalogue.csv (the committed-mirror precedent: header row, encoding behavior)
    - .planning/phases/03.1-data-source-research-user-validation-inserted/03.1-PATTERNS.md "export_source_catalogue.py" and "source_catalogue.csv" sections (the full SOURCES tuple shape, FIELDS list, sort order, already-integrated convention)
    - .planning/phases/03.1-data-source-research-user-validation-inserted/03.1-RESEARCH.md "Catalogue Format" and "Source Inventory: Verified Facts Per Source"
    - CLAUDE.md (human-owned-file rule: scripts emit static text mirrors only)
  </read_first>
  <action>
    Create `data-pipeline/python/export_source_catalogue.py` modelled near-verbatim on `export_variables.py`. Use the same `ROOT = Path(__file__).resolve().parent.parent.parent` / `DATA = ROOT / "data"` idiom, `import csv` from stdlib only (do NOT import openpyxl or pandas — the script writes CSV from an in-memory data structure, no network calls). Docstring states output `data/source_catalogue.csv` and run command, mirroring the analog docstring format.

    Define a module-level `FIELDS` list with EXACTLY the 21 column headers in the locked order from the `<interfaces>` block — the four `(AI) `-prefixed headers must be byte-for-byte exact (single space after the closing paren). Define a `SOURCES` list of tuples (one tuple per row, 17 values covering column groups A-C), sorted by `source_group` then `source_id`.

    Seed `SOURCES` with:
    1. The three already-integrated rows (D-09) fully filled from RESEARCH.md / INTEGRATIONS.md facts: DESTATIS GENESIS (`tabular`, in-progress), DLR croptypes (`raster`, yes), BUeK250 soil (`vector`, yes). These three get their decision column pre-filled `include` in the write step (D-09 convention).
    2. One stub row per locked candidate source from D-07 + docs/data-sources.md (identity + access_url + citation_url populated from RESEARCH.md verified-facts tables where HIGH-confidence facts exist; otherwise leave Group B fact cells empty for Wave 2 to fill). Required candidate source rows: InVeKoS Brandenburg DFBK, InVeKoS Hesse (Landschaftselemente + LPIS-Referenzparzellen as separate layer rows per D-04), Europe-LAND IACS (Zenodo), Protected Areas (BfN WFS), DWD CDC climate, Agraratlas WCS, Thuenen land-use time series, UGR Ecosystem Atlas, JKI small structures, Niedersachsen InVeKoS Referenzparzellen, Degree of Urbanisation, INSPIRE Geoportal.

    Write the CSV with `csv.DictWriter(fieldnames=FIELDS)`, `encoding="utf-8-sig"`, `newline=""`. Decision columns (`decision`, `priority`, `rationale`, `reviewer_comments`) emit as empty strings for all rows EXCEPT the three already_integrated rows, which get `decision="include"`. End with `print(f"[done] {len(SOURCES)} source/layer entries -> {out}")`. The script must NEVER open or write `data/variables_catalogue.xlsx`.
  </action>
  <verify>
    <automated>cd data-pipeline && python python/export_source_catalogue.py && python -c "import csv,io; rows=list(csv.DictReader(io.open('../data/source_catalogue.csv',encoding='utf-8-sig'))); h=list(rows[0].keys()); assert h[0]=='source_id' and h[12]=='citation_url' and h[13]=='(AI) ll_relevance' and h[-1]=='reviewer_comments' and len(h)==21, h; ig=[r for r in rows if r['already_integrated'] in ('yes','in-progress')]; assert len(ig)>=3 and all(r['decision']=='include' for r in ig); print('OK',len(rows),'rows')"</automated>
  </verify>
  <acceptance_criteria>
    - `data-pipeline/python/export_source_catalogue.py` exists, imports only stdlib (`csv`, `pathlib`), and contains no `requests`/`openpyxl`/`pandas`/network calls.
    - Running the script exits 0 and prints a `[done] N source/layer entries -> ...source_catalogue.csv` line.
    - `data/source_catalogue.csv` has exactly 21 columns in the locked order; header index 12 is `citation_url`, index 13 is `(AI) ll_relevance`, last column is `reviewer_comments`.
    - At least 3 rows have `already_integrated` in {yes, in-progress} and each of those has `decision=include` (D-09).
    - Every locked D-07 candidate source has at least one row (Hesse appears as 2 or more layer rows per D-04).
    - The script does not reference `variables_catalogue.xlsx` anywhere (grep returns no match).
  </acceptance_criteria>
  <done>export_source_catalogue.py exists and regenerates data/source_catalogue.csv offline with the 21-column schema, already-integrated rows flagged + pre-decided, and one row per locked candidate source.</done>
</task>

<task type="auto">
  <name>Task 2: Build the source_catalogue review tab in variables_catalogue.xlsx</name>
  <files>data/variables_catalogue.xlsx</files>
  <read_first>
    - data/source_catalogue.csv (the just-generated mirror — source of the header row and seed rows)
    - .planning/phases/03.1-data-source-research-user-validation-inserted/03.1-RESEARCH.md "Existing xlsx Inspection Results" (current sheets: destatis_variables with 75 rows, spatial_data stub with 3 columns) and "Catalogue Format"
    - .planning/phases/03.1-data-source-research-user-validation-inserted/03.1-PATTERNS.md "variables_catalogue.xlsx — spatial_data sheet expansion" (target tab structure, human-owned constraint)
    - CLAUDE.md (variables_catalogue.xlsx is human-owned; this is the one-time scaffolding edit that creates the review tab — done via a standalone openpyxl script, NOT via export_source_catalogue.py)
  </read_first>
  <action>
    Add a `source_catalogue` review tab to `data/variables_catalogue.xlsx` using `openpyxl` (already installed, 3.1.5). Do this with a small one-shot scaffolding helper (e.g. inline `python -c` or a throwaway script — NOT inside export_source_catalogue.py, which must stay CSV-only). Load the existing workbook (preserve the `destatis_variables` 75-row sheet untouched), read `data/source_catalogue.csv` (utf-8-sig), and write its header + all rows into a sheet named `source_catalogue`. The header strings must match the CSV exactly, including the `(AI) ` prefixes.

    Replace the legacy 3-column `spatial_data` stub: either rename it to `source_catalogue` then overwrite its contents, or delete `spatial_data` and create `source_catalogue` fresh — do not leave the obsolete 3-column stub alongside the new tab. Freeze the header row (`ws.freeze_panes = "A2"`) so reviewers keep column headers visible while scrolling, matching the reviewer-friendly intent of D-01. Save the workbook in place. This is the only sanctioned programmatic write to the xlsx; after this scaffolding, the xlsx is human/AI-edited only.
  </action>
  <verify>
    <automated>cd data-pipeline && python -c "from openpyxl import load_workbook; wb=load_workbook('../data/variables_catalogue.xlsx'); assert 'source_catalogue' in wb.sheetnames; assert 'spatial_data' not in wb.sheetnames; assert 'destatis_variables' in wb.sheetnames; ws=wb['source_catalogue']; hdr=[c.value for c in ws[1]]; assert len(hdr)==21 and hdr[0]=='source_id' and hdr[13]=='(AI) ll_relevance' and hdr[-1]=='reviewer_comments', hdr; assert ws.max_row>=13; print('OK rows',ws.max_row)"</automated>
  </verify>
  <acceptance_criteria>
    - `data/variables_catalogue.xlsx` contains a `source_catalogue` sheet with the exact 21-column header (including `(AI) `-prefixed headers).
    - The legacy `spatial_data` 3-column stub no longer exists in the workbook.
    - The pre-existing `destatis_variables` sheet is still present and unmodified (still ~75 data rows).
    - The `source_catalogue` sheet contains one row per CSV data row (>= the seed count), header row frozen.
    - `export_source_catalogue.py` still does not touch the xlsx (the xlsx write happens only via the one-shot scaffolding helper).
  </acceptance_criteria>
  <done>variables_catalogue.xlsx has a source_catalogue review tab matching the 21-column CSV schema, the spatial_data stub is gone, and destatis_variables is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo commit ← catalogue files | Catalogue CSV/xlsx are committed to git; must not contain secrets |
| (no runtime/app boundary) | This wave produces a Python generator + static review files only; no app code, no network calls, no user-facing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Information disclosure | data/source_catalogue.csv, variables_catalogue.xlsx | mitigate | Catalogue stores only public source metadata (URLs, licenses, coverage). The DESTATIS GENESIS row records access_method involving an API token but MUST NOT embed the token value; token storage is deferred (CONTEXT.md). Acceptance check confirms no secret-looking strings in committed files. |
| T-03.1-02 | Tampering | export_source_catalogue.py | accept | Script is stdlib-only, offline, deterministic; no install step, no external input. No supply-chain surface this wave. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — all tooling (openpyxl, pandas, requests, pyyaml) already present per RESEARCH.md Package Legitimacy Audit. No install tasks, so no legitimacy checkpoint required. |
</threat_model>

<verification>
- `python data-pipeline/python/export_source_catalogue.py` runs offline (no network) and regenerates `data/source_catalogue.csv` deterministically.
- `data/source_catalogue.csv` and the `source_catalogue` xlsx tab share the identical 21-column header in the locked order.
- Already-integrated sources are flagged and pre-decided `include`; all other rows have blank decision columns.
- No secret/token value appears in either committed catalogue file.
</verification>

<success_criteria>
1. The review catalogue FORMAT is locked: a 21-column schema present identically in `data/source_catalogue.csv` and the `source_catalogue` xlsx tab, with verified-fact columns separated from `(AI) `-prefixed advisory columns (D-01, D-03, D-04).
2. `data-pipeline/python/export_source_catalogue.py` regenerates the CSV mirror offline and never writes the xlsx (D-02, human-owned-file rule).
3. Seed rows cover every locked D-07 candidate source plus the three already-integrated D-09 sources flagged and pre-decided `include`.
</success_criteria>

<output>
Create `.planning/phases/03.1-data-source-research-user-validation-inserted/03.1-01-SUMMARY.md` when done.
</output>
