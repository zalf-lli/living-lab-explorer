# Wave 2 Summary: Evidence-Chained Source Research

## Completed

- Accepted the Wave 2 checkpoint defaults: use Zenodo `15055561` as the Thuenen fallback if Nextcloud probing is unreachable, and keep JKI as a `[needs-verification]` row.
- Updated `data-pipeline/python/export_source_catalogue.py` with researched Group B fact columns and Group C `(AI)` advisory assessments.
- Regenerated `data/source_catalogue.csv` with 20 source/layer rows.
- Re-synced `data/variables_catalogue.xlsx` `source_catalogue` from the CSV, preserving `destatis_variables`.

## Verification

- `python data-pipeline/python/export_source_catalogue.py`
- Citation-chain check: every populated fact row has `citation_url`.
- Advisory check: every fact row has `(AI) pipeline_path` in `raster`, `vector`, or `tabular`.
- Guard checks: no LL slug appears in `spatial_coverage`; Hesse InVeKoS has multiple layer rows; large portals remain bounded.

## Notes For Wave 3

- Candidate decision columns remain blank; already-integrated rows retain `decision=include`.
- JKI access and several UGR/Niedersachsen fields remain explicitly marked `[needs-verification]`.
- Tabular rows note that `kind: tabular` does not yet exist in `sources.yaml`.
