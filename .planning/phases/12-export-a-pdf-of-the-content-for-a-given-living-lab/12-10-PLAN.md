---
phase: 12-export-a-pdf-of-the-content-for-a-given-living-lab
plan: 10
type: execute
wave: 5
depends_on: ["12-05", "12-06", "12-07", "12-08", "12-09"]
files_modified:
  - data-pipeline/R/report/template.qmd
  - data-pipeline/R/render_reports.py
  - data/reports/report-east-brandenburg-en.pdf
  - data/reports/report-east-brandenburg-de.pdf
  - data/reports/report-havellandisches-luch-en.pdf
  - data/reports/report-havellandisches-luch-de.pdf
  - data/reports/report-hessian-low-mountain-en.pdf
  - data/reports/report-hessian-low-mountain-de.pdf
  - data/reports/report-north-hessian-loess-en.pdf
  - data/reports/report-north-hessian-loess-de.pdf
  - data/reports/report-rheingau-en.pdf
  - data/reports/report-rheingau-de.pdf
autonomous: false
requirements: [D-01, D-05, D-09, D-10, D-11, D-12, D-13, D-14]

must_haves:
  truths:
    - "Each of the ten PDFs opens with a cover page carrying the Living Lab's name, tagline, region, NUTS-3 codes and a locator map"
    - "Each PDF then carries five tab sections in a fixed order, each with its KPI figures, its map, its chart and both narrative blocks"
    - "A Living Lab with no authored narrative for a tab shows that section without an empty text box"
    - "No manager or contact details appear anywhere in any report"
    - "Every PDF stays within an explicit committed size budget"
    - "Re-running the render reproduces all ten files without manual intervention"
  artifacts:
    - path: "data-pipeline/R/report/template.qmd"
      provides: "The full report document: cover page plus five tab sections"
      min_lines: 200
    - path: "data/reports/report-rheingau-en.pdf"
      provides: "One of the ten committed report artifacts"
  key_links:
    - from: "data-pipeline/R/report/template.qmd"
      to: "data-pipeline/R/report/sections.R, maps_vector.R, maps_raster.R"
      via: "source() in the setup chunk, then per-section function calls"
      pattern: "sections.R"
    - from: "data-pipeline/R/render_reports.py"
      to: "data/reports/report-{slug}-{lang}.pdf"
      via: "quarto render subprocess per (slug, lang) pair"
      pattern: "report-"
---

<objective>
Assemble the full report and produce the ten committed PDFs.

Everything this document needs already exists as a tested R function: plan 12-06's theme and
accessors, plan 12-07's KPI tables, narrative accessors and charts, plan 12-08's soil, land-value and
locator maps, and plan 12-09's crop-type, land-cover and climate maps. This plan writes the document
that arranges them, runs the real render, and commits the result.

Two things CONTEXT.md left open get decided here. First, the vertical ordering of KPI, map, chart
and text within a section (explicitly Claude's discretion). Second, the report's file-size budget —
CONTEXT.md flagged this as undiscussed with no ceiling locked, and RESEARCH.md's Pitfall 3
recommends setting a testable one rather than leaving it open. This plan locks it; plan 12-11
enforces it in pytest.

Purpose: the artifact the whole phase exists to produce.
Output: a complete `template.qmd`, ten committed PDFs, and a human sign-off on their content.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-export-a-pdf-of-the-content-for-a-given-living-lab/12-CONTEXT.md
@.planning/phases/12-export-a-pdf-of-the-content-for-a-given-living-lab/12-RESEARCH.md
@.planning/phases/12-export-a-pdf-of-the-content-for-a-given-living-lab/12-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Every function this document calls, with the plan that created it:

  12-06  ll_repo_root(), ll_tokens(), ll_meta(), ll_lab(slug), ll_str(key, lang, vars),
         ll_brand(slug), ll_boundary(slug), theme_ll_base(), theme_ll_map(),
         ll_legend_df(), ll_discrete_map_scale(), LL_TAB_ORDER, LL_TAB_CHART_LAYER, LL_FIG
  12-07  ll_kpi_df(slug, tab, lang), ll_narrative(slug, tab, slot, lang), ll_chart(slug, tab, lang)
  12-08  ll_map_soil(slug, lang), ll_map_economic(slug, lang),
         ll_map_locator(slug, lang), ll_locator_credit()
  12-09  ll_map_agriculture(slug, lang), ll_map_landscape(slug, lang),
         ll_map_climate_grid(slug, lang), ll_raster_sources_present()

Tab-to-map dispatch (this document owns the mapping):
  agriculture -> ll_map_agriculture      climate  -> ll_map_climate_grid
  soil        -> ll_map_soil             economic -> ll_map_economic
  landscape   -> ll_map_landscape

Size budget locked by this plan, enforced by plan 12-11:
  per PDF                                     8,388,608 bytes maximum   (8 MiB)
  ten committed source PDFs, total           52,428,800 bytes maximum   (50 MiB)
  two-copy footprint after sync (plan 12-11) 104,857,600 bytes maximum  (100 MiB)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the cover page and the five tab sections</name>
  <files>data-pipeline/R/report/template.qmd</files>
  <read_first>
    - data-pipeline/R/report/template.qmd (plan 12-05's skeleton — the frontmatter, params block, execute options and title-page chunk you are extending)
    - data-pipeline/R/report/sections.R, data-pipeline/R/report/maps_vector.R, data-pipeline/R/report/maps_raster.R (the finished modules — read the real function signatures and return types rather than trusting this plan's interfaces block)
    - app/src/pages/LLDetail.jsx lines 404-560 (`LayoutSplit`'s header block, which D-11 mirrors on the cover page minus `ContactManagerButton` per the Phase 10 D-19 precedent; and the tab body's KPI-then-map-then-chart-then-text arrangement, which is the reference for section ordering)
    - .planning/phases/12-export-a-pdf-of-the-content-for-a-given-living-lab/12-CONTEXT.md (D-09 all five tabs in one document; D-10 everything each tab shows including both narrative slots; D-11 cover page contents; Claude's Discretion covers vertical ordering within a section)
    - .planning/phases/12-export-a-pdf-of-the-content-for-a-given-living-lab/12-RESEARCH.md (Pitfall 2 on empty narrative slots; Pitfall 3 on file size)
  </read_first>
  <action>
Extend `template.qmd` from a title page into the full document.

Setup chunk: source `theme_llexplorer.R`, `sections.R`, `maps_vector.R` and `maps_raster.R` from
paths resolved through `ll_repo_root()`, and bind `slug` and `lang` from `params`. Call
`ll_raster_sources_present()` and stop with the missing paths listed if any source raster is absent,
so a broken environment fails at the top of the render rather than midway through page four.

Cover page, per D-11: the Living Lab's localized name and tagline, its `region`, its `nuts3` codes
labelled with `ll_str("report.regions", lang)`, a brand-coloured header band using
`ll_brand(slug)$color`, the locator map from `ll_map_locator(slug, lang)` captioned with
`ll_str("report.locatorCaption", lang)`, and the basemap attribution via
`ll_str("report.basemapCredit", lang, list(credit = ll_locator_credit()))`.

Do not render `ll_lab(slug)$manager` or `ll_lab(slug)$contact` anywhere in the document. D-11
excludes contact chrome from the cover following the Phase 10 D-19 precedent, and these PDFs are
published as static files on a public host — a personal email address inside a downloadable document
is a different exposure from one behind a mailto button on a page.

Section loop: iterate `LL_TAB_ORDER` and emit one section per tab. Within each section the vertical
order is, per this plan's discretionary decision:
1. Section heading, from `ll_str(paste0("layers.", tab), lang)`.
2. KPI figures, from `ll_kpi_df(slug, tab, lang)` as a compact table under
   `ll_str("report.kpiHeading", lang)`.
3. Map, from the dispatched map function, under `ll_str("report.mapHeading", lang)`.
4. Chart, from `ll_chart(slug, tab, lang)`, under `ll_str("report.chartHeading", lang)`, omitted
   entirely when it returns NULL.
5. Narrative: the `about` block under `ll_str("llDetail.aboutTheme", lang, list(layer = ...))` and
   the `challenges` block under `ll_str("llDetail.challenges", lang)`.
This is figures-before-prose, matching how `LayoutStacked` arranges the same content on screen.

Omit any narrative block whose `ll_narrative()` call returns NULL — no heading, no empty box, no
placeholder line. RESEARCH.md Pitfall 2 records this as an already-observable data gap rather than a
hypothetical: `havellandisches-luch` has three empty slots today. When both slots are NULL the
section simply ends after its chart.

Emit narrative text as literal text, never as a raw Typst passthrough. Do not use `results='asis'`
for authored content, and escape Typst-significant characters if the engine does not already. This
is authored free text from a human-owned file and it must not be able to alter document structure.

Give the climate section extra vertical room — `ll_map_climate_grid` returns an eight-panel
composition needing roughly a full page. Size every figure chunk from `LL_FIG` rather than with
per-chunk literals, so plan 12-06 stays the single place page geometry is tuned.

Add a running header carrying the Living Lab name and a footer with page numbers, if plan 12-05's
Typst partials do not already provide them.
  </action>
  <verify>
    <automated>python data-pipeline/R/render_reports.py --slug havellandisches-luch --lang en</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 and `python -c "import pathlib; p=pathlib.Path('data/reports/report-havellandisches-luch-en.pdf'); assert p.open('rb').read(5)==b'%PDF-'; print('OK', p.stat().st_size)"` prints `OK` and a size above 500,000 bytes (a title-page-only PDF would be far smaller)
    - Page count is plausible for the locked content: `Rscript -e "cat(pdftools::pdf_info('data/reports/report-havellandisches-luch-en.pdf')\$pages, '\n')"` prints at least 6
    - All five section headings appear in the extracted text: `Rscript -e "t <- paste(pdftools::pdf_text('data/reports/report-havellandisches-luch-en.pdf'), collapse=' '); for (k in c('Agriculture','Climate','Soil','Socio-economic','Landscape')) stopifnot(grepl(k, t, fixed=TRUE)); cat('OK\n')"` prints `OK`
    - No contact leakage: `Rscript -e "t <- paste(pdftools::pdf_text('data/reports/report-havellandisches-luch-en.pdf'), collapse=' '); stopifnot(!grepl('@', t, fixed=TRUE)); cat('OK\n')"` prints `OK`
    - The known-empty narrative slots produce no empty block: visually confirm the landscape section of `report-havellandisches-luch-en.pdf` ends after its chart, with no blank titled box
    - `grep -c "results='asis'\|results=\"asis\"" data-pipeline/R/report/template.qmd` returns 0
    - `grep -Ec "fig-width: [0-9]|fig-height: [0-9]" data-pipeline/R/report/template.qmd` returns 0 (figure dimensions come from `LL_FIG`, not per-chunk literals)
  </acceptance_criteria>
  <done>One Living Lab's full report renders with a cover page, five complete sections, no empty narrative boxes and no contact details.</done>
</task>

<task type="auto">
  <name>Task 2: Render all ten, enforce the size budget, and commit</name>
  <files>data-pipeline/R/render_reports.py, data/reports/report-east-brandenburg-en.pdf, data/reports/report-east-brandenburg-de.pdf, data/reports/report-havellandisches-luch-en.pdf, data/reports/report-havellandisches-luch-de.pdf, data/reports/report-hessian-low-mountain-en.pdf, data/reports/report-hessian-low-mountain-de.pdf, data/reports/report-north-hessian-loess-en.pdf, data/reports/report-north-hessian-loess-de.pdf, data/reports/report-rheingau-en.pdf, data/reports/report-rheingau-de.pdf</files>
  <read_first>
    - data-pipeline/R/render_reports.py (plan 12-05's driver — you are adding budget enforcement to its existing per-render validation)
    - .planning/phases/12-export-a-pdf-of-the-content-for-a-given-living-lab/12-RESEARCH.md (Pitfall 3: the mitigations, in order — explicit `fig-format: png` with a fixed DPI first, geometry simplification second)
    - data-pipeline/R/report/template.qmd (the `execute:` block's `fig-dpi`, the knob to turn if the budget is exceeded)
    - .planning/phases/07-add-boris-land-value-maps-as-spatial-layer-for-socio-economi/07-CONTEXT.md and the STATE.md Phase 8 entry (the project's precedent for treating a committed-footprint cap as a hard fail assertion rather than a warning — Phase 8's 209,715,200-byte cap was asserted inside its verify command)
  </read_first>
  <action>
Add budget enforcement to `render_reports.py`. After all requested renders complete, assert:
no single PDF exceeds 8,388,608 bytes, and the ten files together do not exceed 52,428,800 bytes.
Raise with the offending filenames and their sizes on failure. Follow Phase 7 and Phase 8's
precedent of a binding fail assertion with literal byte constants, not a printed warning.

These figures are this plan's decision, not a locked CONTEXT.md decision — CONTEXT.md's Claude's
Discretion section records the budget as undiscussed. Record the rationale in the SUMMARY: 8 MiB per
file keeps a report comfortably emailable and downloadable on a poor connection, and 50 MiB total
keeps the repository's growth from this phase to roughly a third of what Phase 8's PMTiles cap
allowed, for an artifact that is regenerated far less often.

Run the full ten-file render. If the budget assertion fails, apply RESEARCH.md Pitfall 3's
mitigations in order and re-run: first lower `fig-dpi` in `template.qmd` (150 to 120 is a modest
visual change), then simplify the heaviest boundary geometry — the BORIS layer, whose largest
Living Lab holds roughly 30,000 zones. Do not raise the budget to make the assertion pass; if the
content genuinely cannot fit, stop and report it as a finding for the checkpoint.

Print each file's size and the total after the run, so the SUMMARY can record real measurements
rather than estimates.

Commit all ten PDFs under `data/reports/`. This is the directory plan 12-11's `sync_reports()` will
publish from. Do not create or populate `app/public/data/reports/` here — that is plan 12-11's job,
and creating it early would leave an unsynced copy that its byte-identity test would then lock in.
  </action>
  <verify>
    <automated>python data-pipeline/R/render_reports.py</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 and prints a per-file size line for all ten reports plus a total
    - `python -c "import pathlib; f=sorted(pathlib.Path('data/reports').glob('report-*-*.pdf')); assert len(f)==10; assert all(x.open('rb').read(5)==b'%PDF-' for x in f); assert all(x.stat().st_size <= 8388608 for x in f), [(x.name,x.stat().st_size) for x in f if x.stat().st_size>8388608]; t=sum(x.stat().st_size for x in f); assert t <= 52428800, t; print('OK', len(f), t)"` prints `OK 10` and the total
    - Budget enforcement is real, not decorative: temporarily lowering the per-file constant in `render_reports.py` to 1000 makes a re-run fail with the offending filenames listed; restore the constant afterwards
    - All ten files are tracked by git: `git status --porcelain data/reports/` shows ten added files and `git check-ignore -q data/reports` exits non-zero
    - `test ! -d app/public/data/reports` succeeds (plan 12-11 owns that directory)
    - Both languages differ in content, not just in filename: `Rscript -e "a <- paste(pdftools::pdf_text('data/reports/report-rheingau-en.pdf'), collapse=' '); b <- paste(pdftools::pdf_text('data/reports/report-rheingau-de.pdf'), collapse=' '); stopifnot(a != b); stopifnot(grepl('Kennzahlen', b, fixed=TRUE)); cat('OK\n')"` prints `OK`
    - `python -m pytest data-pipeline/tests/ -q` still fully green
    - The SUMMARY records the ten measured file sizes, the total, and whether any Pitfall 3 mitigation had to be applied
  </acceptance_criteria>
  <done>Ten complete, budget-compliant, bilingual reports are committed under data/reports/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Bilingual content review of the ten reports</name>
  <what-built>
Ten complete reports are committed under `data/reports/`, one per Living Lab per language. Each
opens with a brand-coloured cover page carrying the Living Lab's name, tagline, region, NUTS-3 codes
and a tiles-backed locator map with a Germany inset, then presents five sections — agriculture,
climate, soil, socio-economic, landscape — each with its KPI figures, its map, its chart and its
narrative text.

Automated gates already passed: every file is a valid PDF within the 8 MiB per-file and 50 MiB total
budget; the five section headings appear in every document; no email address appears anywhere; every
map legend matches the web app's own legend for that layer (verified by the R gates against the
JavaScript implementations); every chart re-plots the committed chart JSON rather than recomputing
anything.

What automation cannot check is whether these read as reports you would send to a stakeholder.
  </what-built>
  <how-to-verify>
1. Open `data/reports/report-rheingau-en.pdf` end to end. Confirm each of the five sections is
   complete and legible, no figure is cut off at a page boundary, and no legend is too small to read
   at print size.
2. Open `data/reports/report-rheingau-de.pdf`. Confirm every visible string is German — headings,
   KPI labels, units, legend entries, climate period labels, captions. Report any English leakage.
3. Open `data/reports/report-havellandisches-luch-en.pdf` and go to the landscape section. That
   Living Lab has no authored narrative for that tab, so the section should end cleanly after its
   chart with no empty titled box. Confirm it does.
4. Open `data/reports/report-east-brandenburg-en.pdf` and compare its soil map and its land-value
   map against the same two tabs in the running web app. The classes, colours and legend entries
   should match. The land-value legend's euro ranges should match the browser's for that Living Lab.
5. Check the climate section of any report. Confirm there are eight maps — baseline and 2071-2100
   change for each of gdd, mean temperature, precipitation and warmest-quarter precipitation — each
   with its own legend and unit, and that no ring of odd-coloured cells appears at the boundary.
6. Confirm the cover locator map places the Living Lab correctly and that the basemap attribution
   line is present and legible.
7. Confirm the KPI tables show an explicit not-available marker where a figure is genuinely
   unavailable, rather than a blank cell.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to publishing, or list the defects by report filename and section.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human-authored narrative and names → Typst document structure | Free text from `ll_content.json` is placed into a typeset document |
| `ll_metadata.json` personal fields → publicly downloadable PDF | The metadata carries manager names and email addresses |
| Repository size → deployment | Ten binary artifacts are committed and later duplicated by sync |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-46 | Tampering | Authored narrative altering document structure | mitigate | Task 1 forbids `results='asis'` for authored content (asserted by grep) and requires literal-text emission with Typst-significant characters escaped, so a `#`-prefixed or bracketed string in a narrative field cannot become markup |
| T-12-47 | Information disclosure | Manager or contact details in a public PDF | mitigate | Task 1 forbids rendering `manager` and `contact`; Task 1's acceptance criteria assert no `@` character appears in the extracted text of a rendered report, which catches an email address regardless of how it got there |
| T-12-48 | Denial of service | Repository and download bloat | mitigate | A hard byte assertion inside the render driver, following Phase 7 and Phase 8's precedent of binding caps rather than warnings; Task 2 requires the assertion to be proven real by making it fail |
| T-12-49 | Repudiation | A report that silently misrepresents its data | mitigate | The blocking human checkpoint compares two maps directly against the running web app, and the upstream R gates already assert legend and bucket parity against the JavaScript implementations |
| T-12-50 | Tampering | Budget met by lowering standards instead of fixing size | mitigate | Task 2 explicitly forbids raising the budget to make the assertion pass and prescribes the mitigation order (DPI first, geometry simplification second), with a stop-and-report path if neither suffices |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; all R packages are pinned in `renv.lock` from plan 12-02 and `pdftools` used in the verification commands is among them |
</threat_model>

<verification>
- `python data-pipeline/R/render_reports.py` renders all ten and exits 0 with the budget assertions passing
- All ten files start with `%PDF-`, none exceeds 8,388,608 bytes, and the total is at most 52,428,800 bytes
- Extracted text of an English report contains all five section headings and no `@` character
- An English and a German report of the same Living Lab differ in extracted text
- `python -m pytest data-pipeline/tests/ -q` green
- All four R gates still exit 0
- Human checkpoint (Task 3) approved
</verification>

<success_criteria>
- `template.qmd` renders a cover page per D-11 plus all five tab sections per D-09, each carrying KPIs, map, chart and both narrative slots per D-10
- Empty narrative slots are omitted entirely rather than rendered as empty boxes
- The climate section carries the eight maps D-12 specifies
- No manager or contact detail appears in any report
- Ten PDFs are committed under `data/reports/`, each within 8 MiB and totalling at most 50 MiB, with the budget enforced by a failing assertion in the render driver
- `app/public/data/reports/` does not yet exist
- A human has reviewed the reports in both languages and approved them
</success_criteria>

<output>
Create `.planning/phases/12-export-a-pdf-of-the-content-for-a-given-living-lab/12-10-SUMMARY.md` when done
</output>
