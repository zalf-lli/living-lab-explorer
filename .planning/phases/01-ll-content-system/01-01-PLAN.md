---
phase: 01-ll-content-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - data/ll_content.json
  - data-pipeline/python/fetch_nuts.py
  - data-pipeline/python/generate_metadata.py
  - data-pipeline/sync.py
  - data/ll_metadata.json
  - app/public/data/ll_metadata.json
autonomous: true
requirements:
  - CONTENT-01
  - CONTENT-02
user_setup: []
must_haves:
  truths:
    - Editing a tagline in `data/ll_content.json` changes the merged `data/ll_metadata.json` after running `python data-pipeline/sync.py`.
    - Human-authored fields override any conflicting computed metadata fields during merge.
    - The pipeline continues to emit metadata for all five existing LL slugs without changing geometry outputs.
  artifacts:
    - path: data/ll_content.json
      provides: Single hand-authored LL content source including bilingual copy and display identity fields
      contains: '"east-brandenburg"'
    - path: data-pipeline/python/generate_metadata.py
      provides: Merge logic combining authored content with computed metadata
      exports: ["build_metadata", "load_ll_content", "main"]
    - path: data-pipeline/sync.py
      provides: Sync path that regenerates or copies merged metadata into app/public/data
      contains: ll_metadata.json
  key_links:
    - from: data-pipeline/python/generate_metadata.py
      to: data/ll_content.json
      via: JSON load before merge
      pattern: ll_content\.json
    - from: data-pipeline/sync.py
      to: app/public/data/ll_metadata.json
      via: sync copy after metadata generation
      pattern: ll_metadata\.json
---

<objective>
Create the Phase 1 metadata source-of-truth in the pipeline.

Purpose: Remove the current need to edit LL copy and display identity across multiple files by introducing a single authored JSON input and a deterministic merge step.
Output: A committed `data/ll_content.json`, a dedicated metadata generator, and merged `ll_metadata.json` outputs synced into the app.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ll-content-system/01-RESEARCH.md
@.planning/REQUIREMENTS.md
@data-pipeline/python/fetch_nuts.py
@data-pipeline/sync.py
@data/ll_metadata.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the hand-authored LL content contract</name>
  <files>data/ll_content.json</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, data-pipeline/python/fetch_nuts.py, data/ll_metadata.json</read_first>
  <action>Create `data/ll_content.json` covering all five LL slugs. Include every authored field required by `CONTENT-01`: bilingual tagline and description blocks, NUTS-3 code arrays, brand colours, dark variant colours, outline colours, short region labels, icon identifiers, LL ordering/numbering needed by the UI, and the `mock` flag. Preserve existing slugs exactly and seed the file from current `fetch_nuts.py` metadata plus `app/src/data/ll_display.js` values so the migration starts behavior-preserving rather than aspirational.</action>
  <verify>rg -n "\"(east-brandenburg|havelland|north-hessian-loess|hessian-low-mountain|rheingau)\"" data/ll_content.json</verify>
  <acceptance_criteria>
    - `data/ll_content.json` contains all five existing LL slugs
    - `data/ll_content.json` contains bilingual tagline and description data for each LL
    - `data/ll_content.json` contains `mock`, `nuts3`, `color`, `colorDark`, `outlineColor`, and icon identity fields
  </acceptance_criteria>
  <done>The repo has a single authored JSON file with all LL content and display identity fields needed by Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Move metadata assembly into a dedicated generator with authored-field precedence</name>
  <files>data-pipeline/python/generate_metadata.py, data-pipeline/python/fetch_nuts.py, data-pipeline/sync.py, data/ll_metadata.json, app/public/data/ll_metadata.json</files>
  <read_first>data-pipeline/python/fetch_nuts.py, data-pipeline/sync.py, data/ll_content.json, data/ll_metadata.json</read_first>
  <action>Implement `data-pipeline/python/generate_metadata.py` as the sole builder of `data/ll_metadata.json`. Have it read `data/ll_content.json`, merge authored fields with computed fields, and make authored values win on key collisions. Refactor `fetch_nuts.py` so it continues owning geometry/NUTS source generation but no longer hand-authors display copy inline. Update `sync.py` so running `python data-pipeline/sync.py` regenerates or verifies merged metadata before copying it into `app/public/data/ll_metadata.json`. Keep the output contract compatible enough for the frontend migration plan to build on immediately.</action>
  <verify>python data-pipeline/sync.py</verify>
  <acceptance_criteria>
    - `data-pipeline/python/generate_metadata.py` exists and contains reusable merge helpers
    - `fetch_nuts.py` no longer hardcodes the bilingual authored LL copy as the canonical metadata source
    - Running `python data-pipeline/sync.py` updates both `data/ll_metadata.json` and `app/public/data/ll_metadata.json`
    - Changing a tagline in `data/ll_content.json` is reflected in the merged metadata after rerunning `sync.py`
  </acceptance_criteria>
  <done>The merged metadata pipeline is deterministic, authored fields take precedence, and the synced app asset reflects changes from the new source file.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python data-pipeline/sync.py` succeeds from the repo root
- [ ] `data/ll_content.json` remains hand-authored input and is not overwritten by pipeline scripts
- [ ] `data/ll_metadata.json` and `app/public/data/ll_metadata.json` both reflect a deliberate edit to one tagline
</verification>

<success_criteria>

- All tasks completed
- `CONTENT-01` and `CONTENT-02` are satisfied by code and generated data
- The repo has exactly one human-authored LL content source
- Metadata generation is separated cleanly from geometry fetching
  </success_criteria>

<output>
After completion, create `.planning/phases/01-ll-content-system/01-01-SUMMARY.md`
</output>
