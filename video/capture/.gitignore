node_modules

# Raw Playwright recordings, transcodes, run logs and diagnostic frames — all regenerated by
# `npm run capture`. The finished clips land in video/remotion/public/captured/, which is
# ignored by that project's own .gitignore.
out
