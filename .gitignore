# Python
__pycache__/
*.pyc
.venv/
venv/

# Env / system
.env
.DS_Store

# Data pipeline
data/_cache/
data/croptypes_2024.tif
data/io_lulc_*.tif
data/boart1000_ob_v20
data/pmtiles/*.tmp
data/buek250_mgm_utm_v60*
# Germany-extent CHELSA rasters (baseline + change fields), rebuildable by
# fetch_climate.py. The committed artifacts of this layer are the per-LL
# PMTiles plus data/climate_kpis.json and data/climate_color_breaks.json,
# not these intermediates.
data/climate_source/

# React app (Vite)
app/node_modules/
app/dist/
app/dist-ssr/
app/.vite/

# Runtime-built geodata in the app (copied/generated from /data by the pipeline)

# Legacy Quarto artifacts (kept so old branches / local experiments stay ignored)
_site/
.quarto/
/.quarto/
**/*.quarto_ipynb

# Editor / tooling
*.log
.vscode/*
!.vscode/settings.json
.idea/
.claude/
IMPROVEMENTS.md
